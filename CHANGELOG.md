# 更新日志 · CHANGELOG

> **中南大学生工作台** —— 面向高校辅导员的本机学生工作台。
> 把教务 / 学工系统导出的学生信息表 + 成绩单放进来，自动拼出每个人的画像、成绩、宿舍与学业预警。
> **数据只存本机，全程不联网、不上传服务器。**
>
> 版本写法：`v0.x` 是**网页原型阶段**（未对外发布）；`v1.0` 起是 **Windows 桌面版**。
> 安装包文件名一律带版本号：`中南大学生工作台-v<版本>-<用途>.<扩展名>`。

---

## 🙏 特别鸣谢

本项目的产品思路，受到 **[辅导员 AI 工作台 · counselor-ai-workbench v1.1.0](https://github.com/L-uo/counselor-ai-workbench/releases/tag/v1.1.0)** 的启发，在此特别致谢。

具体借鉴的是这些**做事的思路**（不是代码）：

| 借鉴的思路 | 本项目里的落法 |
| --- | --- |
| **免费使用、本地数据优先、无需注册软件账号** | 全程离线运行、数据只落本机磁盘；不设账号体系，打开即用（只有一个本地访问密码） |
| **把辅导员日常零散事务收进一个工作台** | 学生档案 + 成绩 + 宿舍 + 导入历史 + 备份 + 备忘清单，一屏之内 |
| **校务系统导航**（把校内各平台收在一处） | 「校务导航」页：本校 87 个站点按分区归类，可搜索、可收藏 |
| **常用模板 / 学校制度库**（内容库形态） | 「常用模板」12 条预置文案、一键复制；「AI 辅导员·鹿晓南」扫码即用 |
| **批量导入要能吃下各校格式不一的 Excel** | 字段别名表 + 三种导入模式（合并 / 覆盖 / 新建批次），表头不在第一行也能自动定位 |
| **备份与恢复是刚需，要显眼** | 备份页常驻侧栏，并显示"上次备份：昨天 · 已 1 天未备份"这类人话提醒 |
| **AI 可选、不用也能干活** | 本项目**完全不含 AI 功能**——连 AI 都不要，全部能力纯本地 |

需要说明的是：对方项目**当前未开放源代码**，本项目是**独立实现**，不是它的 fork 或二次分发。
我们的技术路线也不同：整个产品是一个**单文件 HTML**（无框架、无构建），外面套一层很薄的
Tauri 2 桌面壳，只负责网页做不到的事（导出落盘、外链用系统浏览器打开、缩到系统托盘）。

---

## [v1.9.8] — 2026-09-23

**四条来自实际使用的反馈：一个多余的按钮、一批要打包的表单、一层可自定义的关注逻辑、一页太啰嗦的指引。**

**① AI 辅导员页去掉「在浏览器打开」**

那两个入口是**企业微信内**的，在普通浏览器里打开没有意义，留着只会让人点了困惑。
现在只留「复制链接」（粘进班级群）与二维码两条真正有用的路径。

**② 八份表单模板打包进程序**

证明、介绍信、登记表、报销单这类**有固定版式的表单**，复制文字没有意义 —— 要的是原件格式。
所以把**文件本身**内联进了程序（base64），在「常用模板」页新增「📎 表单模板（内置 8 份）」，
按 证明·推荐 / 介绍信 / 登记表 / 报销 四组摆放，点「另存到下载」即得一份干净原件。

> **为什么内联而不是放附件文件夹**：绿色版最大的卖点是「一个 exe，拷到哪儿都能跑」，
> 多一个附件文件夹就把这个卖点破坏掉了。内联之后浏览器版 / Windows / macOS 三个平台一样能用，
> 也不受"把桌面模板文件夹挪走 / 改名"影响。代价是原型 +416 KB，对 1.8 MB 的单文件可接受。

**③ 关注标签：可自定义，「需重点关注」也认它**

原先「需重点关注」只认两类**有数据来源**的信号：成绩表的「不及格门数」、学生表的「挂科情况 / 军训备注」。
可辅导员真正在跟的事常常没有数据来源 —— 心理状态、单亲、人际紧张。

- 新增**关注标签**：预置 🎭心理 / 👩‍👦单亲 / ⛄人际，可自行增删改
- 学生详情里点一下即可标记 / 取消，一个学生可打多个标签
- **列表里默认只显示图标**（如「方雨涵 🎭」），名称放在鼠标悬停提示里 —— 不占地方也不扎眼
- 「需重点关注」把带标签的学生也算进去；另加「带标签学生」快捷视图；筛选条加「关注标签」下拉
- 标签**存的是图标本身**而不是内部编号：导出的 Excel 里就是这些图标，也能在 Excel 里手写后导回

**④ 导入指引页瘦身（并修掉一个被当成 bug 的地方）**

- 全站改名：「取数 · 导入指引」→ 「**导入指引**」（原名字太长会换行，20 多处一起改）
- 删掉与编号卡片重复的四段流程条、与「系统设置 → 关于」逐字重复的关于块
- 头部按钮缩成短词（新手引导 / 页面导览）
- ⚠️ **修掉一个真问题**：原「继续第 ④ 步 · 导入成绩」只要"有学生"就**一直挂着** ——
  成绩早就导完了它也不消失；而且它的「④」是*向导第 4 步*，与页面第 4 张卡片（"几个容易踩的坑"）
  编号对不上，看起来就像 bug。现在**只在「有学生、且还没导成绩」时出现**，并改叫「继续：导入成绩单」。

**顺手修：** `app/package.json` 的版本号还停在 1.9.6（上一轮升 1.9.7 时漏改了这一个），本轮统一。

---

## [v1.9.7] — 2026-09-23

**这一版主要动"数据进出"和"备份"，顺带把总览页的卡片理齐。**

- **顶栏多了个「⋯」菜单**：把导出、备份、批次管理、系统设置这些不常点的入口收进去，
  顶栏不再一排按钮挤着。
- **备份历史**：每次导出备份、或"覆盖批次前自动备份"，都会记一条（时间 / 类型 / 文件名 / 批次）。
  可以在列表里一键定位到该文件（资源管理器里高亮），也能直接删除。配套新增两个外壳命令
  （定位 / 删除本机文件），**只对「下载」目录里的文件生效**。
- **统计条重构**：原来用 auto-fit 排，列数会飘（3 列 / 5 列），加上"暂无成绩"卡片条件性出现，
  整行高度参差不齐；现在固定 4 列（窄屏 2 列）等高对齐。数据总览的卡片也一并重排。
- **导入按学号去重**（硬约束）：一份表里同一学号出现多行（系统导出偶尔会这样）时，
  自动合并并提示合并了几行 —— 不会造出重复的人。三种导入模式都适用。
- **数据管理入口聚合**：导入 / 导出 / 备份 / 批次管理收进一个紧凑卡片
  （v1.9.8 里又按"你要干什么"重整为三组）。

---

## [v1.9.6] — 2026-09-23

**一条"太打扰"的反馈 + 一个会丢数据的 bug + 两个新功能。**

- **备份提醒降频**（用户反馈"提示备份太频繁了，很打扰"）：
  旧版只要导入过数据且没备份，**每次导入都弹**，点「稍后再说」也没用。
  新版三层闸门：① 系统设置 → 数据管理 里新增总开关「导入后提醒备份」；
  ② 弹过一次（含被关掉）后至少隔 **7 天**；③ 只在"从没备份过"或"距上次备份超 30 天"时才弹。
  「上次备份」状态行不受开关影响，始终显示。
- **⚠️ 修掉一个会静默丢数据的缺陷**：`setStudents()` 只在"存在当前批次"时才把名单写进批次，
  而存档持久化的正是批次。于是把批次**全删光**后再选「新增 / 合并」导入时，
  结果页显示"导入成功、新增 78 人"，但数据**没有落进任何批次** —— 重开程序「全部学生」就是空的。
  现在导入前若无批次会自动新建（用文件名命名）并在结果页说明；`setStudents()` 另加兜底，
  任何代码路径都不可能再把数据只写进内存。
- **导入方式三卡紧凑化**：原来三张卡竖排、每张带一段说明，小屏上第三张「新建批次」
  被挤出可视区（用户反馈"小屏看不到新建批次"）。现在改成一行三个小块（标题 + 短标签），
  详细规则由下方按选中项显示；窄屏落成单列，但每张都是单行紧凑卡。
- **导入时可勾选字段**（用户反馈"可勾选导入信息，确定哪些不要"）：
  确认页的"表头识别结果"表新增「导入」列，默认全选，取消勾选的列不参与导入；
  学号标记「必导入」不可取消；实时显示「本次将导入 7 / 8 个字段」；
  名册导入若排除了「政治面貌」会当场红字提醒（否则自动同步会静默失效）。
- **导出 Excel（维护用）**：一个 .xlsx 两张工作表（学生信息 / 成绩），
  各自都能被对应的导入入口直接吃回去 —— 形成「导出 → 在 Excel 里大批量改 → 导回」的闭环。
  学号按文本写（不会变 `2.02E+11`），只导出有内容的列。
  ⚠️ 开发中撞到一个会写出 0 字节文件的坑：`XLSX.write({type:'array'})` 返回的是 ArrayBuffer，
  `Array.from(ArrayBuffer)` 得到空数组 —— 现统一包一层 `Uint8Array` 再取字节。
- **常用模板 · 我的模板文件**：可以登记本机的 PDF / Word / Excel（只记路径、不复制内容），
  点一下用对应程序打开；「从文件夹添加」会递归列出文件夹里的文档供勾选。
  配套新增外壳命令 `list_doc_files`，并给"打开本地文件"的白名单加上"已登记的模板文件"。

---

## [v1.9.5] — 2026-09-23

**新增 macOS 版；顺手修掉一个"到了 Mac 上就会现形"的小缺口。**

**新增 · macOS 版（同一份代码，不改产品逻辑）**

现在除了 Windows 三个安装包，还多一个 **macOS 安装包**，同一个 dmg 同时支持
**Intel 芯片与 Apple 芯片（M 系列）**，macOS 10.15 及以上。

好消息是这次"移植"几乎没改代码 —— 因为这一层的底子本来就是跨平台的：

- 前端是同一个单文件页面，界面早就适配过窄屏，**一行没动**；
- Rust 外壳只有 12 个命令，其中打开外链、打开本地文件、数据读写、系统托盘
  本来就跨平台；**开机启动更是早就写好了 macOS 的 LaunchAgent 分支**。

真正要改的只有一处：

- ⚠️ **`downloads_dir()` 原先只读 `USERPROFILE`** —— 那是 Windows 专有的环境变量。
  到了 macOS 上取不到，就会**静默退回系统临时目录**：辅导员点了导出，
  文件落在 `/tmp` 里，他永远找不到（表现为"点了导出没反应"）。
  现在改为优先用 Tauri 的跨平台路径解析（`app.path().download_dir()`），
  再退回 `HOME`/`USERPROFILE`，最后才退临时目录。

另外补了两样打包需要的东西：macOS 必需的 `icons/icon.icns`，
以及只在 macOS 生效的 `tauri.macos.conf.json`（打包目标用 app + dmg、
最低系统版本、dmg 窗口布局）。**Windows 的打包配置一个字没动。**

> **怎么编出来的？** 苹果的工具链只能在 macOS 上跑，Windows 上编不出 Mac 程序
> —— 这是苹果的限制，不是配置问题。所以改用 GitHub 的 macOS 跑器来编译
> （公开仓库免费）：新增 `.github/workflows/build-macos.yml`，
> 推代码或点一下按钮就自动出一个通用二进制的 dmg。

> ⚠️ **没有苹果开发者账号，所以只能做临时签名**：使用者第一次打开时系统会拦一下，
> 需要「右键 → 打开」放行一次。手册里已写明这一步，已经在群里发之前先看一眼。

**手册同步更新**

《用户使用手册》补上 macOS 的安装与首次打开、两个平台的运行环境差异、
以及 macOS 下的数据目录、`⌘ + Q` / `⌘ + P` 等按键习惯 —— 手册现在同时覆盖两个平台。

**Windows 端一并重出**

因为改动了 Rust 侧代码，Windows 三个安装包也一起重新构建，与 macOS 版同为 1.9.5。

---

## [v1.9.4] — 2026-09-23

**本次只动"讲清楚"这件事，没有数据格式变更，升级不会动你已有的数据。**

**新增 · 引导浮窗补上「常用功能」四步**

原来那份浮窗批注导览只讲"界面上这块是什么"（批次、导入、侧栏、筛选、关注视图、个人资料），
但侧栏里那几个**天天要用的功能**一直没讲 —— 光看名字看不出好处。这次给它们各加一步：

- **校务导航**：教务、学工、图书馆这些校内系统集中在一页，不用记网址。它们走的是**同一套统一身份认证**，
  **登录任意一个，在登录状态还有效的这段时间里，点开其他几个就不用再登一遍**，一个页面来回切很方便。
- **常用模板**：现成文书文案，点开「复制全文」贴到 Word / 微信改两个字就能用。
  **内容还在陆续补** —— 手上有好用的模板欢迎提供，攒够一批一起更新进去。
- **AI 辅导员**：制度流程问鹿晓南、综合评定的口径问答疑机器人，都是校内的答疑好帮手。
  **私信它之后可以把它拉进自己的班级群**，重复的问题学生自己就能问；两张码下面都有「复制链接」。
- **校历作息**：学年校历 + 课堂时间表都在这一页（下课、熄灯、行政上班时间也都有），
  下查寝、排谈话前瞄一眼，不用翻文件。

导览因此从 7 步变成 **11 步**。

**顺带修掉一个"讲不到"的隐患**

这四步的锚点都在侧栏「常用工具」分组里，而这个分组**可以折叠**。
折起来锚点就不存在，导览会按既定规则**静默跳过**那几步 —— 等于白加。
现在导览开始时会**临时展开**该分组，结束时**还原成用户原来的样子**（不擅自改他的偏好，
原本没动过这个设置的话，收尾也不会在数据里留下多余的键）。

**用户手册重写：只讲当前版本**

原来的手册是「1.0.0 基础版 + 从 v1.4 到 v1.9 一长串版本更新说明」堆起来的，
越读越长、还得自己判断"哪条已经被后面的改动推翻了"。现在整本重写成**只描述当前版本的样子**：

- 12 章：这是什么 / 安装与首次启动 / 第一次把数据取进来 / 界面导览 / 日常使用 /
  数据总览与宿舍看板 / **常用功能（校务导航 · 常用模板 · AI 辅导员 · 校历作息）** /
  批次与导入历史 / 个人中心与系统设置 / 数据安全 / 常见问题 / 版本与联系。
- 手册里出现的每个数字都与程序实际行为逐项核对过（模板 12 条、校务导航 87 条 9 分区、
  新手向导 6 步、浮窗导览 11 步、关注视图 8 类……），不再出现"文档写着、程序没有"的情况。
- 版本历史挪到 `CHANGELOG.md`，手册不再承担更新日志的职责。

---

## [v1.9.3] — 2026-09-23

**本次全是"用起来顺手"的调整，没有数据格式变更，升级不会动你已有的数据。**

**界面 · AI 辅导员页重排**

原来的版面是三块信息挤在一起：左卡带二维码、右下卡横跨两列、旁边再挂一张
「什么问题适合问鹿晓南」的说明卡，看着乱。现在改成**两人各占一张卡、左右等宽、同一行起平**：

- 删掉「什么问题适合问鹿晓南」那张说明卡 —— 它讲的内容（请假销假、奖助学金、缓考学籍）
  本来就是左卡该说的话，直接并进左卡的三条要点里，不再单占一块。
- 每张卡从上到下的次序统一：**标题 → 一句话分工 → 二维码 → 能问什么 → 按钮**，
  两张卡长得一模一样，眼睛不用在两个版式之间切换。
- 窄屏（< 980px）自动落成上下两栏，不会挤成一条。

**界面 · 两张二维码统一格式**

原来两张码的图片尺寸、留白、底色都不一样（一张 468×586 带深色背景和标题字，
一张 284×284 是纯码），并排放着明显不齐。现在：

- **去掉背景，只留二维码本身** —— 鹿晓南那张码从带深色底板的整图裁出纯码（378×378）。
- 两张都用同一个 **200×200 白底圆角外框**，图片按比例居中填满，**外框尺寸完全一致**。
- 白底是**写死在外框上**的，不是跟着主题走：切到深色主题时二维码依然是白底黑码，
  不然"黑底上的黑码"根本扫不出来。

**界面 · 「关于」里的二维码删掉，改放在真正有用的地方**

关于页是看版本号和作者的地方，塞二维码没意义，已删（软件 / 存储 / 作者 / 版本号都还在）。
二维码留在 AI 辅导员页就够了，并且**两张码下面各加了一个「复制链接」按钮**：

- 按一下把该机器人的**企业微信链接**复制到剪贴板，可以直接粘进企业微信群 / 班级群，
  学生点开就能用，不必要求每个人都会"扫二维码"。
- 旁边保留「在浏览器打开」，方便你自己先看一眼落地页长什么样。

**界面 · 筛选从"点一个就收起"改成复选框**

以前的筛选是系统自带的下拉框，**勾一个值就自动收起来**，想按"男 + 女"或者
"党员 + 积极分子 + 团员"这种多值筛，只能反复点开、反复找，很烦。现在换成自绘的复选框面板：

- 点一下展开，里面每个取值一个复选框，**勾选立刻生效、面板不关**，
  一次就能把几个值都勾上，最后点「完成」或点面板外面收起。
- **按钮上直接显示当前选了什么**（如"男、女"），选多了就显示角标数字，不用点开也知道筛了啥。
- 面板里有「清除本组」，只清这一个字段，不动你设的其它条件。
- 语义没变：**同一组内多个值是「或」，不同组之间是「且」**（仍沿用原来的筛选逻辑）。
- 「总览」页的筛选条一起换了；**「预设视图」那一个仍是单选**——它本来就是"整套视图"，不是多选。

**顺带修掉一个自己挖的坑**

v1.9.3 加按钮时把主按钮类名写成了 `class="btn primary"`，而本项目 CSS 里主按钮叫 `.btn.pri`，
`.btn.primary` 压根不存在 —— 于是「复制链接」和面板里的「完成」**静默退化成白按钮**：
不报错、不崩溃，只是变丑。已改回 `.btn.pri`，并给语法检查加了一条静态门禁：
**扫出「用了但 CSS 里没定义」的按钮类名就直接报错**，以后写错类名当场拦下，不会再无声无息。

---

## [v1.9.2] — 2026-09-23

**修复：二次导入名册，要么进来一堆"软件模板人员"，要么一个人都进不来**

拿「入党积极分子名册」去更新学生的政治面貌时，会出现两种都很难受的结果：
名册是 **`.xls`（Excel 97-2003）** 时，导进来的是**一堆"软件模板里的人"**；
换成 `.xlsx` 之后，又变成**导入 0 人**。是**两个根因**叠在一起：

**根因一 · 内置的表格解析库是 SheetJS 的精简版，根本不支持 `.xls` 旧格式。**
`.xls` 必然解析失败，而失败后代码会**静默换成内置演示数据导入** —— 于是库里凭空多出
几十个"软件模板里的人"，**而且真的写进了库**，辅导员还会以为是自己导错了。

- 换成**完整版**（`parse_xlscfb` / `CFB` 都在），`.xls` 直接能读；内联体积 +388,748 字符。
- **解析失败不再顶替**：改为明说"这个文件打不开"、列出常见原因（加密 / 损坏 / 正被 Excel
  占用 / 旧格式）并给"另存为 .xlsx"的下一步，**一个字都不入库**。

**根因二 · 程序写死了「Excel 第 1 行就是表头」。**
这类表格的第 1~3 行是「附件6：」标题、盖章栏、填报日期，真正的表头在第 4 行。
整表错位后列名被认成「附件6：」，学号列识别不出来，每一行都因"学号为空"被跳过。

- 新增**表头自动定位**：优先找"同时含学号与姓名"的那一行，其次找"含学号"的那一行，
  都找不到才退回第 1 行 —— 老样式的表完全不受影响（有回归测试守这条）。
- 顺带剔掉**不像人的行**（表头正下方「培养联系人 1 | 2」这种子表头、表尾的签名与说明行），
  它们学号姓名都是空的，留着会被当成"异常行"报出来，白吓一跳。拿不准时不过滤，宁可多报也不漏报。
- 报错行号改为**指向原表真实行号**：以前靠"下标 + 表头行 + 2"估算，剔过行之后就会错位。

**新增：积极分子名册 → 政治面貌自动同步**

既是名册，那"把这些人改成入党积极分子"本来就是你要做的事，让程序顺手做掉：

- 匹配到学号的学生，**政治面貌自动同步为「入党积极分子」**，不必再几百人一个个手点；
  名册里的所在党支部、递交入党申请书时间、确定为入党积极分子时间等列一并写入档案。
- 两道闸门保安全：**已是中共党员 / 中共预备党员 / 发展对象的一律不降级**；
  名册自带「政治面貌」列且写了值的，以名册为准。
- 识别到名册会在确认页顶部**亮出说明**（会改什么、保护谁），结果页汇报「同步 N 人 / 保住 N 人」。
  选了「覆盖当前批次」还会额外警告 —— 名册通常只是全班的一部分，覆盖会把名单外的同学清掉。
- 政治面貌下拉补齐 **「入党积极分子」「发展对象」**，不再只能靠手输（手输最容易出现
  「积极分子」「入党积极分子(已备案)」这类五花八门的写法）。

**新增：班委词条范围扩大**

各校叫法不一，现在「班干部」「班级委员」「班级职务」「职务」「担任职务」都归一到「班委」，
列表的班委列与关注视图「班委成员」都能吃到。（表头里夹了换行/空格也能认，例如「学号/职工号」）

**新增：综合评定答疑机器人二维码**

「AI 辅导员」页新增一张横跨两列的卡片，内联企业微信二维码（离线也能显示），
并带「复制链接（发群公告用）」「在浏览器打开」两个按钮；「系统设置 · 关于」里也放了一份，
方便随时掏出来给学生扫。分工写清了：**制度流程问鹿晓南，综合评定口径问它。**

**新增：首次引导之后的「页面浮窗批注」导览**

新手向导讲的是"数据怎么取进来"，这个导览讲的是"取进来之后界面上的东西都在哪"：

- 走完（或跳过）新手引导后自动浮出一圈批注，**7 步**逐块点出批次条、导入按钮、侧栏、
  取数指引、筛选与列设置、关注视图、个人中心；挖洞式高亮，目标元素本身照样看得清。
- 只指路不啰嗦：全篇每步一句话，不做功能手册（手册在「取数 · 导入指引」页）。
- 锚点用 `data-tour` 属性而不是位置选择器 —— 以后界面调顺序也不会指错地方；
  找不到锚点（侧栏收起、没数据时没有筛选条）就自动跳过那一步，绝不指着一片空白讲。
- **老用户升级不会被打扰**：老数据里没有这个标记，一律视为"看过"。
  清空数据 / 重看入口都在：个人中心 →「重看页面导览」，取数指引页也放了一个。

---

## [v1.9.1] — 2026-09-23

**修复：程序会开出第二个窗口**

程序缩到托盘后，再双击桌面图标会又弹一个窗口。这不只是"多一个窗口"的麻烦：
**两个窗口各存一份数据，后保存的那份会把先保存的盖掉。**

- 新增官方单实例插件：已在后台运行时，再双击只**把原来那个窗口叫回前台**，不会新开。
- Windows 侧靠「命名互斥量 + 隐藏窗口消息」实现，**不监听任何网络端口**，不会被防火墙 / 安全软件拦。
- 开机自启拉起的那次照旧安静待在托盘，不会把窗口弹出来打扰。

**变更：发布文件带版本号**

文件名规则定为 `中南大学生工作台-v<版本>-<用途>.<扩展名>`，
并新增 `.build/pack-release.py` 在出包后自动改名、清理旧命名文件、同步三处文档里的文件名。
（起因：文件名里不写版本，过两周就分不清手里这份是哪一版。）

---

## [v1.9] — 2026-09-22

**新增**

- **新手引导重做为"跟着点一遍就能完成"的分步实操向导**：6 步，每步都配可执行按钮
  （打开智慧学工 / 打开综合教务 / 直达导入框），并带两份文件的进度清单，随时知道还差哪份。
- **宿舍看板新增「查寝打分表」导出**：A4 横向打印页，表头跨页自动重复、按楼栋房号自然排序、
  住宿人员姓名 + 床位不会被裁；打分项可自由增删改，满分自动合计。生成后直接落到「下载」文件夹并自动打开。

**修复**

- **「引导找不到」**：引导页的侧栏入口此前被误删，只剩首次自动弹一次；
  现在侧栏常驻入口，另在指引页 / 个人中心各加一个"重看引导"按钮。

**改进**

- 演示数据更真实：80 名学生按性别编组为 25 间 2~4 人寝室、床位连续编号、男女不混寝，「室友」列不再全空。

---

## [v1.8] — 2026-09-22

- **最小化到系统托盘、后台常驻**：点窗口右上角「×」不再退出，程序缩到托盘继续跑；
  左键单击托盘图标唤回窗口，右键菜单里有"退出"。想改成点「×」直接退出，可在系统设置里关。
- **开机自动启动（可自选）**：只写当前用户的「启动」目录，**不需要管理员权限**，随时可关。
- **语录自动轮换变成可设置项**：开关与间隔时间都能在系统设置里调。

---

## [v1.7] — 2026-09-22

- **数据总览的问候会看时间、认名字**：早上好 / 中午好 / 下午好 / 晚上好 / 夜深了，并带上你在个人中心填的姓名。
- **顶部金句每 5 秒自动换一句**（鼠标移上去暂停，方便看清）。
- **手机端整体适配**：弹窗全屏、操作区单列、点击区加高；窄屏下不再显示那行校名，只留"第 N 周"。

---

## [v1.6] — 2026-09-22

- **窄窗口顶栏自适应**：1024 / 900 / 760 / 560 四档逐级收敛，不再竖排变形；
  显示模式改成一个按钮循环切换（明 → 暗 → 护眼）。
- **数据管理全部收进「系统设置」**：侧栏不再单独挂一组，六项操作并入设置页的操作区。
- **备份与恢复看得见上次备份时间**：「上次备份：昨天 · 已 1 天未备份」这种说法。
- **系统设置与个人中心各管一摊**：设置管外观 / 学期 / 数据 / 固化，个人中心管资料与安全，不再重复。

---

## [v1.5] — 2026-09-22

- **顶栏显示日期与教学周次** + 新增「系统设置」页（学期名 / 开学日 / 总周数，带实时预览）。
- **我的班级支持拖拽排序**，排序结果会记住。
- **宿舍看板支持跨宿舍拖拽调宿**：把床位卡从一个房间拖到另一个房间，带上确认与动画。
- **导入历史与一键复原**：每次导入留一份快照，随时能把数据退回导入前的样子（复原前会自动先备份当前）。
- **数据本地固化**：除 localStorage 外，同时向磁盘镜像一份
  `%APPDATA%\cn.edu.zuel.studentworkstation\workstation-data.json`，
  清缓存 / 重置 / 重装都不会丢。

---

## [v1.4] — 2026-09-22

- 界面修正：登录页居中、校历图不再撑爆页面（可点开全屏看）、二维码不再被压扁。
- **「工作笔记」升级为首页「备忘清单」**：可勾选、可增删，原来记的内容自动迁过去。
- **数据安全提醒**：改动数据后，若长时间没备份会主动提醒一次。

---

## [v1.3] — 2026-09-22

- **「制度速查」改为「AI 辅导员·鹿晓南」**：扫码即用，并给出"什么问题适合问它"的说明。
- **新增「校历作息」**：内嵌学校官方校历与课堂时间表（含查看原文入口）。
- **新增「个人中心」**：姓名 / 学院 / 电话 / 头像 / 主题 / 改密码 / 备份入口。
- **校务导航换成本校网站集合**：87 个站点按分区归类，可搜索。
- 侧栏三个分组可折叠，"常用工具"移到"我的班级"上方。

---

## [v1.2] — 2026-09-21

内容库三件：

- **校务导航**：常用校内系统集中管理，只放行 http/https，点开走系统浏览器。
- **常用模板**：12 条预置文案，一键复制。
- **制度速查**：制度摘要 + 工作笔记（自动保存）。

---

## [v1.1] — 2026-09-21

- **删除三件套**：单个学生删除、勾选批量删除、整批删除（等同回滚），删错有 30 秒撤销条。
- **备份与恢复**：全量 JSON 导出 / 恢复，侧栏与批次管理两处入口。
- **应用内确认框**替代系统弹窗（桌面外壳下系统弹窗不可用），全项目不再出现 `confirm()` / `prompt()`。
- 视觉升级：字体、卡片阴影、滚动条、侧栏可收缩、侧栏新增"常用工具"分组。

---

## [v1.0] — 2026-09-19 · Windows 桌面版首发

把网页原型封成 Windows 桌面程序（Tauri 2 + WebView2），桌面壳只做网页做不到的三件事：

- **导出文件真正落盘**：WebView 里 `a[download]` 会静默失效，改为写进「下载」文件夹，重名自动加 `(1)`。
- **外链用系统默认浏览器打开**：`<a target="_blank">` 在应用里会把窗口自己导航走。
- **关掉浏览器右键菜单**（输入框里保留，否则没法粘贴）。

**修复（2026-09-20）**：绿色版只有顶栏金句能点、其余按钮全无反应。
根因是 Tauri 会改写 CSP，把 `script-src` 里的 `'unsafe-inline'` 换成静态脚本的 hash，
于是**运行时由 innerHTML 生成的内联 `onclick` 一律被拦**（131 处）。
处理：在 `tauri.conf.json` 里关闭这项改写（本应用纯本地、不加载远程内容，风险极小）。

---

## 原型阶段 v0.1 – v0.7（2026-09 上旬，未对外发布）

| 版本 | 主要内容 |
| --- | --- |
| **v0.1** | 需求确认与方案定稿：单文件 HTML 原型路线、数据模型、导入合并策略 |
| **v0.2** | **按列合并**（不是整行覆盖）、字段别名表把各校表头归一、学号为空的异常行按 Excel 行号点名列出、不静默删除 |
| **v0.3** | **三种导入模式**（合并 / 覆盖 / 新建批次）、**数据批次**（上限 20 个，可切换 / 重命名 / 删除并自动备份）、存储升级 v2 并自动迁移、顶部数字可点击、窄屏适配到 360px、图表按真实像素重绘 |
| **v0.4** | **成绩层**：成绩作为独立数据集按学号挂在批次上；成绩表导入向导（自动定位第 3 行表头、剔合计行）、列表与详情绑定显示、看板成绩化（分档 / 班级均分 / 学业预警）、导出带成绩 |
| **v0.5** | **列表表头排序**、**详情页全字段编辑**（改学号会带动成绩搬家）、**宿舍看板**（床位居 / 空位 / 满员 / 超员，床位基准可调）、三种宿舍表结构统一解析、看板卡片瘦身（删掉没信息量的两张图） |
| **v0.6** | **列表列自定义**（任意字段成列、拖动排序、落盘记忆）、**详情页字段设置**（只隐藏不删数据）、**自定义字段**、枚举字段改**选择题**、**筛选条件多选**（同字段"或"、跨字段"且"）；v0.6.1 把"手输其他"改成内联输入框，全项目不再用 `prompt()` |
| **v0.7** | 产品定名为**中南大学生工作台**、校徽图标（全项目只存一份 base64）、作者与反馈方式、**「取数 · 导入指引」页**（写明两个系统的取数路径）、**首次打开的新手引导**、页面上可见版本号 |

> 这个阶段就定下的两条硬规矩，至今没变：
> **① 数据只存本机，全程不联网；② 所有图表手写 SVG + CSS，不引任何图表库。**

---

## 如何查看当前版本

软件内：**系统设置 → 关于**；或看安装包文件名里的 `vX.Y.Z`。
