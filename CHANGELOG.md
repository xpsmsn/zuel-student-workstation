# 更新日志 · CHANGELOG

> **中南大学生工作台** —— 面向高校辅导员的本机学生工作台。
> 把教务 / 学工系统导出的学生信息表 + 成绩单放进来，自动拼出每个人的画像、成绩、宿舍与学业预警。
> **数据只存本机，全程不联网、不上传服务器。**
>
> 版本写法：`v0.x` 是**网页原型阶段**（未对外发布）；`v1.0` 起是 **Windows 桌面版**。
> 安装包文件名一律带版本号：`中南大学生工作台-v<版本>-<用途>.<扩展名>`。

---

## 🙏 特别鸣谢

本项目的产品思路，受到 **[辅导员 AI 工作台 · counselor-ai-workbench v1.1.0](https://github.com/L-uo/counselor-ai-workbench/releases/tag/v1.1.0)** 的启发，在此特别致谢。

具体借鉴的是这些**做事的思路**（不是代码）：

| 借鉴的思路 | 本项目里的落法 |
| --- | --- |
| **免费使用、本地数据优先、无需注册软件账号** | 全程离线运行、数据只落本机磁盘；不设账号体系，打开即用（只有一个本地访问密码） |
| **把辅导员日常零散事务收进一个工作台** | 学生档案 + 成绩 + 宿舍 + 导入历史 + 备份 + 备忘清单，一屏之内 |
| **校务系统导航**（把校内各平台收在一处） | 「校务导航」页：本校 87 个站点按分区归类，可搜索、可收藏 |
| **常用模板 / 学校制度库**（内容库形态） | 「常用模板」12 条预置文案、一键复制；「AI 辅导员·鹿晓南」扫码即用 |
| **批量导入要能吃下各校格式不一的 Excel** | 字段别名表 + 三种导入模式（合并 / 覆盖 / 新建批次），表头不在第一行也能自动定位 |
| **备份与恢复是刚需，要显眼** | 备份页常驻侧栏，并显示"上次备份：昨天 · 已 1 天未备份"这类人话提醒 |
| **AI 可选、不用也能干活** | 本项目**完全不含 AI 功能**——连 AI 都不要，全部能力纯本地 |

需要说明的是：对方项目**当前未开放源代码**，本项目是**独立实现**，不是它的 fork 或二次分发。
我们的技术路线也不同：整个产品是一个**单文件 HTML**（无框架、无构建），外面套一层很薄的
Tauri 2 桌面壳，只负责网页做不到的事（导出落盘、外链用系统浏览器打开、缩到系统托盘）。

---

## [v1.9.1] — 2026-09-23

**修复：程序会开出第二个窗口**

程序缩到托盘后，再双击桌面图标会又弹一个窗口。这不只是"多一个窗口"的麻烦：
**两个窗口各存一份数据，后保存的那份会把先保存的盖掉。**

- 新增官方单实例插件：已在后台运行时，再双击只**把原来那个窗口叫回前台**，不会新开。
- Windows 侧靠「命名互斥量 + 隐藏窗口消息」实现，**不监听任何网络端口**，不会被防火墙 / 安全软件拦。
- 开机自启拉起的那次照旧安静待在托盘，不会把窗口弹出来打扰。

**变更：发布文件带版本号**

文件名规则定为 `中南大学生工作台-v<版本>-<用途>.<扩展名>`，
并新增 `.build/pack-release.py` 在出包后自动改名、清理旧命名文件、同步三处文档里的文件名。
（起因：文件名里不写版本，过两周就分不清手里这份是哪一版。）

---

## [v1.9] — 2026-09-22

**新增**

- **新手引导重做为"跟着点一遍就能完成"的分步实操向导**：6 步，每步都配可执行按钮
  （打开智慧学工 / 打开综合教务 / 直达导入框），并带两份文件的进度清单，随时知道还差哪份。
- **宿舍看板新增「查寝打分表」导出**：A4 横向打印页，表头跨页自动重复、按楼栋房号自然排序、
  住宿人员姓名 + 床位不会被裁；打分项可自由增删改，满分自动合计。生成后直接落到「下载」文件夹并自动打开。

**修复**

- **「引导找不到」**：引导页的侧栏入口此前被误删，只剩首次自动弹一次；
  现在侧栏常驻入口，另在指引页 / 个人中心各加一个"重看引导"按钮。

**改进**

- 演示数据更真实：80 名学生按性别编组为 25 间 2~4 人寝室、床位连续编号、男女不混寝，「室友」列不再全空。

---

## [v1.8] — 2026-09-22

- **最小化到系统托盘、后台常驻**：点窗口右上角「×」不再退出，程序缩到托盘继续跑；
  左键单击托盘图标唤回窗口，右键菜单里有"退出"。想改成点「×」直接退出，可在系统设置里关。
- **开机自动启动（可自选）**：只写当前用户的「启动」目录，**不需要管理员权限**，随时可关。
- **语录自动轮换变成可设置项**：开关与间隔时间都能在系统设置里调。

---

## [v1.7] — 2026-09-22

- **数据总览的问候会看时间、认名字**：早上好 / 中午好 / 下午好 / 晚上好 / 夜深了，并带上你在个人中心填的姓名。
- **顶部金句每 5 秒自动换一句**（鼠标移上去暂停，方便看清）。
- **手机端整体适配**：弹窗全屏、操作区单列、点击区加高；窄屏下不再显示那行校名，只留"第 N 周"。

---

## [v1.6] — 2026-09-22

- **窄窗口顶栏自适应**：1024 / 900 / 760 / 560 四档逐级收敛，不再竖排变形；
  显示模式改成一个按钮循环切换（明 → 暗 → 护眼）。
- **数据管理全部收进「系统设置」**：侧栏不再单独挂一组，六项操作并入设置页的操作区。
- **备份与恢复看得见上次备份时间**：「上次备份：昨天 · 已 1 天未备份」这种说法。
- **系统设置与个人中心各管一摊**：设置管外观 / 学期 / 数据 / 固化，个人中心管资料与安全，不再重复。

---

## [v1.5] — 2026-09-22

- **顶栏显示日期与教学周次** + 新增「系统设置」页（学期名 / 开学日 / 总周数，带实时预览）。
- **我的班级支持拖拽排序**，排序结果会记住。
- **宿舍看板支持跨宿舍拖拽调宿**：把床位卡从一个房间拖到另一个房间，带上确认与动画。
- **导入历史与一键复原**：每次导入留一份快照，随时能把数据退回导入前的样子（复原前会自动先备份当前）。
- **数据本地固化**：除 localStorage 外，同时向磁盘镜像一份
  `%APPDATA%\cn.edu.zuel.studentworkstation\workstation-data.json`，
  清缓存 / 重置 / 重装都不会丢。

---

## [v1.4] — 2026-09-22

- 界面修正：登录页居中、校历图不再撑爆页面（可点开全屏看）、二维码不再被压扁。
- **「工作笔记」升级为首页「备忘清单」**：可勾选、可增删，原来记的内容自动迁过去。
- **数据安全提醒**：改动数据后，若长时间没备份会主动提醒一次。

---

## [v1.3] — 2026-09-22

- **「制度速查」改为「AI 辅导员·鹿晓南」**：扫码即用，并给出"什么问题适合问它"的说明。
- **新增「校历作息」**：内嵌学校官方校历与课堂时间表（含查看原文入口）。
- **新增「个人中心」**：姓名 / 学院 / 电话 / 头像 / 主题 / 改密码 / 备份入口。
- **校务导航换成本校网站集合**：87 个站点按分区归类，可搜索。
- 侧栏三个分组可折叠，"常用工具"移到"我的班级"上方。

---

## [v1.2] — 2026-09-21

内容库三件：

- **校务导航**：常用校内系统集中管理，只放行 http/https，点开走系统浏览器。
- **常用模板**：12 条预置文案，一键复制。
- **制度速查**：制度摘要 + 工作笔记（自动保存）。

---

## [v1.1] — 2026-09-21

- **删除三件套**：单个学生删除、勾选批量删除、整批删除（等同回滚），删错有 30 秒撤销条。
- **备份与恢复**：全量 JSON 导出 / 恢复，侧栏与批次管理两处入口。
- **应用内确认框**替代系统弹窗（桌面外壳下系统弹窗不可用），全项目不再出现 `confirm()` / `prompt()`。
- 视觉升级：字体、卡片阴影、滚动条、侧栏可收缩、侧栏新增"常用工具"分组。

---

## [v1.0] — 2026-09-19 · Windows 桌面版首发

把网页原型封成 Windows 桌面程序（Tauri 2 + WebView2），桌面壳只做网页做不到的三件事：

- **导出文件真正落盘**：WebView 里 `a[download]` 会静默失效，改为写进「下载」文件夹，重名自动加 `(1)`。
- **外链用系统默认浏览器打开**：`<a target="_blank">` 在应用里会把窗口自己导航走。
- **关掉浏览器右键菜单**（输入框里保留，否则没法粘贴）。

**修复（2026-09-20）**：绿色版只有顶栏金句能点、其余按钮全无反应。
根因是 Tauri 会改写 CSP，把 `script-src` 里的 `'unsafe-inline'` 换成静态脚本的 hash，
于是**运行时由 innerHTML 生成的内联 `onclick` 一律被拦**（131 处）。
处理：在 `tauri.conf.json` 里关闭这项改写（本应用纯本地、不加载远程内容，风险极小）。

---

## 原型阶段 v0.1 – v0.7（2026-09 上旬，未对外发布）

| 版本 | 主要内容 |
| --- | --- |
| **v0.1** | 需求确认与方案定稿：单文件 HTML 原型路线、数据模型、导入合并策略 |
| **v0.2** | **按列合并**（不是整行覆盖）、字段别名表把各校表头归一、学号为空的异常行按 Excel 行号点名列出、不静默删除 |
| **v0.3** | **三种导入模式**（合并 / 覆盖 / 新建批次）、**数据批次**（上限 20 个，可切换 / 重命名 / 删除并自动备份）、存储升级 v2 并自动迁移、顶部数字可点击、窄屏适配到 360px、图表按真实像素重绘 |
| **v0.4** | **成绩层**：成绩作为独立数据集按学号挂在批次上；成绩表导入向导（自动定位第 3 行表头、剔合计行）、列表与详情绑定显示、看板成绩化（分档 / 班级均分 / 学业预警）、导出带成绩 |
| **v0.5** | **列表表头排序**、**详情页全字段编辑**（改学号会带动成绩搬家）、**宿舍看板**（床位居 / 空位 / 满员 / 超员，床位基准可调）、三种宿舍表结构统一解析、看板卡片瘦身（删掉没信息量的两张图） |
| **v0.6** | **列表列自定义**（任意字段成列、拖动排序、落盘记忆）、**详情页字段设置**（只隐藏不删数据）、**自定义字段**、枚举字段改**选择题**、**筛选条件多选**（同字段"或"、跨字段"且"）；v0.6.1 把"手输其他"改成内联输入框，全项目不再用 `prompt()` |
| **v0.7** | 产品定名为**中南大学生工作台**、校徽图标（全项目只存一份 base64）、作者与反馈方式、**「取数 · 导入指引」页**（写明两个系统的取数路径）、**首次打开的新手引导**、页面上可见版本号 |

> 这个阶段就定下的两条硬规矩，至今没变：
> **① 数据只存本机，全程不联网；② 所有图表手写 SVG + CSS，不引任何图表库。**

---

## 如何查看当前版本

软件内：**系统设置 → 关于**；或看安装包文件名里的 `vX.Y.Z`。
