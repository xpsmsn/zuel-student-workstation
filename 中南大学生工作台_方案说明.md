# 中南大学生工作台 · 需求确认与实现方案

> 本地单机 · 离线运行 · 面向高校辅导员学生工作（中南财经政法大学定制）
> 版本：**v1.0**（Windows 桌面版 · 可安装运行） · 日期：2026-09-19
> 对应产物：① `中南大学生工作台.html`（高保真原型，约 4800 行单文件，浏览器双击即可运行）；
> 　　　　　② `app/`（Tauri 桌面工程）→ 出包 `中南大学生工作台_1.0.0_x64-setup.exe` 与 `中南大学生工作台_1.0.0_x64_zh-CN.msi`

> ⚠️ **改名不改数据**：本版把产品名由「辅导员工作站」改为**「中南大学生工作台」**，文件名同步改为
> `中南大学生工作台.html` / `中南大学生工作台_方案说明.md`。但**本地存储的键仍是 `counselor_workstation_v2`**，
> 老盘上的数据（批次 / 备注 / 成绩 / 个性化设置）**原样继续可用**，不需要任何迁移。

**v1.0 修订说明（Windows 桌面版 · 正式出包）**
本版把原型变成**双击即装、桌面上有图标的真软件**，走的是附录 B 里定下的**方案甲（原型直套 Tauri 外壳）**：
① **前端一行没改** —— 原型 HTML 原样进包，桌面端的所有差异收敛在一段注入的「桌面适配层」里
（导出落地到「下载」文件夹 / 外链交给系统浏览器 / 关掉浏览器右键菜单）；
② **数据仍只在本机**，落在 `%LOCALAPPDATA%\cn.edu.zuel.studentworkstation`；「不联网、不上报」这条红线由外壳层再兜一道
（CSP 只允许本地来源，`open_external` 只放行 `http/https/mailto`）；
③ 出两个安装包：`…-setup.exe`（NSIS，**按当前用户安装、不弹 UAC**）与 `….msi`（WiX，中文界面，需管理员），
另附一个可直接拷走的**绿色版单 exe**；
④ **应用图标换成校徽**，窗口标题 / 安装列表 / 任务栏全部一致；关于页版本号同步升到 `v1.0`。
新增章节：`八、桌面版（Windows）`；新增自检：`test-desktop.js`（**28 条**），新增构建脚本 `build-desktop.js` / `make-icons.py`、
实机验证脚本 `verify-desktop.ps1`。
⚠️ 本版**还没有**换到 SQLite 与加密存储（见 6.2 表），数据仍存在 WebView 的 localStorage 里；
v0.7 及更早结论**全部保留不变**。

**v0.7 修订说明（中南大定制 · 上手路径）**
本版按辅导员第 10 轮反馈，做四件"更像自家软件"的事：
① **产品改名**为「中南大学生工作台」（含文件名）；② **校徽**内联为应用图标（登录页 / 顶栏 / 页签，base64 一份复用）+
另生成打包用 `assets/南大学生工作台.ico`；③ **作者与反馈**（吴章凡 · 企业微信 · `wuzhangfan110@163.com`）落进登录页与设置页；
④ **取数 · 导入指引**：把"学生信息从智慧学工怎么导、成绩单从综合教务怎么导"写成**一个常驻页面**，
并配一套**首次打开的多步新手引导**（欢迎 → 取学生信息 → 取成绩单 → 导入本工作台 → 可以开始了）。
新增章节：`5.12 取数与导入指引`、`5.13 首次引导（新手向导）`、`6.10 v0.7 原型新增`。
回归新增 `test-brand-guide.js`（**56 条**）。v0.6.1 及更早结论**全部保留不变**。

**v0.6.1 修订说明（桌面端硬约束补丁）**
辅导员反馈："编辑页面得有一个自定义，因为很多情况不在选项中。" 定位到根因：
「＋ 手输其他…」原来用的是 `prompt()` 系统弹窗，而**打包成桌面应用后跑在 WebView 里，系统弹窗会被直接吞掉**，
用户点了像"没反应"。本版把所有"当场敲一个自定义值"的地方**改成页面内输入控件**：
① 详情页枚举字段选「＋ 手输其他…」→ **字段下方就地展开输入框**（预填原值、边敲边记、回车提交、留空拨回原值、提交后收起）；
② **批次重命名**同步改为**行内编辑**；
③ 静态守住：**源码里不再有任何 `prompt(` 调用**。
新增章节：`5.10.5 「自定义值」一律走内联输入`。回归：`test-custom.js` 新增第五节/第六节（**104/104**）、`verify-real.js` 增两条真实数据断言（**78/78**）。
v0.6 及更早结论**全部保留不变**。

**v0.6 修订说明**
本版把"页面长什么样"从写死改成**交给辅导员自己配**，并解决"导入的字段很多但用不上"的问题：
① **列表列自定义**（勾选显示 + 拖动/上下调顺序，任意导入字段——邮箱/身份证号/宿舍——都能成列，设置记住）；
② **详情页字段自定义 + 自定义字段**（隐藏不关心的字段；还能新建导入表里没有的字段，如「家长电话」「家长姓名」，逐人录入）；
③ **详情页枚举字段改「选择题」**（性别 / 政治面貌用预置项，班级 / 专业 / 院系 / 民族 / 生源地 / 学籍状态等按本批数据自动生成选项，并始终保留原值 + 留「手输其他」兜底）；
④ **筛选条件支持多选**（同一字段可同时选多个值，值之间是「或」，字段之间是「且」）。
新增章节：`5.10 页面个性化`、`5.11 筛选条件多选`、`6.9 v0.6 原型新增`。
**三条不变量本版继续守住**：隐藏字段 / 删除自定义字段 **都不删数据**；本批没有的字段 **不凭空占一列**；选择题 **不吞历史脏值**。
v0.5 及更早的既有结论（成绩层、宿舍看板、批次模型、四条铁律、字段缺失降级、手写 SVG）**全部保留不变**。

**v0.5 修订说明**
本版按辅导员第 7 轮反馈，补四块**面向"每天真正要用"的操作与信息**：
① **学生列表表头排序**（点「成绩」高→低、再点低→高，可对任意列排序）；
② **学生详情页全字段可编辑**（不再只有备注能改）；
③ **新增「宿舍看板」**（哪些人住哪间、男女寝分布、空位与超员，一屏看清）；
④ **总览看板卡片瘦身**（删掉数据不支持/太碎的内容，换成"值得关注"的信息）。
新增章节：`5.7 列表表头排序`、`5.8 详情页全字段编辑`、`5.9 宿舍看板`、`6.8 v0.5 原型新增`。
**本版删除**：总览的「**宿舍楼分布**」条形图（真实数据里一栋楼装百余人、太碎无信息量）与「**毕业学校词云**」（新模板无此列，永远空白）。
v0.4 及更早的既有结论（成绩层、批次模型、导入三模式、四条铁律、字段缺失降级、手写 SVG）**全部保留不变**。

**v0.4 修订说明**
本版把数据底座换成**系统真实导出的两份模板**：学生信息表（41 列）+ 成绩信息表（11 列）。
新增**成绩层**——成绩作为一份独立数据集，按 `学号` 挂在批次上，与学生画像绑定后综合显示（列表、详情、看板、导出全覆盖）。
新增章节：`2.2 系统导出模板（权威口径）`、`4.6 成绩层模型`、`5.6 成绩绑定显示`、`6.6 v0.4 原型新增`。
**明确不做（辅导员决定）**：按学期筛选、成绩升降趋势 —— 系统导出的成绩表是「一名学生一行」的**总评快照**，
**没有学期列**，也拿不到课程级明细，故放弃该功能，不再列为待办。
v0.3 的既有结论（批次模型、导入三模式、四条铁律、字段缺失降级、手写 SVG）**全部保留不变**。

**v0.3 修订说明**
本版按辅导员实际使用反馈，补三块：**① 导入三模式（覆盖 / 新增 / 新建）与数据批次；② 窄屏适配（360px 起可用）；③ 顶部数字可点击直达名单**。
只增补与修正，v0.2 的既有结论（四铁律、字段别名表、字段缺失降级、手写 SVG）**全部保留不变**。
新增章节：`4.4 批次模型`、`4.5 导入三模式`、`5.4 顶部数字可点击`、`5.5 窄屏与响应式规格`。

**v0.2 修订说明**
v0.1 是原型动工**之前**的设计稿。原型完成后做了逐项核对，本版把三者对齐：文档写的 / 原型做的 / 还差的。
核对结论一句话：**原型超出了设计稿的范围（多做了数据总览看板、三套主题、育人金句），同时也漏了几项文档承诺（导入日志、不静默删除、异常行清单）**。
差异明细见文末「附录 A」，桌面版落地路线对比见「附录 B」。

**阅读约定（v0.2 新增标记）**
- ✅ 已实现（原型中可实际点击验证）
- ⬜ 未实现（文档写了，原型没做，需进 v1.0）
- ➕ 超纲实现（原型做了，文档没写，需补回文档）

---

## 一、需求确认结论

经与你逐项确认，关键决策如下：

| 议题 | 结论 | 对设计的影响 |
|---|---|---|
| 数据来源 | 迎新/智慧学工 + 本科生院教务系统，**均只能手动导出 Excel，无 API、辅导员无同步权限** | 不做自动同步，做「手动导入 + 增量合并」 |
| 网络 | **全程本地，杜绝任何联网** | 打包后无网络请求；不使用云服务、不上报 |
| 字段采集 | **全部 32 列原样保留**，不做列裁剪 | 导入层「无损」，界面层按需展示；后续加功能不改导入逻辑 |
| 表格预处理 | **辅导员零操作**，系统导出的原始表直接导入 | 需自动识别表头、兼容列顺序变化 |
| 班级/年级 | 多班级普遍，多年级极少数 | 以**班级**为筛选主轴，年级仅作轻量标签 |
| 备注 | **一人一条**，不做谈话记录流水 | 单文本字段，降低操作负担 |
| 筛选 | 预设视图 + 自定义筛选并存 | 侧栏预设视图 + 可保存的常用筛选 |
| 照片匹配 | 优先级最低，**第 1 版不做** | 但字段结构中预留 `照片` 列 |
| 成绩 | MVP 只看**总览** | 展示加权平均分、专业排名、年级排名、挂科情况 |
| 使用设备 | 非学校电脑，开发无障碍 | 本地存储方案可行 |

---

## 二、数据实证分析

基于你提供的脱敏样例（`2024级新生名单 演示 全虚假信息.xlsx`）实测：

**规模**：单 sheet「新生名单」，260 条记录，32 列。

**字段清单与质量**：

| # | 字段 | 非空 | 唯一值 | 关键发现 |
|---|---|---|---|---|
| 1 | 姓名1 | 260 | 260 | 与第 6 列「姓名」**完全重复** |
| 2 | 生源地 | 260 | 42 | **仅到市级**，无省份 |
| 3 | 考生号 | 260 | 260 | 14 位 |
| 4 | **学号** | 260 | 260 | 12 位，**无空值、无重复** → 理想主键 |
| 5 | 通知书编号 | 260 | 260 | 5 位 |
| 6-9 | 姓名/性别/出生日期/证件号码 | 260 | — | 证件号码 260 全唯一 |
| 10-11 | 民族 / 政治面貌 | 260 / **238** | 11 / 2 | 政治面貌**缺 22 条** |
| 12-14 | 院系 / 专业 / 班级 | 260 | 1 / 4 / 11 | 11 个班级，规模 15~30 人 |
| 15-16 | 毕业学校 / 联系电话 | 260 | 185 / 260 | 院校分布分散 |
| 17 | 录取通知书邮寄地址 | 260 | 260 | 含省市区街道，可用于地域分析 |
| 18 | 备注（保密） | **0** | 0 | **全空 → 辅导员备注的天然载体** |
| 19-21 | 宿舍 / 床位 / 电话 | 260 | 260 / 4 / 260 | 「电话」与「联系电话」重复 |
| 22-23 | 军训备注 / 班委 | **4** / 101 | 4 / 16 | 军训备注样例：膝盖受伤、请假 → **高价值关注信号** |
| 24-27 | 转专业报名/意向、国际班报名、挂科情况 | 0/0/0/**12** | — | 挂科情况仅 12 条有值，均为 "1" → 布尔标志 |
| 28-32 | 照片 / 加权平均成绩 / 专业排名 / 年级排名 | 0 / 260 / 260 / 260 | — | 照片全空（预留）；三项成绩指标完整 |

> **⚠ 字段清单修正（v0.2）**
> 上表编号排到 32，但实际只列出了 **31 个字段名**，漏掉的是 **「家长电话」**。
> 核对依据：原型的演示数据（80 条）每条都含 `家长电话`；真实字段总数为 **32 个（含「家长电话」）**。
> 影响面：**不影响导入逻辑**（导入层本就按表头动态读取，不依赖固定字段清单），但**界面分组与筛选字段列表必须包含「家长电话」**（原型已正确包含）。
> 全文口径统一为：**32 列原样保留，含「家长电话」**。

**对界面设计的直接指导**：
1. 「生源地」42 个值但"市辖区"占 21 条 → 必须用**可搜索下拉**，不能平铺
2. 「政治面貌」有缺失 → 筛选器需支持**「未填写」**选项
3. 「挂科情况」「军训备注」虽少量但有值 → 做**关注标识**比做筛选更有价值
4. 「班委」101 人 → 独立的**班委视图**很实用
5. 「备注（保密）」全空 → 直接复用为辅导员备注字段

### 2.1 第二份来源：新系统导出的表头（v0.2 新增）

除迎新系统外，**新系统导出的学生信息表**表头完全不同，只有 **12 列**：

| # | 新系统列名 | 归一字段名 | 与旧表（32 列）的关系 |
|---|---|---|---|
| 1 | 姓名 | 姓名 | 旧表有「姓名1」与「姓名」两列重复，取其一 |
| 2 | 学号 | 学号 | 一致，仍为主键 |
| 3 | 专业 | 专业 | 一致 |
| 4 | 班级 | 班级 | 一致 |
| 5 | 性别 | 性别 | 一致 |
| 6 | 出生日期 | 出生日期 | 一致 |
| 7 | **住宿地址** | 宿舍 | **列名不同** |
| 8 | **室友** | 室友 | **全新字段（旧表没有）** |
| 9 | 生源地 | 生源地 | 一致 |
| 10 | **身份证件号** | 证件号码 | **列名不同** |
| 11 | **手机号** | 联系电话 | **列名不同**（旧表另有「电话」「家长电话」） |
| 12 | **电子信箱** | 电子信箱 | **全新字段（旧表没有）** |

**结论：两个来源能对上的只有 10 个逻辑字段；旧表另有 22 个字段在新表里根本不存在。**

旧表有、新表没有的 22 个字段：
`姓名1`、`考生号`、`通知书编号`、`民族`、`政治面貌`、`院系`、`毕业学校`、`录取通知书邮寄地址`、`备注（保密）`、`床位`、`电话`、`家长电话`、`军训备注`、`班委`、`转专业报名`、`转专业意向`、`国际班报名`、`挂科情况`、`照片`、`加权平均成绩`、`专业排名`、`年级排名`

**这带来四个必须解决的新问题：**

**① 列名不统一 → 必须有「字段别名表」**
同一件事三个来源三种叫法（宿舍 / 住宿地址、证件号码 / 身份证件号、联系电话 / 电话 / 手机号）。不做归一的话，第二次导入后同一个人会出现"两套列"，筛选和统计立刻乱掉。→ 方案见 **4.3 字段别名表**。

**② 合并策略必须从"整行覆盖"升级为"按列合并"**
新表只有 12 列，若按行覆盖，会把旧表已有的民族、班委、成绩、备注**一次性抹掉**——**这是本项目最容易发生数据丢失的地方**。→ 规则见 **4.2**。

**③ 字段缺失会让部分视图变成空的 → 必须做降级提示，不能显示 0**

| 依赖字段 | 受影响的功能 | 只导入新系统 12 列时的表现 |
|---|---|---|
| 挂科情况、军训备注 | 视图「需重点关注」「有挂科记录」、看板「关注提醒」 | 恒为 0 条 |
| 班委 | 视图「班委成员」 | 恒为 0 条 |
| 政治面貌 | 视图「团员/党员」、看板「政治面貌构成」 | 恒为 0 条 / 空图 |
| 加权平均成绩 | 看板「加权成绩分布」「平均加权成绩」 | 显示「—」 |

> **处理方式**：当某字段**在本批数据中 100% 为空**时，隐藏对应视图/图表，改为一行提示——
> 「本批数据未包含该字段（如挂科情况），请从原始系统导出后补充」。
> **绝不能显示 0**，那会让人误以为"这批学生没人挂科"，是比没有数据更危险的事。

**④ 两个新字段要进界面**
- `室友` → 详情页「入学与住宿」组，并**纳入筛选**（同宿舍关系是宿舍走访、矛盾排查的有效线索）
- `电子信箱` → 详情页「联系方式与地址」组

---

### 2.2 系统导出模板（权威口径 · v0.4 新增）

**导入口径的最终结论：以系统导出的这两份模板为准，个人维护的旧表降为补充来源。**

#### ① 学生信息表 `xsjbxxModel_xsjbxxbgdz_display*.xlsx`（41 列，189 人实测）

表头第 1 行，单行表头，无合并单元格、无公式。主键 `学号` 为 **12 位纯数字文本**，实测 189 条无重复/无空值/无前后空格。

> 姓名、学号、专业、班级、性别、出生日期、住宿地址、室友、生源地、身份证件号、手机号、电子信箱、院系、现在年级、学制、预计毕业年份、考生号、民族、出生地、国家地区、港澳台侨、政治面貌、户口所在地、入学前户口性质、预计毕业日期、入学年月、学生类别、管理老师、培养层次、学籍状态、联系电话、QQ号、微信号、家庭地址、家庭电话、在籍状态、是否在校、宿舍楼、房间号、床位号、校区

注意点（导入实现必须处理）：
- **第 191–200 行是 10 行尾部空行**，取数须按「学号非空」过滤或固定到第 190 行。
- `现在年级` 存的是**入学年份**（2024 / 2026），不是「大二」；学号前 4 位即入学年级。
- 日期列**全是文本**且三种写法：`出生日期` = `YYYY-MM-DD`、`预计毕业日期`/`入学年月` = `YYYYMMDD`。
- `室友` 是**逗号拼接的多值串**（如 `秦雪,邓紫依,郑周凤`），要按人建模必须拆分后再统计。
- 8 列整列为空：`身份证件号 / 手机号 / 电子信箱 / 考生号 / 出生地 / 国家地区 / 港澳台侨 / 培养层次`（前 4 个即「隐私已清除」）。
- 全半角括号混用：`英语(翻译)` 与 `商务英语（第二学士学位）` 并存，**不要做精确字符串匹配式关联**。

#### ② 成绩信息表 `成绩信息.xlsx`（11 列，162 人实测）

| 项目 | 实际情况 |
|---|---|
| 表头位置 | **第 3 行**（第 1–2 行是合并的标题带 `A1:K2="成绩信息"`） |
| 列 | `排名、平均学分绩点、学号、姓名、班级、课程门数、不及格门数、平均分、总分、所得学分、加权平均成绩` |
| 数据行 | 第 4–165 行 = **162 名学生，一人一行** |
| 必须剔除 | **第 166–167 行是合计/统计行**（"162人"、"不及格"），导入时按「学号必须为纯数字」过滤 |
| 空列 | L–Z 整列为空；第 168–200 行为空行 |

**关键事实：这是「总评快照」，不是课程级明细。**
- **全表没有「学期/学年」列**（检索 0 命中）。
- 没有单科成绩、课程名/代码/性质/任课教师。
- 各指标都是**累计值**（课程门数 31–62、所得学分 51.25–129.5、总分 2521–5609）。

**→ 因此「按学期筛选 / 看每学期升降」在当前导出下做不出来**，需要另要一份**课程级明细**（最小字段：`学号 / 学期 / 课程 / 成绩`）。
经辅导员确认，**该功能直接放弃**，不再列为待办。

指标实测范围（用于分档与预警阈值）：排名 1–162；平均学分绩点 1.89–3.96（4 分制）；课程门数 31–62；
不及格门数 0–8（28 人有不及格）；平均分 68.72–93.52；总分 2521–5609；所得学分 51.25–129.5；加权平均成绩 64.74–93.65。

#### ③ 两表绑定可行性（实测）

| 指标 | 数量 |
|---|---|
| 成绩表出现的不同学号 | 162 |
| 能匹配上学生表 | **162（100%）** |
| 成绩表孤儿学号 | **0** |
| 学生表中完全没有成绩记录 | **27 人**（2 名 2024 级 + 25 名 2026 级二学位新生） |

> 那 27 人必须显示成「暂无成绩」，**不可丢弃** —— 这是「不静默删除」铁律在展示侧的推论。

#### ④ ⚠️ 教务系统导出坑（务必写进操作说明）

`jwxt.zuel.edu.cn` 的「导出」是**弹窗 + 二次请求**：若在「系统正在准备导出,请稍候…」那一页用浏览器「另存为」，
得到的会是一个 **HTML 文件被存成 `.xlsx`** 的废文件（用 Excel/openpyxl 打开报错）。
**识别方法**：真 xlsx 文件头是 `50 4B`（`PK`），HTML 是 `3C 21 44 4F`（`<!DO`）。
**正确做法**：点「确定/导出」后**等浏览器真正弹出下载**再保存。

---

## 三、技术栈方案

### 3.1 选型结论

| 层 | 选型 | 理由 |
|---|---|---|
| 应用外壳 | **Tauri 2.x**（Rust + 系统 WebView2） | 安装包约 10MB（Electron 约 120MB+），内存占用低，Windows 原生安装体验，**默认无网络能力**，契合离线要求 |
| 前端框架 | **Vue 3 + TypeScript + Vite** | 组件化清晰、生态成熟、开发效率高 |
| UI 组件库 | **Naive UI** 或 **Element Plus** | 表格、筛选、弹层开箱即用，适合信息密集型界面 |
| 本地数据库 | **SQLite**（单文件） | 零配置、免安装、**单个 .db 文件拷走即完整备份** |
| 数据加密 | SQLCipher（整库加密） | 敏感学生信息落盘加密 |
| 密码存储 | **Argon2id**（或 bcrypt）+ 随机盐 | 只存哈希，不存明文 |
| Excel 解析 | 前端 `SheetJS (xlsx)`；大文件/AI 识别走 Rust `calamine` | 本地解析，不联网 |
| 打包 | `tauri build` → `.msi` / `.exe` | 一键安装，无需管理员权限亦可 |

> **v0.2 执行备注**：上表中「Tauri 2.x + Vue3 + TS + Naive UI」的组合对应**附录 B 的方案乙**。
> 但原型是用**原生 JS 单文件**做的，且本机**当前未安装 Rust 工具链**——因此 **v1.0 走甲还是走乙尚未拍板**（三方案差异见附录 B）。
> 不受路线影响、已可锁定的部分：**SQLite 作本地库**、**全程零网络**、**备注永不覆盖等三条铁律**、**手写 SVG 图表不引图表库**。

### 3.2 为什么不选其他方案

| 方案 | 不选原因 |
|---|---|
| Electron | 体积 120MB+、内存占用高，对「常驻轻量」场景不划算 |
| WPF / WinForms | 界面观感偏传统，讲师接受度与迭代速度都不如 Web 技术栈 |
| 纯 Python + 浏览器 | 打包成 exe 体积大、启动慢，且用户需开浏览器，不够「软件化」 |
| 云端 SaaS | **违反离线红线**，且学生信息上云有合规风险 |

### 3.3 架构分层

```
┌─────────────────────────────────────────┐
│  界面层  Vue3 + Naive UI                 │
│  登录 / 列表+筛选 / 详情 / 导入向导       │
├─────────────────────────────────────────┤
│  应用层  Tauri Command (Rust)            │
│  导入编排 · 字段映射 · 增量合并 · 校验     │
├─────────────────────────────────────────┤
│  数据层  SQLite (SQLCipher 加密)          │
│  students · classes · courses · imports  │
│  settings · audit_log                    │
└─────────────────────────────────────────┘
        ↑ 无任何出网调用 ↑
```

---

## 四、数据模型设计

### 4.1 核心表

**students（学生主表）**
```sql
CREATE TABLE students (
  student_id     TEXT PRIMARY KEY,        -- 学号，业务主键
  name           TEXT NOT NULL,           -- 姓名1
  source_city    TEXT,                    -- 生源地
  exam_no        TEXT,                    -- 考生号
  notice_no      TEXT,                    -- 通知书编号
  -- 以下为原始表全字段（32 列全部落地）
  ...            ,
  remark         TEXT,                    -- 辅导员备注（一人一条）
  raw_json       TEXT,                    -- 原始行完整快照，防止未来字段变更丢数据
  imported_at    TEXT,                    -- 最近一次导入时间
  created_at     TEXT,
  updated_at     TEXT
);
```

> **关键设计**：除结构化字段外，额外存一列 `raw_json` 保存原始行的完整快照。这样即使将来学校改了表头、加了新列，也不会丢失任何数据，且能随时回溯。

**classes（班级）**：`class_id / name / grade / major / dept`

**courses（课程，为后续成绩模块预留）**
```sql
CREATE TABLE courses (
  id, student_id, term, course_name, score, credit, course_type, exam_type
);
```
> 成绩用**长表**存储（一行一条成绩），而非把课程名做成列。原因：教务系统课程名每学期都在变，横表会导致字段无限膨胀。

**imports（导入日志）**：`id / file_name / file_hash / row_total / row_added / row_updated / operator / created_at`
> 用于追溯「哪次导入、变了什么」，也支持按 `file_hash` 识别重复导入并提示。

**settings / audit_log**：密码哈希、盐、偏好设置；关键操作留痕（导出、清空、改密）。

### 4.2 合并策略（最关键的业务逻辑 · v0.2 升级为「按列合并」）

> **升级原因**：新增了第二个数据来源（新系统导出，仅 12 列）。原来的"整行覆盖"会把新表里没有的字段（民族、班委、成绩、备注…）一次性抹掉。

```
对导入的每一行：
  ├─ 学号为空              → 列入「异常行」清单，不静默丢弃
  ├─ 学号已存在（UPDATE）   → 逐列合并：
  │                         ├─ 本行【有值】且该列在本次导入表中 → 覆盖
  │                         ├─ 本行【为空】                    → 保留旧值【不覆盖】
  │                         ├─ 该列【不在本次导入表中】         → 保留旧值【原样不动】
  │                         └─ remark（备注）                   → 永不覆盖
  │                         记录到 imports 更新计数
  └─ 学号不存在（INSERT）   → 新增，raw_json 存原始行
```

**四条铁律（v0.2 由三条扩为四条）**：
1. **备注永不覆盖** —— 这是辅导员持续使用的前提，一旦备注被冲掉软件就废了
2. **空值不覆盖**（新增）—— 新表某列为空，不代表"该生信息为空"，很可能只是这个系统没导出
3. **未出现的列保留**（新增）—— 只覆盖本次表真实出现的列；新表没有的字段（民族/政治面貌/班委/成绩…）原样带着走
4. **不静默删除 + 可回溯**（原铁律②③合并）—— 新表里没出现的学生不自动删除，标记「本次未出现」交辅导员判断；导入日志记录增改计数；异常行单独列出而非丢弃

> 一句话记住：**多来源表共用一个学生档案，任何一次导入都只能"补充和订正"，绝不能"抹掉"。**

### 4.3 字段别名表（多来源表头归一 · v0.2 新增）

解决"同一件事、三个系统三种叫法"的问题。

| 归一字段（内部统一） | 别名（各来源实际叫法） | 来源 |
|---|---|---|
| 姓名 | `姓名`、`姓名1` | 新旧 |
| 学号 | `学号` | 新旧 |
| 专业 | `专业` | 新旧 |
| 班级 | `班级` | 新旧 |
| 性别 | `性别` | 新旧 |
| 出生日期 | `出生日期` | 新旧 |
| 生源地 | `生源地` | 新旧 |
| **宿舍** | `宿舍`、**`住宿地址`** | 旧 / **新** |
| **证件号码** | `证件号码`、**`身份证件号`** | 旧 / **新** |
| **联系电话** | `联系电话`、`电话`、**`手机号`** | 旧 / **新** |
| **室友** | **`室友`** | **新（旧表无）** |
| **电子信箱** | **`电子信箱`** | **新（旧表无）** |
| 其余旧表字段 | `民族`、`政治面貌`、`院系`、`考生号`、`通知书编号`、`毕业学校`、`录取通知书邮寄地址`、`床位`、`家长电话`、`军训备注`、`班委`、`转专业报名`、`转专业意向`、`国际班报名`、`挂科情况`、`照片`、`加权平均成绩`、`专业排名`、`年级排名` | 旧（无别名） |

**归一三原则**：
1. **只做"读入时归一"，不改写原始表**：`raw_json` 永久保存**原始列名 + 原始值**，随时可回溯（对应铁律 4）——将来学校又改表头，历史数据仍然可读。
2. **别名可扩展**：遇到新叫法只在表里加一行，**不改导入代码**。这就是"辅导员零操作、原始表直接导入"的实现方式。
3. **同行多别名冲突的处理**：若同一行同时命中多个别名（如旧表同时有「联系电话」和「电话」），**优先取非空值；都非空则取本表靠前的那个**。

**导入向导第 2 步（确认页）需相应升级**：把现在的一律「✓ 保留」改为三态显示——
| 状态 | 判定 | 界面文案 |
|---|---|---|
| `✓ 已识别` | 命中别名表且原名≠归一名 | `住宿地址 → 宿舍`（让辅导员一眼确认没认错） |
| `✓ 保留` | 命中已知字段 | `保留`（学号额外标「主键」） |
| `＋ 新字段` | 不在已知字段清单内 | `新字段，将原样入库` |
| `⚠ 全空列` | 该列在本批数据中所有行都为空 | `本列全部为空，导入后不带来新信息` |

---

### 4.4 批次模型（v0.3 新增）

**动机**：原型原来只有一份 `students`，导致三个问题——
1. 「换一届学生」「另带一个班」没法并存，只能覆盖；
2. 所谓"清空全部数据"其实是删 key，重启后代码发现库是空的，**又自动灌回 80 条演示数据**（辅导员按"清空"再导入，其实是在演示数据上做合并）；
3. 没有任何"这批数据是哪天导的、从哪来的"的痕迹。

**结构**：存储 key 升为 `counselor_workstation_v2`，每次导入是一份**独立快照**。

```
counselor_workstation_v2 = {
  password, theme, savedFilters,
  batches: [ { id, name, importedAt, sourceFile, mode, demo, studentCount, students:[…] } ],
  activeBatchId
}
```

| 要点 | 说明 |
|---|---|
| **批次关系** | **各自独立快照**——切换批次 = 换一整套学生与备注，彼此互不影响 |
| **切换时清筛选** | 批次之间班级与字段可能完全不同；若保留旧筛选，切过去会是一个空列表，反而让人以为数据丢了。故切换时清空 `filters / classFilter / quickView / _search`，`savedFilters`（筛法本身）保留 |
| **迁移** | 首次启动 v2 时，若存在旧版 `counselor_workstation_v1`，把它的 `students` 包成第一个批次（`mode:'migrate'`）。**v1 原样保留不清除**，可回退 |
| **容量** | 单批次（260 人 × 32 字段）≈ 250KB；localStorage 上限约 5MB → **上限取 20 个批次**，超限时拒绝新建并提示先删历史批次 |
| **清空全部数据** | 改为**软清空**：写回「空批次表」而不是删 key。这样重启时 `load()` 返回 true → **不再自动灌演示数据**，而是显示空状态引导导入（并留「恢复演示数据」出口） |
| **演示数据** | 只在**从未使用过**（v2 不存在）时建立，标记 `demo:true`，在批次条上可辨认、可一键删除 |

> **实现关键（给 v1.0 的提醒）**：原型让 `S.students` 始终指向**当前批次的 `students` 数组（同一引用）**，
> 因此所有读 `S.students` 的十余个函数一行都不用改。代价是**必须只有一个写入口** `setStudents(arr)`——
> 任何 `S.students = [...]` 的写法都会让顶层引用与批次脱钩（界面看着对，一刷新就回滚）。
> 原型已在 `save()` 里加了开发期断言来自查这一点。

---

### 4.5 导入三模式（v0.3 新增）

原型的导入在 v0.2 只有一种行为（按列合并且不删人）。v0.3 把它拆成**用户显式选择**的三种模式：

| 模式 | 精确语义 | 适用场景 |
|---|---|---|
| **新增 / 合并**（默认） | 已有学号**按列合并**（四铁律照旧）；新学号追加；**本次表里没出现的学生保留**并提示「本次未覆盖」 | 同一批学生的信息补全与订正，最常用、最安全 |
| **覆盖当前批次** | **清空当前批次后**按合并逻辑重建；辅导员备注**按学号搬回**；新表里没出现的学生**被移除** | 换了一届/换了一套名单，需要整体替换 |
| **新建批次** | 本次行**原样**组成一份新快照，**不做任何合并**，与现有批次并存并自动切换过去 | 另带一个班、留一份历史版本 |

**「覆盖」为什么不违反「不静默删除」铁律？**
铁律约束的是**自动导入时不得静默丢数据**。覆盖是**用户在界面上显式选择 + 二次确认 + 已自动导出 CSV 备份**的一次性授权动作。
UI 上必须三处到位，缺一不可：
1. 第 1 步模式卡：红色描边 + 写明「清空当前批次的 N 人」；
2. 第 2 步确认页：红色警示块，列出「将清空 N 人 / 本次导入 M 行 / 可带走备注 K 条」；
3. 点「确认覆盖并导入」时再弹一次 `confirm`，写明不可恢复 + 已自动备份。

**覆盖模式必须做到的三件事**（缺任何一件都会变成"数据事故"）：
- **备注按学号搬回**：清空前先把 `{学号 → 备注}` 取出来，导入后回填。备注是辅导员唯一的私有资产，覆盖时抹掉等于软件作废。
- **搬不回去的要点名**：新表里没有的学号，其备注无法搬回 → **列出「姓名（学号）」清单**，并说明原备注完整保留在自动导出的备份 CSV 里。绝不静默丢弃。
- **删前自动备份**：执行覆盖前自动导出 `学生数据_批次名_YYYY-MM-DD.csv`（BOM + 全字段）。同理，**删除批次**前也自动导出备份，再二次确认。

**第 3 步结果页按模式呈现**：
| 模式 | 额外展示 |
|---|---|
| 合并 | 新增 / 更新 / 异常行（含 Excel 行号）/ 本次未覆盖（仍保留） |
| 覆盖 | 清空 N → 导入 M · 备注搬回 K 条 · **未匹配备注点名清单** · 移除 L 人（已备份） · 备份文件名 |
| 新建 | 新批次名与人数 · 原有批次未受影响 |

---

### 4.6 成绩层模型（v0.4 新增）

**一句话：成绩是一份独立数据集，按 `学号` 挂在批次上，绝不把成绩字段复制进学生记录。**

#### 为什么单独一层

学生画像与成绩是**两个系统、两份导出、两种粒度**：

| | 来源 | 主键 | 粒度 | 更新节奏 |
|---|---|---|---|---|
| 学生画像 | 学生信息表（41 列） | 学号 | 一人一行 | 一年一变 |
| 成绩指标 | 成绩信息表（11 列） | 学号 | 一人一行（当前口径） | 每学期一变 |

如果把它们按行合并进同一张表，就会出现：**只导成绩就会抹掉学生 41 列中的大部分**（正是四条铁律里最危险的那条）。
所以做成「两份数据 + 按学号绑定」，各自独立导入、独立替换，互不污染。

#### 数据结构

```
batch = {
  id, name, mode, importedAt, sourceFile, demo, studentCount,
  students: [ { 姓名, 学号, 专业, 班级, … } ],   // 来自学生信息表
  grades:   [ { 学号, 排名, 平均学分绩点, 加权平均成绩, 不及格门数, … } ]  // 来自成绩信息表
}
```

- `S.grades` 与当前批次的 `grades` **是同一个数组引用**（和学生层同一套手法），全项目读函数无需关心批次切换。
- 写入口唯一：`setGrades(arr)`。换引用会「界面看着对、刷新就回滚」，`save()` 内置开发期断言自查。
- 按学号索引走 `gradeMap()`（懒构建 + `invalidateGradeMap()` 显式失效），避免每行现建 Map 的 O(n²)。
- 取某生成绩走 `gradeOf(s)`，无记录返回 `null` → 界面显示「暂无成绩」，**绝不显示 0**。
- 存储 schema 未升版本（仍 `counselor_workstation_v2`）：**老批次缺 `grades` 字段时载入自动补空数组**，向后兼容。

#### 成绩导入规则

- **单独入口**，不进学生导入向导（两者粒度与校验规则完全不同）。
- **表头自动定位**：扫描前 20 行找「学号」列所在行 —— 因为该表第 1–2 行是合并标题带、真表头在第 3 行。
- **只认「学号是纯数字」的行**：合计行（"162人"、"不及格"）与说明行在此被剔除，并**留痕列出**（不静默丢弃）。
- **按学号 upsert**：已有更新、没有新增；本次表里没出现的学号其成绩**原样保留**（不静默删除）。
- **导入前先预览**：识别到的列、匹配人数、孤儿学号（点名）、本批暂无成绩人数，确认后才写库。

---

## 五、界面设计

### 5.1 页面结构

> v0.2 按**原型实际实现**修订（➕ 为原型新增、文档原稿未写的内容）

```
登录页
  ├─ 首次：设置密码（含确认，≥4 位）
  └─ 后续：输入密码解锁
      └─ 主界面
          ├─ 顶栏：产品名 · 育人金句（➕ 点击换句）· 主题切换（➕ 明/暗/护眼）
          │         · 导入数据 · 导出当前结果 · 账号（→设置）
          ├─ ➕ 批次条（v0.3）：按导入时间排列的数据批次 Tab · 批次管理（切换/重命名/删除/导出）
          ├─ 侧栏：总览
          │         ├─ ➕ 数据总览（仪表盘）
          │         ├─ ➕ 宿舍看板（v0.5；本批无住宿字段时置灰）
          │         └─ 全部学生
          │       我的班级（按实际班级数动态生成，非固定 11）
          │       关注视图（v0.2：5 个预设；v0.3 增至 7 个，加「已写备注」「未录入成绩」）
          │       其他：➕ 取数 · 导入指引（v0.7） / 导入数据 / 导入成绩 / 设置
          └─ 主区
              ├─ ➕ 取数 · 导入指引视图（v0.7）：三步流程带 + 四张卡（智慧学工取学生信息 / 教务取成绩单 / 导进本工作台 / 易踩坑）
              │        ⚠️ 该分支**排在「无数据」判断之前** —— 一条数据都没有时也要能打开
              ├─ 列表视图
              │   ├─ 统计条（v0.4 起 5 个数字）：筛选结果数 · 已写备注 · 需重点关注 · 有不及格 · 暂无成绩
              │   ├─ 筛选面板：快速搜索 + 班级/性别/政治面貌/宿舍 + 自定义条件
              │   │        ➕ 多选（v0.6）：同一字段可叠加多个取值，选一个就多一个 chip
              │   ├─ 已启用条件 chips（每个取值一个 chip，可逐个 × 掉）+ 常用筛选快捷入口
              │   ├─ 工具条 ➕「列设置」（v0.6）：勾选显示哪些列 + 拖动/上下调整顺序（可勾到任意导入字段）
              │   ├─ ➕ 表头排序（v0.5）：点列头即按该列排序，再点反向；带 ▲▼ 指示与「恢复原序」按钮
              │   └─ 学生表格：默认 序号/姓名/学号/班级/**成绩**(v0.4)/性别/政治面貌/生源地/电话/关注标识/备注摘要
              │            ➕ 列与顺序均可自定义（v0.6），设置落盘、重开保持
              ├─ ➕ 宿舍看板视图（v0.5）
              │   ├─ 工具条：床位基准（2/4/6/8 人间）· 性别 · 楼栋 · 排序 · 只看有空位
              │   ├─ 统计条：宿舍间数 / 已分配人数 / 空床位 / 满员 / 超员 / 男寝 / 女寝
              │   ├─ 未分配名单（点名列出，不静默丢）
              │   └─ 房间卡网格：每间一卡（楼栋-房号 · 男/女/mixed 标签 · 满员/空位 n/超员），卡片内逐个床位显示姓名，空位显式写「空位」
              └─ ➕ 总览视图（数据总览）
                  ├─ 欢迎条：当前口径 + 育人金句
                  ├─ ➕ 6 张可点击指标卡（在册学生/覆盖班级/需重点关注/有不及格/平均加权成绩/平均学分绩点）
                  ├─ ➕ 筛选器（与列表页共享同一份筛选状态，图表与列表同口径）
                  └─ ➕ 图表区：
                      班级人数分布（柱状，点柱可下钻到班级）
                      政治面貌构成（环形，点图例可筛选；单类别时隐藏）
                      加权成绩分布（柱状，<70 / 70-79 / 80-84 / 85-89 / 90-94 / 95-100 六档）
                      ➕ 学业预警名单（v0.5，按严重度排序，挂科多/绩点低在前）
                      ➕ 宿舍分配概览（v0.5，楼栋/房间/空位一屏摘要，点进宿舍看板）
                      ➕ 关注提醒（有不及格/绩点偏低/军训/未写备注/暂无成绩，可点击跳转）
                      ➕ 数据完整度（v0.5，改自「备注完成度」：只列本批真实存在的字段，缺字段不虚报）
                      ➕ 班级平均加权成绩（整行柱状，按均分降序，v0.4 新增）
                      生源地 Top 10（横向条形）
                  ~~宿舍楼分布（横向条形）~~ ❌ v0.5 删除：一栋楼装百余人，太碎无信息量
                  ~~毕业学校词云（字号代表人数）~~ ❌ v0.5 删除：新模板无此列，永远空白
```

> **实现方式备注**：所有图表为**手写 SVG + CSS**，未引入任何图表库（ECharts/AntV 等）。好处：天然随主题变色、零依赖、体积小、完全离线。此约束建议在桌面版中**继续保持**。

### 5.2 关注视图（预设筛选）

| 视图 | 逻辑 | 业务价值 | 字段可用性 |
|---|---|---|---|
| 全部学生 | 无过滤 | 默认 | 永远可用 |
| 需重点关注 | **有不及格（成绩表）** 或 有军训备注/旧挂科字段 | 一屏锁定需要干预的学生 | 永远可用 |
| 有不及格 ➕(v0.4) | 成绩表 `不及格门数 > 0` | **学业预警主视图** | ⚠ 依赖成绩层 |
| 绩点偏低 ➕(v0.4) | 成绩表 `平均学分绩点 < 2.5` | 绩点预警（阈值可调） | ⚠ 依赖成绩层 |
| 暂无成绩 ➕(v0.4) | 成绩表无该生记录 | 成绩未覆盖清单（含二学位新生） | ⚠ 需先导成绩 |
| 未写备注 | 备注为空 | **督促辅导员补全信息**，实用度最高 | 永远可用 |
| 已写备注 ➕(v0.3) | 备注非空 | 已写过的备注快速回看；列表页「已写备注」数字的落点 | 永远可用 |
| 团员/党员 | 政治面貌匹配 | 党团工作 | ⚠ 依赖该字段 |
| 班委成员 | 班委非空 | 学生干部管理 | ⚠ 新模板无此列，自动置灰 |

> **⚠ 字段可用性（v0.2 新增 / v0.4 扩到成绩层）**：
> · 依赖**学生表字段**的视图，若本批没有该字段（如新模板无「班委」）→ **置灰并提示「本批数据未包含该字段」，不显示 0**。
> · 依赖**成绩层**的视图，若本批**还没导入成绩** → 置灰并提示「尚未导入成绩」；若导了成绩但该指标缺失 → 提示「成绩表未提供：××」。
> · 「暂无成绩」在**未导成绩时不出现**（否则等于全部人，没有意义）。
> 理由见 2.1 第③条：0 会被误读成"这批学生没人挂科/都是 0 分"，比没有数据更危险。

### 5.3 交互设计要点

- **快速搜索**：一次输入同时匹配姓名/学号/考生号/电话，避免多个输入框
- **可搜索下拉**：生源地 42 个值必须可搜索，不用平铺
- **未填写选项**：政治面貌等有缺失的字段，筛选器带「未填写」
- **条件可见**：所有生效条件以 chip 展示，可单独 × 掉，避免「为什么少了人」的困惑
- **备注就地编辑**：详情页直接写、直接存，无需跳转，降低操作成本
- **备注在列表可见**：列表页直接展示备注摘要，一眼看出谁还没写
- **字段缺失要"说话"（v0.2 新增）**：任何依赖缺失字段的视图/图表，在数据不支持时置灰并写明原因，**不显示 0**
- **列名不同也要认得出（v0.2 新增）**：导入确认页把「住宿地址」标成「→ 宿舍」、「身份证件号」标成「→ 证件号码」、「手机号」标成「→ 联系电话」，让辅导员一眼确认没认错
- **多来源可混用（v0.2 新增）**：迎新系统表、新系统表可以先后导入同一个档案；先导 32 列的，再导 12 列的补新字段，两边数据都不丢

---

### 5.4 顶部数字可点击（v0.3 新增）

原则：**能点就一定要有明确的落点；没有自然落点的数字，宁可不可点，也不做成"点了没反应"。**

**列表页统计条（v0.4 起 5 个数字）**

| 数字 | 点击行为 | 设计理由 |
|---|---|---|
| 当前筛选结果 N | 仅在**存在筛选**时可点 → 「清空筛选，回到全部」 | 它就是当前视图本身，点自己没意义；做成"一键回到全部"才有用 |
| 已写辅导员备注 N | → 名单（预设 `hasremark`） | 计数为 0 时不可点（点进去只会得到空名单） |
| 需重点关注 N | → 名单（预设 `focus`） | 计数为 0 或字段缺失时不可点 |
| 有不及格 N ➕(v0.4) | → 名单（预设 `fail`） | 依赖成绩层；未导成绩时置灰 |
| 暂无成绩 N ➕(v0.4) | → 名单（预设 `nograde`） | 依赖成绩层；未导成绩时不出现（否则等于全部人） |

**总览页 6 张指标卡（v0.4 口径）**

| 卡片 | 处理 |
|---|---|
| 在册学生 / 需重点关注 / 有不及格 | 可点 → 对应名单 |
| 平均加权成绩 / 平均学分绩点 ➕(v0.4) | **可点** → 暂无成绩名单；缺成绩时置灰 |
| 覆盖班级 | **保持不可点**。班级是"维度"不是"名单"，没有自然落点；侧栏已有可点的班级列表，卡片提示改为「个班级 · 侧栏可直接选」 |

**两处一致性问题一并修掉**：
1. 快速搜索打字时**只刷新第 1 个数字**，另 3 个与表格对不上 → 抽出 `statsHtml()`，一次刷新 4 个数字（输入框仍不失焦）。
2. 指标卡的 `isEmpty` 会把提示语统一替换成「暂无」，导致**「字段缺失降级」的提示语永远显示不出来** → 改为直接渲染传入的提示语，缺字段时显示「本批数据未包含「挂科情况」字段…」。

**顺带修掉的显示 bug（v0.3，测试抓出来的）**：
迎新系统（32 列）用 `姓名1`，新系统（12 列）只有 `姓名`。从新系统导入的**新学生**在库里没有 `姓名1`，
而列表/详情/搜索都只读 `姓名1` → 这些学生**姓名显示为空白**。
修法：显示层统一走 `studentName(s) = s['姓名1'] || s['姓名']`（搜索也同时匹配两列）。

---

### 5.5 窄屏与响应式规格（v0.3 新增）

**目标：360px 起可用**（手机也能用）。全文断点统一为 4 档，不再散落：

| 断点 | 场景 | 主要变化 |
|---|---|---|
| `>1180px` | 桌面 | 看板 12 列网格、指标卡 6 列（每行 6 张） |
| `≤1180px` | 平板横 | 看板网格降为 6 列、`span-7/span-5` 变整行；**指标卡改 3 列**（原实现在此处会让 6 张卡竖排成 6 行，是最明显的空间浪费） |
| `≤1024px` | 平板竖 | **侧栏变抽屉**（左上角汉堡按钮，选完自动收起 + 遮罩关闭）；侧栏项触控高度提到 ≥44px |
| `≤760px` | 手机横 / 平板竖 | 看板单列；顶栏精简；筛选控件撑满一行；**表格重排为卡片**；弹窗内边距收紧 |
| `≤560px` | 手机竖 | 内边距与字号再收紧；环形图改纵向；进度条标签变窄；批次条只留图标 |

**图表缩放（关键修正）**：原来 `barChart` 写死 `viewBox="0 0 560 210"` + 等比缩放，结果**两头都不合适**——
窄屏轴字 10.5px 被等比缩到约 5px（不可读），宽屏卡片被拉高到 500px+ 留一片空白。
> 采用方案：**按容器真实像素宽度重绘**。JS 量出卡片内容区宽度作为 `viewBox` 宽度、高度恒定 210px，
> 于是 **1 单位 = 1px**：轴字恒为 10.5px、高度恒定不被拉高、点击坐标换算天然正确。
> 绘制时把宽度与内边距写进 `svg.data-*`，点击定位改从 `data-*` 读取（原来写死 560，容器一变就点错位）。
> ❌ 已否决「CSS 尺寸 + 非等比 viewBox（`preserveAspectRatio="none"`）」：会把文字和圆角矩形压扁变形。

**表格 → 卡片式重排（同一套 DOM）**：
`renderRows()` 给每个 `<td>` 加 `data-label`，窄屏下靠 `.tbl-wrap.as-cards` 把 `tr/td` 改成块级、
用 `::before { content: attr(data-label) }` 显示字段名。**不加逻辑分叉、不维护两套模板**，宽屏仍是熟悉的表格。

**顶栏在手机上的取舍**：
| 元素 | 处理 |
|---|---|
| 汉堡菜单 | ≤1024px 出现 |
| 育人金句 | ≤1100px 隐藏（沿用 v0.2） |
| 导出当前结果 | ≤760px 隐藏，挪进「批次管理」弹窗（增加「导出当前批次全部」） |
| 主题切换 | 保留（收窄） |
| 导入数据 | 保留为主要动作，≤560px 只留 `+` |
| 账号 | ≤760px 只留头像 |

**批次 Tab 条**：顶栏下方常驻，任何时候都看得见"当前在哪个批次"；窄屏横向滚动；最右「批次管理」入口。
高度用 CSS 变量 `--topbar-h` / `--batch-h` 统一，`.shell` 高度由两者算出（改高度只需改一处）。

---

### 5.6 成绩绑定显示（v0.4 新增）

绑定后「学生画像 × 成绩」在各处的呈现：

| 位置 | 呈现 |
|---|---|
| 学生列表 | 新增**「成绩」列**（仅当本批已导入成绩时出现）：主值为**加权平均成绩**，副行为 `绩点 3.94 · 排名 1`；无成绩显示灰字斜体「暂无成绩」 |
| 列表「关注」列 | 不及格 → 红标 `不及格 3 门`（取自成绩表「不及格门数」） |
| 顶部数字 | 4→5 个：当前筛选结果 / 已写备注 / **需重点关注** / **有不及格** / **暂无成绩**，全部可点直达名单 |
| 关注视图（侧栏） | 新增 **「有不及格」**（不及格门数 > 0）与 **「绩点偏低」**（平均学分绩点 < 2.5）；「暂无成绩」替代原「未录入成绩」 |
| 学生详情 | 「成绩」区块直出 8 项：加权平均成绩 / 平均学分绩点 / 排名 / 平均分 / 课程门数 / 不及格门数 / 总分 / 所得学分；无成绩则显示说明条 |
| 总览看板 | 指标卡：在册学生 / 覆盖班级 / 需重点关注 / **有不及格** / **平均加权成绩** / **平均学分绩点**；<br>图表：**加权成绩分布**（`<70 / 70-79 / 80-84 / 85-89 / 90-94 / 95-100` 六档）、**班级平均加权成绩**（整行图表，按均分降序，≥85 绿 / ≥70 蓝 / 其余橙）；<br>关注提醒新增「有不及格科目」「绩点偏低」「暂无成绩记录」三项 |
| 导出 | 「导出当前结果」与批次备份会**把成绩并进同一行**，列名前缀 `成绩·`（如 `成绩·加权平均成绩`），避免与档案列重名；无成绩时不产生多余空列 |

**沿用「字段缺失降级」原则**：整批没导入成绩时，成绩相关的数字/图表/视图一律**置灰并说明原因**，绝不显示 0；
「暂无成绩」这类视图在**没导成绩时不出现**（否则等于全部人，没有意义）。

**沿用「不默认删除」原则**：学生表里有、成绩表里没有的 27 人，显示「暂无成绩」而不是从名单里消失。

---

### 5.7 列表表头排序（v0.5 新增）

**交互**：点表头单元格 → 按该列排序；同一列再点一次 → 反向。列头显示 `▲/▼` 指示，工具条出现「恢复原序」。

| 列 | 排序口径 |
|---|---|
| 姓名 / 学号 / 班级 / 性别 / 政治面貌 / 生源地 / 电话 | 文本列：按中文拼音序（`localeCompare('zh')`），数字部分按数值比较（学号 `2024211100 06` 不会排成 `…1, 10, 11, 2`） |
| **成绩** | 数值列：取该生**加权平均成绩**（来自成绩层）。**默认降序**（高→低），一点就是第一名；再点变升序 |
| 序号 `#` / 关注标识 | **不可排序**：这两个不是数据列（序号随排序变、关注是多条件合成标签），点了没有稳定语义 |

**三条硬规则**：

1. **无值一律沉底**，升序降序都沉底 —— 「暂无成绩」的 27 人不会在升序时冒到最前面冒充"最低分"。
2. **排序是"看"的状态，不落盘**：它只影响当前视图，**不改动数据、不写 localStorage**；刷新/重开自动回到**导入顺序**（辅导员第二次打开时看到的仍是原始名册顺序，不会被上次的排序搞晕）。
3. **筛选 → 搜索 → 排序 是同一条管道**（`viewList()` 是列表唯一出口）——保证列表、统计条数字、导出三处的顺序与人数永远一致，不会出现"表里排好了、导出还是乱序"。

> 实现上排序键走 `sortValue(s, key)`：数值列返回数字或 `null`（`null` = 无值 → 沉底），文本列返回字符串；
> 比较器 `sortList()` 统一处理"无值沉底"。改列的排序行为**只需在 `SORT_COLS` 里加一行**，不动渲染逻辑。

### 5.8 详情页全字段编辑（v0.5 新增）

v0.4 及以前，详情页只有「辅导员备注」能改，其余 41 个字段是只读的 —— 遇到姓名/电话/宿舍/政治面貌录错，辅导员只能回 Excel 改再重导。

**v0.5 起，学生详情页任意字段都可编辑**：

| 设计 | 说明 |
|---|---|
| 进入方式 | 详情页右上「编辑」按钮 → 整页切换为输入态（不是逐字段弹窗，改完一次性保存） |
| 可编辑范围 | ① 「学生信息」分组内的全部字段；② 成绩区块的 8 项（加权成绩/绩点/排名/…）；③ 辅导员备注；④ **「其他字段」组** —— 凡是本批数据里出现过、但没被预设分组收纳的列，自动列到最后一组，保证**新表带来的新字段也不会漏在外面改不了** |
| 保存行为 | 一次写入全部改动；**学号被改时，同步把该生的成绩记录搬到新学号上**（否则会产生"孤儿成绩"——记录还在、却再也挂不到任何人） |
| 清空 = 删除 | 成绩 8 项**全部清空**，等于"该生没有成绩记录"，按 upsert 语义**删掉这条成绩**，界面回到「暂无成绩」（而不是留一条 0 分记录） |
| 成绩补录 | 原本没有成绩的学生，在详情页填入成绩即可**手工新增**一条成绩记录（方便零星补录，不必回系统重导整表） |
| 铁律不破 | 编辑是**用户显式操作**，不是导入合并；写入仍走唯一入口 `setStudents()` / `setGrades()`，引用不变式与 `save()` 自检照常生效 |
| 取消 | 点「取消」或关闭弹窗 → 丢弃草稿（`detailDraft`），原数据不动 |

> 取舍说明：本版**不做**"编辑历史 / 撤销"。逐字段的操作留痕属导入日志范畴，列入 v1.0（见 6.2 #1）。
> 但编辑会写进**导入日志的"增改计数"口径之外**——即：手工编辑不改变批次的导入来源标记，只是就地修正。

### 5.9 宿舍看板（v0.5 新增）

辅导员最常被问到"XX 住哪间、那间还空不空"。这一屏专门回答它。

**一屏看到什么**

```
工具条：床位基准[4人间▾] 性别[全部▾] 楼栋[全部▾] 排序[按房间▾] [只看有空位]
统计条：宿舍 54 间 · 已分配 189 人 · 空床位 27 · 满员 35 · 超员 0 · 男寝 11 / 女寝 42
未分配：3 人（点名）                                   ← 有住宿字段却没填的，单独列出
─────────────────────────────────────────────
[滨湖1栋-634  女  满员]        [滨湖1栋-635  女  空位 2]
 01 李蓉   02 秦雪              01 王某某   02 赵某某
 03 邓紫依 04 郑周凤            03 空位     04 空位
```

| 能力 | 说明 |
|---|---|
| 每间有哪些人 | 房间卡内**逐个床位**显示姓名；床位号排序（01 在前），**空位显式写「空位」**而不是留白 |
| 男女寝排序 | 自动按房间内学生性别判为 `男 / 女 / mixed`；`mixed`（男女同住）**照实标出、不猜不掩盖**，多半意味着数据有误，值得辅导员核 |
| 是否有空位 | 每张卡角标标 `满员 / 空位 n / 超员`；「只看有空位」一键过滤（排床位用） |
| 容量怎么定 | 数据里**没有"房间容量"这一列**，故用「床位基准」设定（2/4/6/8 人间，默认 4，可调，落盘记忆）；单间容量取 `max(床位基准, 该间实际最大床位号)`，保证不放走超员 |
| 超员 | 一间住了超过容量的人 → 标 `超员`，但**空位按 0 计**（绝不出现负空位） |
| 楼栋/房间识别 | 三种表结构都吃下，见下方「⚠️ 解析陷阱」 |

**⚠️ 解析陷阱（本版真实踩过的坑，务必保留）**

宿舍字段在不同模板里长三种样子，**认错就全盘皆错**：

| 来源 | 写法 | 解析规则 |
|---|---|---|
| 41 列模板 | `宿舍楼=滨湖1栋` + `房间号=634` + `床位号=03` | 三列分开，最干净 → key = `滨湖1栋-634` |
| 旧表 | `宿舍=100栋-101`（+ `床位=01床`） | 已是「楼-房」，原样用 |
| **12 列模板** | `住宿地址=滨湖1栋634-03`（**楼+房+床挤在一格，且无任何床位列**） | 末段两位数字 = 床位 → 剥离后归一成 `滨湖1栋-634` |

> **若不处理第三种**：`滨湖1栋634-03` 会被当成一个房间号 → **189 人 = 188 间房**，空位统计直接全废（算出 500+ 个假空位）。
> 这是本版真实的线上数据缺陷，靠"拿系统导出的两套模板分别跑端到端"才抓出来（见 6.5 的 `verify-real.js`）。
> 归一后，**两种模板对同一间房算出的 key 完全一致**，混用也不怕拆成两套房间。

**字段缺失降级**：本批数据完全没有住宿字段（`宿舍 / 住宿地址 / 宿舍楼` 全空）时，侧栏「宿舍看板」入口**置灰**，
点进去显示说明条而不是空网格 —— 沿用 2.1 第③条，**不显示 0**。

---

### 5.10 页面个性化（v0.6 新增）

**动机**：系统导出的学生表字段很多（41 列模板尤其如此），但辅导员日常只看其中十来个；
而「邮箱 / 身份证号 / 家长联系方式」这类字段平时用不上、要用时又找不到。
本版把"显示什么、按什么顺序、能不能自己加"三件事都交给辅导员。

#### 5.10.1 列表列自定义

| 能力 | 说明 |
|---|---|
| **任意字段成列** | 只要字段在本批数据里存在（含**自定义字段**），就能勾成列 —— 邮箱、身份证号、宿舍、室友、专业…不再限于预设那 11 列 |
| 勾选显示 | 「列设置」弹窗里逐个勾选；第一列「#」恒在，不参与自定义（它是行号） |
| **调整顺序** | 按住 `⠿` **拖动**，或用 `↑`/`↓` 按钮（窄屏/触摸下按钮更稳） |
| 设置记住 | 列与顺序**落盘**，下次打开还是你排的样子（与"排序"不同：排序是看一眼就过，不落盘） |
| 本批没有的字段 | **不凭空占一列**，并在设置里标注原因：`本批无此字段` / `本批全空` / `未导成绩`。乱填的列名安静跳过，不会把表格搞崩 |
| 恢复默认 | 一键回到出厂列 |

> 实现上把原来的 `SORT_COLS`（写死的 11 列）拆成 **`FIXED_COLS`（有专门长相的列：姓名加粗、学号等宽、成绩带副行…）+ 通用渲染**。
> 于是"加一列"不再需要改渲染代码 —— 这是本版能支持"任意字段成列"的前提。

#### 5.10.2 详情页字段自定义

详情页右上角新增「**字段设置**」：按分组列出本批所有字段（含「其他字段」与「自定义字段」），取消勾选即不显示。

**关键约定：取消勾选只是不显示，数据不会被删。** 勾回来内容还在。
「辅导员备注」与成绩不在此列 —— 它们始终显示（一个是本软件的核心产出，一个是预警依据）。

> 实现要点：编辑草稿仍然覆盖**全部字段**（包含被隐藏的），因此保存时被隐藏字段的值原样写回、**不会被误清空**。
> 这条有专门的回归用例（`test-custom.js` 与 `verify-real.js` 各一条带 ★ 的断言）。

#### 5.10.3 自定义字段（导入表里没有的字段）

「字段设置」底部可以**新建自定义字段**，例如「家长电话」「家长姓名」「高考位次」。

| 设计 | 说明 |
|---|---|
| 一视同仁 | 建好后与导入字段**完全等价**：能录入、能当列表的列、能筛选、能导出 |
| 存在哪 | 值直接写进该生的记录（键名 = 字段名）。因为导入合并遵循「**未出现的列保留**」，重新导入名册不会冲掉它 |
| 重名保护 | 与已有字段（含导入列）重名时拒绝新建，避免制造两个同义字段 |
| 删除字段 | **只删定义、不删数据**：已录入的内容留在记录里，重新建同名字段即可找回（沿用"不静默删除"铁律） |
| 与导入字段同名时 | 若该名字在本批数据里已存在（哪怕整列全空），会被判为重名而拒绝新建 —— 直接用现成的那个即可 |

#### 5.10.4 详情页枚举字段改「选择题」

编辑态下，枚举类字段不再是自由输入框，而是下拉选择：

| 类别 | 字段 | 选项来源 |
|---|---|---|
| **固定预置** | 性别（男/女）、政治面貌（群众/共青团员/中共预备党员/中共党员/民主党派/无党派人士）、学生类别、学籍状态、在籍状态、是否在校、港澳台侨、入学前户口性质、培养层次 | 内置常用值 + 本批已有值 |
| **按数据自动生成** | 班级、专业、院系、民族、生源地、户口所在地、出生地、校区、宿舍、宿舍楼/房间号/床位号、现在年级、学制、入学年月、毕业年份/日期、管理老师 | 本批数据里出现过的值（不写死） |
| 仍是输入框 | 姓名、学号、出生日期、证件号码、电子信箱、电话、地址、备注、成绩各项… | 自由文本 / 数值 |

**三条防呆**：

1. **当前值永远保留**：若该生的现值不在候选里（历史脏值、或别处导进来的新值），会**置顶**出现在下拉里 —— 不能被悄悄改成别的值。
2. **留「＋ 手输其他…」兜底：选项没覆盖到的值也能录。** ⚠️ 这个出口用的是**字段下方就地展开的输入框**，**不是系统弹窗**——
   打包成桌面应用后跑在 WebView 里，`prompt()` 一类的系统弹窗会被直接吞掉，用户点了像"没反应"。见 5.10.5。
3. **一律可清空**：下拉首项是「（未填写）」，对应把该字段置空（不是填 0、也不是填字符串"未填写"）。

> 效果：`英语2401` 与 `英语 2401`（多个空格）这类"看着一样、实际两个班"的问题，从此改不出来 —— 选项都来自本批数据本身。
> 实测本批 10 个班级：`俄语2401 / 法语2401 / 日语2401 / 商法（双）2401 / 商法（双）2402 / 商英（二）2601 …`，下拉里就是这 10 个。

#### 5.10.5 「自定义值」一律走内联输入（v0.6 补丁 · 桌面端硬约束）

> **背景**：原型在浏览器里跑得好好的，一放进桌面外壳（Tauri / Electron 的 WebView）就出问题 ——
> 用户点了「＋ 手输其他…」**像是没反应**。原因是那个出口原来调的是 `prompt()`，
> 而 **WebView 内核根本不实现系统级输入弹窗**（`confirm()` 尚可用，`prompt()` 直接返回空）。

因此凡"需要用户当场敲一个不在候选项里的值"的地方，**一律改成页面内的输入控件**：

| 场景 | 做法 |
|---|---|
| 详情页枚举字段 | 选「＋ 手输其他…」→ **字段下方就地长出一个输入框**（虚线边框，与下拉同宽）；回车或点开别处即提交，提交后自动收起 |
| 展开时 | 预填该生当前值（便于在原值上改），并把原值记在 `dataset.prev` 上 |
| 边敲边记 | 每敲一个字符就写进编辑草稿；**只写草稿、不重渲染** —— 否则输入框会掉焦点 |
| 提交 | 新值**补进下拉并置顶**（以后可直选）、写进草稿；**留空 = 取消**，拨回进入前的原值，**不会把字段清成空** |
| 防重复 | 回车提交后输入框已收起，随后浏览器会补一个 `blur`；提交函数开头若发现"已收起"就直接返回 —— 否则会出现**前脚提交、后脚被抹掉** |
| 批次重命名 | 同样改为**行内编辑**（该行变成输入框 + 确定/取消），不再弹 `prompt()` |

> 回归口径：`test-custom.js` 第五节整节守这条（含"源码里不再有任何 `prompt(` 调用"的静态断言），
> `verify-real.js` 亦在真实学生上复核同样两条。

### 5.11 筛选条件多选（v0.6 新增）

原来一个字段只能选一个值（想看两个班得来回切）。现在**同一个字段可以叠加多个取值**：

```
[班级：俄语2401 ×] [班级：法语2401 ×] [性别：女 ×]     ← 每个取值一个 chip，可逐个 × 掉
```

| 规则 | 说明 |
|---|---|
| 选值即累加 | 下拉里选一个 → 变成一枚 chip；**不是覆盖**，可以连续选多个 |
| **值之间是「或」** | 班级 = 俄语2401 或 法语2401（两个班一起看） |
| **字段之间是「且」** | （那两个班）且 性别 = 女 |
| 逐个可撤 | 每枚 chip 上的 `×` 只撤这一个值，不必整组清空 |
| 已选有标记 | 下拉里的已选值带 ` ✓`，一眼看出选过什么 |
| 「未填写」也能多选 | 与具体值混选同样成立 |
| 常用筛选 | 存下来的筛选条件会**原样保留多选状态** |
| 总览页同步 | 总览页的筛选器与列表页共用同一份状态，多选口径完全一致 |

> 实现上把 `S.filters` 的值从"字符串"升级为"数组"，并保留一个 `filterValues()` 读取口，
> **旧的字符串写法读进来照样能用**（老用户存过的常用筛选不会失效）。
> 全项目只有 `applyAll()` 一处做筛选判断，所以多选只需改一处，列表 / 统计数字 / 图表 / 导出自动一致。

### 5.12 取数 · 导入指引（v0.7 新增）

**要解决的问题**：软件本身好用了，但"第一份 Excel 从哪来"是个空白 ——
辅导员要自己记住：学生信息在智慧学工哪个菜单下、成绩单在教务系统怎么筛、导出时哪个按钮是"自定义导出"。
这些**不属于软件功能，却决定软件能不能用起来**。所以把它做成软件里的一个页面。

**入口**（三处都能进，同一个页面）：

| 入口 | 位置 |
|---|---|
| 侧栏 | 「其他 → 取数 · 导入指引」（**一条数据都没有时也能进**，那正是最需要它的时候） |
| 空状态页 | 「还没有学生数据」页上的「取数 · 导入指引」按钮 |
| 设置弹窗 | 「取数指引」区的两个按钮（看指引 / 看新手引导） |

**页面内容**：顶部一条三步流程带（导学生信息 → 导成绩单 → 导入本工作台 → 开始用），下面四张卡片：

| # | 卡片 | 内容要点 |
|---|---|---|
| 1 | 取「学生基本信息」→ **智慧学工** | 路径面包屑 `学生数据 › 学生基本信息`；五步操作：登录 → 进菜单 → 点「导出」选「**自定义导出**」→ **字段全选** → 导出保存；附可点击的系统入口地址 |
| 2 | 取「成绩单」→ **综合教务系统** | 路径面包屑 `成绩管理 › 查询分析统计 › 专业成绩单`；五步操作：登录 → 进菜单 → 确认「**上课年级**」、「**上课专业**」按需多选 → 点「**查询**」→ 导出保存；附入口地址 |
| 3 | 导进本工作台 | ①学生表：右上「+ 导入数据」，确认页看列识别结果，选导入方式（**新增 / 覆盖当前批次 / 新建批次**，三种并排说明）②成绩表：右上「+ 导入成绩」，按学号自动挂靠 |
| 4 | 几个容易踩的坑 | 教务导出是**二次请求**（等真正开始下载再保存，否则存成网页文件打不开）；不同系统**列名自动归一**；**辅导员备注永远不会被覆盖**；列设置 / 字段设置的出口 |

> **两个系统地址**（统一身份认证入口）：
> · 智慧学工 `https://ids.zuel.edu.cn/authserver/login?service=…istu.zuel.edu.cn…`（学生数据 → 学生基本信息 → 导出 → 自定义导出 → 全选 → 导出）
> · 综合教务 `https://ids.zuel.edu.cn/authserver/login?service=…jwxt.zuel.edu.cn…`（成绩管理 → 查询分析统计 → 专业成绩单 → 确认上课年级/上课专业 → 查询 → 导出）

**实现约定**：指引正文由 `guideBody(mode)` **一份数据渲染**，同时服务"指引页"与"新手向导"两处 ——
避免同一套说明写两遍、改一处漏一处。系统地址与菜单路径集中在 `ZUEL_SYS` 常量里，改路径只改这一个地方。
页面里所有外链都是 `<a target="_blank">`，**只有用户主动点击才会打开**，软件本身依旧零网络请求。

### 5.13 首次引导（新手向导 · v0.7 新增）

**触发**：首次进入应用（`S.guideSeen` 为假）时自动弹出，**延迟 420ms** —— 让主界面先画出来，
不至于"一进来就撞上一堵弹窗"。

| 步 | 标题 | 内容 |
|---|---|---|
| 1 | 欢迎 | 校徽 + 软件名 + 一句话说明；三条保证：**数据只在你这台电脑上** / 接下来三步约 3 分钟 / 用着不顺手随时找我 |
| 2 | 第一步 · 取学生信息 | 智慧学工路径与五步操作 + 「记得自定义导出时全选」的提醒 |
| 3 | 第二步 · 取成绩单 | 教务路径与五步操作 + 「导出后先确认能打开」的提醒 |
| 4 | 第三步 · 导入本工作台 | ①导学生表 ②导成绩表 两个动作 + 「两份表都可反复导、备注永不覆盖」 |
| 5 | 可以开始了 | 提示左侧可按班级/关注点筛人、列设置与字段设置的作用；**忘了路径就回侧栏看指引** |

**三条行为规则**（都有回归用例守着）：

| 规则 | 说明 |
|---|---|
| 走完 / 点「跳过，直接开始」→ **记已看过** | 落盘 `guideSeen=true`，下次不再弹；「跳过」保证用户随时能明确退出 |
| **中途关掉（点 × / 点遮罩）→ 不记** | 下次进入还会弹，避免"随手点掉后再也找不到" |
| **老数据视为已看过** | 老盘上没有 `guideSeen` 字段 → 当作 true，**不让老用户被突然拦一道**；想看可随时从设置或指引页手动打开 |

> 「清空全部数据」会把 `guideSeen` 重置为假 —— 清空即"从头开始"，也让辅导员能方便地重看一遍引导。
> 结构守卫：`S.guideSeen = true` 在整个源码里**只允许出现在 `finishOnboarding()` 一处**（有静态断言）。

### 5.14 品牌与作者信息（v0.7 新增）

| 项 | 做法 | 说明 |
|---|---|---|
| 名称 | **中南大学生工作台** | 页面标题、登录页、顶栏、设置、指引页统一；旧名「辅导员工作站」在源码里已完全清除 |
| **存储键不动** | 仍是 `counselor_workstation_v2` | **改名不能碰存储键**，否则老盘数据全丢 —— 有专门断言守这条 |
| 校徽 | 内联 base64（128×128，**调色板量化后仅约 6KB**） | 全项目**只存这一份**，favicon 与两处 `<img class="brand-logo">` 共用；无任何外链请求 |
| 校徽适配 | 自带透明底；暗色主题加 `filter:brightness(1.5)` | 深青色校徽在深底上不发闷 |
| 打包图标 | `assets/南大学生工作台.ico`（16/24/32/48/64/128/256 多尺寸） | 供 v1.0 的 Tauri 打包使用（原型阶段用不到） |
| 作者与反馈 | **吴章凡** · 企业微信 · `wuzhangfan110@163.com` | 出现在登录页页脚、设置弹窗、指引页页脚、向导欢迎页与末页 |
| 版本号 | `APP_VER` 常量（出现在设置与指引页脚） | 方便辅导员反馈时说清是哪个版本 |

> 反馈邮箱用 `mailto:` —— **只有用户点了才会拉起邮件客户端**，软件自身不产生任何网络请求，不破"全程离线"红线。

---

## 六、交付说明

### 已交付
| 文件 | 说明 | 状态 |
|---|---|---|
| `中南大学生工作台.html` | 高保真可点击原型，浏览器双击即开，无需任何环境（**v0.7 起由「辅导员工作站原型.html」更名而来**） | ✅ 已交付 |
| `assets/南大学生工作台.ico` | 多尺寸应用图标，供 v1.0 Tauri 打包使用 | ✅ 已备好 |

### 6.1 原型实际已覆盖的功能（v0.2 逐项核对）

| # | 功能 | 文档原稿 | 原型实况 |
|---|---|---|---|
| 1 | 密码设置与登录验证（本地模拟） | 有 | ✅ 一致 |
| 2 | 学生列表 + 统计条（4 项指标） | 有 | ✅ 一致 |
| 3 | 预设关注视图 + 班级切换 | 有（6 个） | ✅ 侧栏展示 5 个 + 「全部学生」 |
| 4 | 筛选器（快速搜索/多字段下拉/自定义条件）+ 常用筛选保存 | 有 | ✅ 一致（含「未填写」选项） |
| 5 | 学生详情（32 字段分组展示 + 成绩总览） | 有 | ✅ 一致（7 个分组） |
| 6 | 辅导员备注编辑与保存 | 有 | ✅ 一致 |
| 7 | 导入向导（三步：选文件 → 确认映射 → 完成报告） | 有 | ✅ 一致 |
| 8 | 导出当前筛选结果为 CSV（含全部字段） | 有 | ✅ 一致（带 BOM，Excel 打开不乱码） |
| 9 | 设置页（改密码 / 清空数据） | 有 | ✅ 一致 |
| ➕10 | **数据总览看板**：6 指标卡 + 9 个图表/看板模块（v0.5 删 2 增 3），全部可下钻筛选 | **文档未写** | ✅ 已实现，超出设计稿 |
| ➕11 | **三套主题**：明亮 / 暗黑 / 护眼（豆绿米黄） | **文档未写** | ✅ 已实现，超出设计稿 |
| ➕12 | **育人金句**：60 条（立德树人/因材施教/师生关系/师德修养/成长鼓励五类），顶栏点击换句 | **文档未写** | ✅ 已实现，超出设计稿 |
| ➕13 | **页面个性化**（v0.6）：列表列自定义（勾选 + 拖动排序）· 详情页字段自定义 · 自定义字段 · 枚举字段选择题 · 筛选多选 | **文档未写**（v0.6 已补 5.10/5.11） | ✅ 已实现 |
| ➕14 | **中南大定制 + 上手路径**（v0.7）：改名「中南大学生工作台」· 校徽图标 · 作者与反馈 · **取数 / 导入指引页** · **首次打开新手引导** | **文档未写**（v0.7 已补 5.12–5.14） | ✅ 已实现 |

> **重要更正（v0.2）**：本节原稿写「Excel 解析：联网时用 SheetJS，离线降级演示数据」——**这一条已经过期**。
> 原型现已把 **SheetJS 0.18.5 完整内联进 HTML**（约 245KB），任何情况下都不再发起网络请求，离线解析 xlsx 为真实能力，不再是演示降级。

### 6.2 文档承诺但原型尚未实现（⬜ 需进 v1.0）

| # | 文档承诺 | 现状 | 影响 |
|---|---|---|---|
| 1 | **导入日志**（imports 表：文件名/哈希/增改计数/操作人/时间） | 🟡 **v0.3 部分实现** | 「批次」已记录 `name / importedAt / sourceFile / mode / 人数`，结果页有本次增改计数与异常行；**仍缺**：文件哈希（识别重复导入）、按次查询的历史日志表 |
| 2 | **不静默删除**（新表未出现的学生标记「本次未出现」） | ✅ **已实现（v0.2 补上，v0.3 修正本节）** | 合并模式保留下落并列出「本次未覆盖（仍保留）」名单；覆盖模式下被移除的学生另计并已自动备份 |
| 3 | **异常行单独列出**（学号为空的行） | ✅ **已实现（v0.2 补上，v0.3 修正本节）** | 学号为空的行不写入，并在结果页给出**对应 Excel 行号** |
| 4 | **classes / courses / settings / audit_log 表** | ⬜ 未做 | 原型用 localStorage 存批次，班级仍为派生值 |
| 5 | **SQLCipher 整库加密 / Argon2id + 随机盐** | ⬜ 未做（原型为简易哈希） | 属"原型 → 正式版"的预期差异，非缺陷 |
| 6 | **照片字段预留** | ✅ 字段已保留（`照片`，全空） | 第 1 版不做匹配，仅留列，符合原结论 |

### 6.4 v0.3 原型新增（需求反馈三项）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **导入三模式** | 新增/合并（默认）· 覆盖当前批次 · 新建批次；第 1 步模式卡、第 2 步按模式预统计、第 3 步按模式报告 |
| 2 | **覆盖模式的备注搬运** | 清空前按学号取出备注并回填；搬不回去的**点名列出**；执行前自动导出 CSV 备份 |
| 3 | **数据批次** | 顶栏下方常驻批次 Tab 条；批次管理弹窗支持切换 / 重命名 / 删除（删前自动备份 + 二次确认）/ 单批次导出；上限 20 个批次 |
| 4 | **存储 v2 + 迁移** | `counselor_workstation_v2`；旧 v1 数据自动迁移为第一个批次，v1 保留可回退 |
| 5 | **修掉「清空后又灌回演示数据」** | 清空改为软清空（写回空批次表），重启不再自动生成演示数据，改为空状态引导 + 手动「恢复演示数据」 |
| 6 | **顶部数字可点击** | 列表页 4 个数字全部有落点；看板「平均加权成绩」改可点；新增 `已写备注`、`未录入成绩` 两个预设 |
| 7 | **快速搜索计数同步** | 打字时 4 个数字与表格一起刷新（原来只刷第 1 个） |
| 8 | **窄屏适配至 360px** | 4 档断点；侧栏改抽屉；表格重排为卡片；看板单列；顶栏精简 |
| 9 | **图表按真实像素重绘** | 轴字不再被等比缩到不可读；卡片高度恒定；点击定位改从 `data-*` 读宽度，不再错位 |
| 10 | **姓名显示兜底** | 新系统表只有「姓名」没有「姓名1」，新增学生不再显示空白姓名（测试抓出） |

### 6.5 原型自检（v0.3 新增 · v0.4 / v0.5 / v0.6 / v0.7 增补）

| 脚本 | 覆盖内容 | 结果 |
|---|---|---|
| `.build/check-syntax.js` | 内联 script 块语法（v0.7 起为 5 块：主题预载 / 校徽 / 应用 / SheetJS） | ✅ 全部通过 |
| `.build/test-import.js` | 四铁律 · 列名归一 · 三模式导入 · 字段缺失降级 · 顶部数字落点（含成绩口径）· 图表宽度兜底 | ✅ **47/47** |
| `.build/test-batch.js` | v1→v2 迁移 · 软清空 · 批次切换 · 删除批次 · 容量上限 · 引用不变量 | ✅ **43/43** |
| `.build/test-grade.js`（v0.4 新建） | 成绩表脏结构解析（表头第 3 行 / 剔合计行 / 跳空行）· 按学号 upsert · 批次隔离 · 预设与分档 · 导出合并 · 老批次无 grades 的向后兼容 | ✅ **59/59** |
| `.build/test-sort-dorm.js`（v0.5 新建） | ① **列表排序**：数值/文本两种口径 · 无值沉底（升降序都沉）· 表头渲染 · 取消排序复原；② **详情编辑**：字段/备注/成绩写回 · 清零即删记录 · **改学号带动成绩搬家** · 手工补录成绩 · 草稿随关闭丢弃；③ **宿舍看板**：三种表结构解析 · **无床位列的末段识别** · 同房合并 · 容量上限 · 空位/超员 · 男女/mixed · 筛选排序 · 床位基准调整 · 无字段置灰；④ **看板卡片**：被删的卡确已消失 · 新卡存在 · 预警排序 · 单类别隐藏 | ✅ **118/118** |
| `.build/test-custom.js`（v0.6 新建 · 104 条） | ① **列自定义**：任意字段成列 · 顺序被尊重 · 本批没有的字段不占列 · 全空列可选且如实标注 · 拖动/上下排序 · 草稿两段式（取消不落盘）· 至少保留一列 · 渲染联动；② **字段设置**：隐藏字段 **数据不丢** · 自定义字段能建/能录/能当列/能筛 · **删除字段不删数据** · 重名保护；③ **选择题**：分类正确（枚举 vs 自由文本）· 选项来自本批 + 预置 · **历史脏值置顶保留** · 渲染成 `<select>` · 选值写草稿、保存才落库；④ **多选筛选**：累加/去重/逐个移除 · 「或」与「且」· `__EMPTY__` 多选 · chip 逐个可撤 · 常用筛选保留多选 · 旧字符串写法兼容；⑤ **内联自定义值**：展开/预填/边敲边记（不掉焦点）/回车提交补选项/留空拨回原值/**blur 不二次清空** · 源码内无 `prompt(`；⑥ **批次重命名**：行内编辑 · 确定落盘 · 取消还原 · 空名拒绝 | ✅ **104/104** |
| `.build/test-brand-guide.js`（v0.7 新建 · 56 条） | ① **品牌**：新名字到位 · **旧名零残留**（「工作站」这一旧称谓也清零） · **存储键未被改名动过**（老盘数据不丢）· 校徽 base64 内联且**全文件只存一份**（favicon 与两处 `<img>` 共用）· 体积可控；② **作者与反馈**：登录页 / 设置弹窗 / 指引页都有作者与邮箱 · 邮箱走 `mailto:` 不产生请求；③ **指引页**：智慧学工路径（学生数据›学生基本信息›自定义导出›全选）· 教务路径（成绩管理›查询分析统计›专业成绩单›上课年级/专业›查询）· 两个系统入口可点 · 三种导入方式 · **二次请求坑与「备注永不覆盖」都写明** · **一条数据都没有时指引页照样能开**；④ **首次引导**：全新安装会弹 · **老数据视为已看过（老用户不被拦）** · 五步内容逐页校对 · 步骤点与边界（不能翻过头/回头）· **走完或跳过才落盘** · **中途关掉不算看过** · **结构守卫：`guideSeen=true` 全源码只在一处** | ✅ **56/56** |
| `.build/verify-real.js`（v0.4 新建 · v0.5 扩宿舍/排序/预警 · v0.6 扩页面个性化） | **用系统真实文件端到端跑**：导入 189 人 + 162 条成绩 → 100% 匹配、0 孤儿、27 人「暂无成绩」；**宿舍看板 54 间 / 空位 27 / 满员 35 / 男寝 11 / 女寝 42**（房间数远小于人数，说明确实按房间合并）；列表按成绩升降序 · 学业预警 28 人（最严重者挂 8 门排第一）；**列自定义（真实字段成列/缺字段不占列）· 选择题选项=本批真实 10 个班级 · 内联自定义值在真实学生上可用 · 多选筛选与真实人数对账（俄语2401=9 人 + 法语2401=12 人 → 21 人）· 自定义字段在真实记录上能录能筛** | ✅ **78/78** |

> 全部脚本用 Node `vm` + DOM 桩**跑原型里的真实函数**（不是纸上推演），`verify-real.js` 还会加载内联的 SheetJS 真读 xlsx。
> **v0.5 的教训**：`verify-real.js` 原先只喂了**41 列模板**（有独立的房间号列），宿舍解析走的是"三列分开"这条最干净的路，
> 于是 **12 列模板（只有一整格「住宿地址」）的解析缺陷一直没暴露** —— 直到 v0.5 补上宿舍看板、换成 12 列模板重跑才现形（189 人 → 188 间房）。
> 结论：**真实数据要覆盖"所有导出模板"，只跑一份不算验过**。
> **v0.6 的收获**：`verify-real.js` 里发现「**新建批次会丢弃整列全空的字段**」（`rec[k]` 只收非空值）——
> 于是"邮箱/身份证号"这类本批全空的列**不会**出现在字段清单里。这不是缺陷（符合"不存空值"），
> 但它决定了 5.10.1 里那条"本批全空"的标注在本批真实数据上摸不到；需要时可用**自定义字段**建同名字段把它启用起来。
> 这一条已写进自检，避免以后有人当成 bug 去改。

> 被这套测试抓出并修掉的缺陷：**「姓名显示兜底」**、**「覆盖模式下缺失名单恒为 0」**（v0.3）；
> **「首次安装时 S.grades 未同步触发脱钩自检」**、**「成绩缺失提示语把'没导成绩'说成'字段缺失'」**（v0.4）；
> **「12 列模板把床位当房间 → 189 人拆成 188 间房」**（v0.5）。
> v0.6 未产生产品缺陷，但自检抓出**我自己写错的 3 条断言**（其中 1 条恰好证明功能是对的：删掉自定义字段后数据仍在，
> 所以它仍算"本批存在的字段"）—— 断言错了要改断言，不要改功能去迎合断言。

> **⚠️ v1.9.5 脚本现状（上表已是历史记录）**：`test-batch / test-grade / test-sort-dorm / test-custom /
> test-brand-guide` **这 5 个文件在仓库里已经不存在** —— 它们后来被合并进了 `test-library.js`（上表的条数
> 是各自"当年"的结果，保留下来用于追溯覆盖范围，不代表当前文件数）。
> **当前 `.build/` 一共 32 个脚本**（v2.0.0 新增 `embed-templates.py`、`verify-v198.js` 等；早期新增的 `make-icns.py`、`ci-watch.py`、`ci-fetch.py` 是做 macOS 版时加的）：
>
> | 脚本 | 跑法 | 结果（v1.9.4 实测） |
> |---|---|---|
> | `check-syntax.js` | `node .build/check-syntax.js <html>` | ✅ 原型 5 块 / 桌面版 6 块全通过（另含**按钮类名静态门禁**，见 6.12） |
> | `test-import.js` | `node .build/test-import.js 中南大学生工作台.html` | ✅ **38/38** |
> | `test-library.js` | `node .build/test-library.js 中南大学生工作台.html` | ✅ **128/128**（上述 5 个脚本的合体） |
> | `test-delete-backup.js` | `node .build/test-delete-backup.js 中南大学生工作台.html` | ✅ **32/32**（v1.9.3 新增 §[9] 筛选复选框面板 6 条） |
> | `test-desktop.js` | `node .build/test-desktop.js` | ✅ **28/28**（读 `app/src/index.html`） |
> | `real-roster-check.js` | `node .build/real-roster-check.js <页面URL> <真实.xls>` | ✅ **9/9**（**真浏览器 + 真实名册**，见 6.11） |
> | `shot-v192.js` | `node .build/shot-v192.js <页面URL>` | ✅ **11 步**浮窗批注逐项核对 + 常用功能四步点名 + 折叠还原 + 2 个二维码解码，0 报错 |
> | `shot-v193.js` | `node .build/shot-v193.js <页面URL> .build` | ✅ AI 页两卡并排 / 二维码同框同底色 / 关于无码 / 筛选面板 / 0 报错（见 6.12） |
> | `count-facts.js`（工具） | `node .build/count-facts.js` | ✅ 从原型核出模板 12 条 / 导航 87 条 9 分区 / 向导 6 步 / 导览 11 步等（见 6.13） |
> | `md2html-manual.py`（工具） | `python .build/md2html-manual.py <md> <html>` | ✅ 手册 Markdown → 符合 doc-typeset 规格的 HTML（html-review 100 分） |
> | `make-icns.py`（工具） | `python .build/make-icns.py <png> <icns>` | ✅ 生成 macOS 的 6 档 .icns（容器长度自洽） |
> | `ci-watch.py`（工具） | `python .build/ci-watch.py [run_id]` | ✅ 盯 Actions 运行，失败自动抓日志尾部 |
> | `ci-fetch.py`（工具） | `python .build/ci-fetch.py logs\|artifact <run_id> [目录]` | ✅ 取日志 / 下载 dmg（手动跟随 302，第二跳去掉鉴权头） |
>
> `verify-real.js`（用学校真实导出的 189 人学生表 + 162 条成绩表端到端跑）**仍然保留，但这次没有重跑**：
> 它依赖两份含学生个人信息的原始导出文件，按惯例**不进版本库**；这次改的是导入路径，覆盖它的是上面
> `real-roster-check.js`（真实 `.xls` 名册）与 `test-import.js`（38 条导入断言）。下次手上有那两份源文件时
> 再跑一遍即可：`node .build/verify-real.js 中南大学生工作台.html <学生信息表> <成绩表>`。

### 6.6 v0.4 原型新增（成绩层）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **成绩层数据模型** | 成绩作为独立数据集按 `学号` 挂在批次上（`batch.grades`），`S.grades` 与批次同引用，写入口唯一 `setGrades()`；存储向后兼容，老批次自动补空数组 |
| 2 | **成绩表导入向导** | 独立入口（侧栏/列表页卡片头/批次管理）；**自动定位第 3 行表头**；按「学号是纯数字」过滤，**合计行剔除并留痕**；导入前预览匹配数/孤儿点名/暂无成绩数；按学号 upsert，未出现的成绩保留 |
| 3 | **绑定显示** | 列表「成绩」列（加权成绩 + 绩点·排名）、详情「成绩」区块 8 项、关注列「不及格 n 门」红标；无成绩统一显示「暂无成绩」 |
| 4 | **预设与顶部数字改口径** | 新增 `有不及格`／`绩点偏低`／`暂无成绩`；顶部数字 4→5 个；`需重点关注` 改为「有不及格或军训/旧挂科字段」 |
| 5 | **看板成绩化** | 指标卡改「有不及格／平均加权成绩／平均学分绩点」；成绩分档铺满 `<70…95-100` 六档；新增**班级平均加权成绩**整行图表；关注提醒新增三项 |
| 6 | **导出带成绩** | 「导出当前结果」与批次备份把成绩并进同一行，列名 `成绩·××`，不覆盖档案列；无成绩则不加空列 |
| 7 | **演示数据补成绩** | 首次安装的演示批次按内置成绩推导出成绩层，开箱即可看到绑定效果 |

### 6.7 v0.4 明确不做（辅导员决定）

| 功能 | 不做的原因 |
|---|---|
| 按学期筛选成绩 | 系统导出的成绩表是**一人一行的总评快照**，全表**无「学期」列**，也没有课程级明细 → 数据不支持，直接放弃 |
| 成绩升降趋势 / 单科挂科明细 | 同上：缺「学期 + 课程 + 单科成绩」这份源数据 |
| 课程维度分析 | 同上：成绩表无课程名/代码/性质/教师 |

> 若将来教务系统能导出**课程级明细**（`学号 / 学期 / 课程 / 成绩`），可在此基础上重启该功能；
> 当前**不列为待办**，避免在数据不支持的条件下做伪功能。

### 6.8 v0.5 原型新增（辅导员第 7 轮反馈）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **列表表头排序** | 任意数据列可点排序、再点反向；成绩列默认降序；无值升降序都沉底；排序只在视图层、不落盘，重开回到导入序；`#`/`关注` 不可排 |
| 2 | **详情页全字段编辑** | 详情页整体进编辑态；学生信息 + 成绩 8 项 + 备注 + 自动归集的「其他字段」组全部可改；改学号同步搬移成绩记录；成绩清空即删记录；可手工补录成绩 |
| 3 | **宿舍看板** | 房间卡网格（逐床位显示姓名、空位显式标注）；男女/mixed 判定；满员/空位/超员角标；床位基准可调（2/4/6/8 人间，落盘记忆）；性别/楼栋/排序/只看有空位四类筛选；未分配点名；无住宿字段时置灰 |
| 4 | **宿舍解析归一** | 三种表结构（三列分开 / 旧表「楼-房」/ 12 列模板「楼房床挤一格」）统一解析为 `楼栋-房号`；修掉 12 列模板把床位当房间的缺陷 |
| 5 | **看板卡片瘦身** | **删**「宿舍楼分布」（太碎）、「毕业学校词云」（无此列）；**加**「学业预警名单」（按严重度排序）、「宿舍分配概览」（点进宿舍看板）；「备注完成度」→「数据完整度」（只列本批真实存在的字段） |
| 6 | **单类别自动隐藏** | 专业/民族/政治面貌等分布图，若本批**只有一个取值**则该卡自动隐藏（一张图只画一根柱没有信息量），避免"有数据但无意义"的卡片占位 |
| 7 | **看板"该说不说"补齐** | 新卡片同样遵守字段缺失降级：没导成绩时「学业预警名单」不出现或置灰说明；没住宿字段时「宿舍分配概览」置灰 |

> 需求原文（第 7 轮）：① 列表表头加排序；② 详情页所有项目最好都能编辑；③ 新增宿舍看板（知道不同宿舍有哪些人、按男女排序、是否有空位）；④ 总览卡片优化 —— 宿舍楼分布太碎取消、毕业学校词云没数据删掉，找"有效看板、值得关注的信息"。

### 6.9 v0.6 原型新增（辅导员第 8 轮反馈）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **列表列自定义** | `FIXED_COLS`（有专门长相的列）+ 通用单元格渲染；任意字段（含自定义字段）可成列；勾选显示 + 拖动/上下调顺序；落盘记忆；本批没有的字段不占列并标注原因 |
| 2 | **详情页字段设置** | 按分组勾选显示哪些字段；**只隐藏不删数据**，勾回来内容还在；备注与成绩始终显示 |
| 3 | **自定义字段** | 新建导入表里没有的字段（家长电话/家长姓名/高考位次…），与导入字段等价：能录、能当列、能筛、能导出；重名保护；**删除定义不删数据** |
| 4 | **详情页选择题** | 性别/政治面貌等用预置项；班级/专业/院系/民族/生源地/学籍状态等按本批数据自动生成选项；当前值永远置顶保留；留「手输其他」**（内联输入框，不用系统弹窗——见 5.10.5）**；首项「未填写」可清空 |
| 5 | **筛选条件多选** | `S.filters` 值升级为数组；同字段多值「或」、跨字段「且」；chip 逐值可撤；下拉带 ✓；常用筛选保留多选；兼容旧字符串写法 |
| 6 | **总览页筛选同步** | 总览页筛选器与列表页共用同一份多选状态，图表与列表永远同口径 |
| 7 | **个性化落盘** | `listCols / hiddenFields / customFields` 三项随本地存储持久化，重开保持；老数据缺失时自动取默认 |

> **v0.6.1 追加（第 9 轮反馈）**
>
> | # | 功能 | 说明 |
> |---|---|---|
> | 8 | **内联自定义值** | 枚举字段选「＋ 手输其他…」→ 字段下方**就地展开输入框**（预填原值 · 边敲边记 · 回车提交 · 留空拨回原值 · 提交后收起）；**不再用 `prompt()`** |
> | 9 | **批次重命名改行内编辑** | 该行直接变成"输入框 + 确定/取消"，同一原因（WebView 不支持系统弹窗） |
> | 10 | **静态守住** | 源码里**不得再出现 `prompt(` 调用**；回归里有一条专门的静态断言 |
>
> 需求原文（第 9 轮）：**「编辑页面得有一个自定义，因为有很多情况不在选项中。」**
> —— 表面诉求是"要能录自定义值"，实际暴露的是**浏览器能跑、桌面外壳跑不了**的环境差异。

> 需求原文（第 8 轮）：① 部分字段在学生详情可否改为选择题，例如班级、性别、政治面貌（群众/共青团员/中共党员）等；
> ② 我导入的学生信息模板还有很多数据，例如邮箱、身份证号等等，能否在详情页面自定义；理论上还有学生家长与联系方式 —— 这种能否在全部学生的列表中，对列进行自定义、定制个人的页面。
> 追加一条：**「同时筛选条件支持多选」**。

### 6.10 v0.7 原型新增（辅导员第 10 轮反馈 · 中南大定制）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **产品改名** | 「辅导员工作站」→ **中南大学生工作台**；页面标题 / 登录页 / 顶栏 / 设置 / 指引页统一；文件名同步改为 `中南大学生工作台.html`；**存储键 `counselor_workstation_v2` 保持不动**，老盘数据无需迁移 |
| 2 | **校徽图标** | 校徽内联 base64（128×128 调色板量化，约 6KB），全项目**只存一份**，favicon + 登录页 + 顶栏共用；暗色主题自动提亮；另生成 `assets/南大学生工作台.ico` 供打包 |
| 3 | **作者与反馈** | **吴章凡** · 企业微信 · `wuzhangfan110@163.com`；落在登录页页脚、设置弹窗、指引页页脚、向导首末页；反馈用 `mailto:`，软件自身零请求 |
| 4 | **取数 · 导入指引页** | 常驻页面（侧栏 / 空状态页 / 设置三处入口）；写明**智慧学工**（学生数据›学生基本信息›导出›自定义导出›全选›导出）与**综合教务**（成绩管理›查询分析统计›专业成绩单›确认上课年级/专业›查询›导出）两条路径 + 两个系统入口 + 导入本工作台三步 + 四条易踩坑 |
| 5 | **首次打开新手引导** | 五步向导（欢迎 → 取学生信息 → 取成绩单 → 导入 → 可以开始了）；**延迟 420ms 弹出**不打断首屏；走完/跳过才落盘，中途关掉下次还弹，**老数据视为已看过** |
| 6 | **版本号可见** | 新增 `APP_VER` 常量，出现在设置与指引页脚，方便反馈时报版本 |
| 7 | **一条数据都没有时也能进指引页** | `renderMain` 把 `guide` 分支提到最前 —— 否则刚装好、还没导入的辅导员恰好看不到最该看的东西 |

> 需求原文（第 10 轮）：**「功能上没有大问题了。下面我们结合中南大的特点，做一些使用体验优化。
> 1 名字修改为 中南大学生工作台 2 icon 修改为校徽 3 添加作者信息和反馈方式… 4 软件首次打开，引导辅导员使用导引优化。
> 学生信息数据从智慧学工… 学生数据—学生基本信息，然后选择导出—自定义导出—全选—点击导出保存。
> 学生成绩信息从综合教务系统… 成绩管理—查询分析统计—专业成绩单，确认上课年级，上课专业按需多选，点击查询后导出保存。
> 然后根据本软件的导入，写一个页面的导入提示。」**

### 6.11 v1.9.2 原型新增（辅导员第 11 轮反馈）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **表头自动定位** | 各校导出的表前面常压着 1~3 行标题带、盖章栏、填报日期，**真表头不在第 1 行**（本校积极分子名册就在第 4 行）。导入时扫描前 20 行，按「同时出现学号列与姓名列」找表头；找不到就退化为「第一处出现学号列的行」，再找不到才用第 1 行。报错给的行号改成**原表真实 Excel 行号**（逐行记录），不再用"下标 + 表头行 + 2"推算 —— 中途剔过行那样会错位 |
| 2 | **非人员行过滤** | 表头正下方常压着一行「培养联系人 1 \| 2」的子表头，表尾还可能吊着「二级党委主要负责人签名」「说明：…」。这些行在**学号/姓名两列都是空的**，一律剔除——**不再被当成"异常行"报给辅导员**（"有 1 行学号为空"这种虚警是上一版最主要的不满来源）。拿不准时（表里压根没有学号/姓名列）不做过滤，**宁可多报也不漏报** |
| 3 | **入党积极分子名册 → 自动同步政治面貌** | 识别到名册（列里出现「所在党支部」/「确定为入党积极分子时间」/「递交入党申请书时间」任一）后，把匹配到学生的「政治面貌」同步为「入党积极分子」，省下几百次手点。**两道闸**：① **不降级** —— 已是中共党员/预备党员/发展对象的原样不动（名册是"曾经是积极分子"的存底，不该把已入党的人拉回来）；② **不覆盖** —— 名册自带「政治面貌」列且写了值的，以名册为准。结果页分别报「同步 N 人」「保住 N 人」，覆盖模式下还会额外提示"名册新增的 N 人不会进库" |
| 4 | **班委别名扩充** | 班干部 / 班级委员 / 班级职务 / 职务 / 担任职务 → 统一归到「班委」，列表「班委」列与关注视图「班委成员」都能吃到 |
| 5 | **学号别名扩充** | 「学号/职工号」（单元格里还带换行）、「学号（职工号）」、「职工号」→ 归到「学号」主键 |
| 6 | **二维码内置** | 综合评定答疑机器人（企业微信）二维码**内联进程序**，出现在「AI 辅导员」页与「系统设置 → 关于」两处，旁边给「复制链接」「用浏览器打开」；并写明分工（制度流程找鹿晓南、综合评定找它）。图片先裁成**纯二维码**（284×284 / 16 色 / 5.3 KB），标题交给页面文字承担，避免图里图外重复一遍；每张都用 `.build/qr-check.js`（jsQR）解码验证确实可扫<br>⚠️ **v1.9.3 已调整**：「关于」里那份**已删掉**，AI 页两张码改为等宽双卡并统一外框（详见 **6.12**） |
| 7 | **首次引导之后加"页面浮窗批注"** | 走完首次引导再带一遍 **7 步浮窗批注**（顶栏导入 / 我的、批次条、左侧栏、取数指引、筛选条、关注视图、个人资料），高亮框用 `box-shadow` 挖洞、气泡自动避让视口边界。`S.tourSeen` 落盘；**老用户不打扰**（数据里已有 `S.tourSeen`，或从更早的备份导入时视为已看过）；「取数 · 导入指引」与「系统设置」都留了「重看页面导览」 |
| 8 | **导入失败不再"用演示数据顶替"** | 见下方"两个真根因" |

**这一轮修掉的两个真根因**（辅导员反馈的现象是"二次导入积极分子手册，结果进来一堆软件模板人员"）：

| 根因 | 为什么会出现那个现象 | 修法 |
|---|---|---|
| 内联的 SheetJS 是 **mini 精简版**，**根本不支持 `.xls`（Excel 97-2003 / CFB）**，只认 `.xlsx` | 学校发的名册是 `.xls`，一律解析失败 → 于是必然走到下面那个兜底上 | 换成**完整版**（`xlsx.full.min.js`，含 `parse_xlscfb` 与 `CFB`）；内联体积 250,376 → 639,124 字符（+388,748） |
| 解析失败被 `catch` 吞掉后**静默换成内置演示数据导入** | 读不了的表被当成"导入成功"，库里凭空多出几十个"软件模板里的人"，**而且真的落盘了**，辅导员还以为是自己导错了 | 删掉 `simulateImport()`；改为 `openFileError()` **明说打不开**、列常见原因（加密 / 损坏 / 正被 Excel 占用 / 旧格式）并给"另存为 .xlsx"的下一步，**一个字都不入库** |

> 需求原文（第 11 轮）：**「1 补充积极分子预审表模板识别。支部统计上报材料 无需处理，自动同步积极分子信息，并绑定政治面貌选项题 方便后期维护。
> 2 班委词条范围扩大，匹配班干部 班委 班级委员 职务等。
> 3 补充学工机器人二维码 教师使用更友好。
> 4 添加首次引导后 页面浮窗批注 进一步做好新人功能掌握。
> 5 解决二次导入（例如用积极分子手册 更新学生政治面貌时）导入软件模板人员的bug。」**

> **本轮自检新增 / 改动的脚本**
>
> | 脚本 | 覆盖内容 | 结果 |
> |---|---|---|
> | `.build/test-import.js`（扩） | ① **表头自动定位**：表头在第 1/3/4 行都能找到 · 找不到时的退化路径；② **非人员行过滤**：子表头行与签名/说明行被剔除、**不再报成异常行**、拿不准时不过滤；③ **行号**：剔行后报出的仍是原表真实 Excel 行号；④ **名册同步**两道闸（不降级 / 不覆盖）· 结果页统计；⑤ 新别名（班干部·班级职务·学号/职工号）；⑥ **老样式表行为不变**（表头第 1 行 · 铁律①备注不被覆盖） | ✅ **38/38** |
> | `.build/real-roster-check.js`（新建） | **拿辅导员那份真实的 `.xls`《20260706附件6 入党积极分子名册》走完整解析路径**：表头落在第 4 行 · 认出名册 · 「学号/职工号」归一 · 首条真数据在 **Excel 第 6 行**（第 5 行「培养联系人 1\|2」子表头已剔除）· **解析出 71 条**（老代码这里是 **0 条**）· 0 异常行 · 政治面貌同步 71 人 · 日期没变成 `45938` 这种序列号 | ✅ **9/9** |
> | `.build/shot-v192.js`（新建 · 真浏览器） | 用 Playwright 驱动本机 Edge 打开真实页面：**走完 7 步浮窗批注**，每步断言「高亮框套住了目标元素」且「气泡完整在视口内」· 走完浮层自动关闭 · `S.tourSeen` 已置位 · **两个二维码都能解码**（jsQR）· 全程 **0 条 JS 报错** | ✅ **全部通过** |

### 6.12 v1.9.3 原型调整（辅导员第 12 轮反馈）

> 本轮**没有数据格式变更**，纯界面与交互调整，升级不动已有数据。

| # | 调整 | 说明 |
|---|---|---|
| 1 | **AI 辅导员页重排** | 原版式是"左卡带图 + 右下卡横跨两列 + 旁边再挂一张「什么问题适合问鹿晓南」说明卡"，三块挤在一起。现改为**两人各占一张等宽卡片、左右并排、同一行起平**；说明卡删除 —— 它讲的内容（请假销假 / 奖助学金 / 缓考学籍）本来就是左卡该说的话，**并进左卡的三条要点**。两卡内部次第统一为：标题 → 一句话分工 → 二维码 → 能问什么 → 按钮。窄屏（< 980px）自动落成上下两栏 |
| 2 | **两张二维码统一格式** | 原来一张是 468×586 带深色底板与标题字的整图、一张是 284×284 纯码，并排明显不齐。现用 `scipy.ndimage.label` 连通域从整图里**裁出纯二维码**（378×378），两张统一套 **200×200 白底圆角外框**（`.qr-box`），图片 `object-fit:contain` 按比例填满，**外框尺寸完全一致**。**白底硬编码在卡片上、不跟主题走** —— 深色主题下若改用 `var(--card)`，会变成"黑底上的黑码"，扫不出来 |
| 3 | **「关于」里的二维码删掉 + 两码下面加「复制链接」** | 关于页是看版本号与作者的地方，塞码没意义，已删（软件 / 存储 / 作者 / 版本号都在）。AI 页两张码下面**各加一个「复制链接」**：按一下把该机器人的企业微信链接复制到剪贴板，可直接粘进企业微信班级群，学生点开即用、不要求人人会扫码；旁边保留「在浏览器打开」。为此从两张码分别解出链接常量 `LU_URL` / `BOT_URL` |
| 4 | **筛选改复选框面板** | 原来是系统 `<select>`，**勾一个值就自动收起**，多值筛（"男 + 女"、"党员 + 积极分子 + 团员"）只能反复点开反复找。改为自绘 `.fdrop` 面板：点开 → 每个取值一个复选框 → **勾选立刻生效、面板不关** → 点「完成」或点面板外收起。按钮上**直接显示已选值**（如"男、女"），选多了显示角标数字；面板内「清除本组」只清该字段。**筛选语义不变**：同组多值 =「或」，不同组之间 =「且」。「总览」页筛选条一并替换；**「预设视图」仍是单选**（它本质是"整套视图"，不是多值条件） |

**这一轮顺手修掉的一个自挖坑**

加按钮时把主按钮类名写成 `class="btn primary"`，而本项目 CSS 里主按钮叫 **`.btn.pri`**，
`.btn.primary` 不存在 —— 于是「复制链接」和面板里的「完成」**静默退化成白按钮**：
不报错、不崩溃、纯丑，自动化测试也难发现。已改回 `.btn.pri`，并给
`check-syntax.js` 加了一道**静态门禁**：从真实 CSS 里解析已定义类名，扫出"用了但 CSS 里没定义"的按钮类名就**直接失败**（判定放宽为 `.X` 或 `.btn.X` 任一存在即合法，避免误报 `.memo-add` 这类独立辅助类）。

> 需求原文（第 12 轮）：**「ai辅导员页面重新排版 现在好丑 什么问题适合问鹿晓南 可以删掉。左边辅导员 右边综合答疑。图片格式可以保持一致 去掉背景 仅保留二维码。由于这是因为应用。关于中的 二维码可以删掉 但是 上面2个图片下面 倒是可以添加 复制链接按钮（粘贴进企业微信中使用）筛选目前一次只能点一个 然后需要重新点击筛选。能否修改为 复选框。」**

> **本轮自检新增 / 改动的脚本**
>
> | 脚本 | 覆盖内容 | 结果 |
> |---|---|---|
> | `.build/shot-v193.js`（新建 · 真浏览器） | Playwright 驱动本机 Edge 打开真实页面：① AI 页**两卡左右并排**（同顶、右卡左边界在左卡右边界之后）· **两个 `.qr-box` 外框同为 200×200** · 两图 `naturalWidth > 0` · 旧说明卡已删且**三条内容确实并进了左卡** · 两个「复制链接」· **主按钮底色是品牌色（不是白）**；② 深色主题下 `.qr-box` 仍是白底；③ 关于页无内联图、作者/版本仍在，并把「关于」卡片单独截图；④ 筛选面板：能展开 · **连勾两个值面板全程不关** · 两个复选框都保持勾选 · 按钮显示"男、女" · 点面板外收起 · chip 同步 · 「完成」也是实心主按钮；⑤ 全程 **0 条 JS 报错**；⑥ 把两张码落盘交 `qr-check.js` 真解 | ✅ **全部通过** |
> | `.build/qr-check.js`（复用） | 解码重裁后的两张码：鹿晓南 → `…/24e2c0d1…897e8b`（378×378 / 9,166 B）、答疑机器人 → `…/24d1d5d6…4e857fc`（284×284 / 5,280 B），**两条 payload 与页面内 `LU_URL` / `BOT_URL` 逐一对应** | ✅ **2/2 可解** |
> | `.build/test-delete-backup.js`（扩） | 新增 **§[9] 筛选：复选框面板** 6 条：筛选栏已换掉原生 `<select>` · 连勾两个值都生效 · 勾选后面板保持展开（`_fdOpen` 记忆生效）· 按钮显示"男、女" · 再勾一次可取消单个值 · 「清除本组」只清本字段 | ✅ **6/6** |
> | `.build/check-syntax.js`（扩） | 新增按钮类名静态门禁（见上）；同时在 5 个 script 块语法检查里顺带覆盖桌面壳 `app/src/index.html`（6 块） | ✅ **通过** |
> | `.build/test-library.js`（改） | 原有一条断言引用已删除的「什么问题适合问鹿晓南」卡片，改为断言"2 张卡 / 2 个 `.qr-box` / 2 个 `copyPlain(` / 两个链接 hash 都在 / 旧卡不存在" | ✅ **128/128** |

**踩坑记录（供以后写测试参考）**

同一轮里我先写了一条断言 `document.body.innerHTML.includes('什么问题适合问鹿晓南')` 来确认旧卡已删，
结果**误报"旧说明卡还在"** —— 因为 `body.innerHTML` 会把 `<script>` 里的文本一起算进来，
而 `renderAssistant()` 上方那段解释"为什么要删这张卡"的**注释正好引用了这个字符串**。
**产品没问题，是断言写错了范围**。改为只看渲染容器 `#mainArea` 的 `innerHTML` 即通过。
**规矩：断言"某段文案不存在"时，永远不要把 `document.body` 当范围。**

### 6.13 v1.9.4 原型调整（辅导员第 13 轮反馈）

> 本轮只动"讲清楚"这件事，**没有数据格式变更**，升级不动已有数据。

| # | 调整 | 说明 |
|---|---|---|
| 1 | **引导浮窗补「常用功能」四步（7 → 11 步）** | 原导览只讲"界面上这块是什么"（批次 / 导入 / 侧栏 / 取数指引 / 筛选 / 关注视图 / 个人中心），但侧栏里几个**天天要用**的功能一直没讲 —— 光看名字看不出好处。现给侧栏「常用工具」四项各加一步：**① 校务导航**（校内系统集中在一页，走同一套统一身份认证 —— **登录任意一个，在登录状态还有效的这段时间里，点开其他几个不用再登一遍**）；**② 常用模板**（现成文案一键复制；**内容会持续更新，欢迎提供素材**）；**③ AI 辅导员**（校内答疑好帮手，**私信后可拉进自己的班级群**，重复问题学生自己问）；**④ 校历作息**（学年校历 + 课堂时间表，随手查阅）。锚点为侧栏对应项的 `data-tour="nav/tpl/pol/cal"` |
| 2 | **导览的"折叠即静默跳过"隐患** | 上述四步的锚点都在侧栏「常用工具」分组内，而该分组**可折叠**（`S.sideFold.tools`）—— 折起来 `tourAnchor()` 找不到元素，按既定规则会**静默跳过**那几步，等于白加。现改为 `startTour()` **临时展开**该分组、`tourEnd()` **还原**成用户原来的状态：原来设过就还原成设过的值，**原来根本没这个键就把临时写的键删掉**（不在用户数据里留脏值）。`tourScan` 早期返回（一个可指位置都没有）的分支也走同一套收尾，避免临时展开后没人还原 |
| 3 | **用户手册重写为"只讲当前版本"** | 原手册是「1.0.0 基础版 + v1.4~v1.9 逐版更新说明」的堆叠，读者得自己判断"哪条已被后续改动推翻"。现整本重写为**只描述当前版本**的 12 章手册（这是什么 / 安装与首次启动 / 第一次取数 / 界面导览 / 日常使用 / 总览与宿舍看板 / **常用功能** / 批次与导入历史 / 个人中心与系统设置 / 数据安全 / 常见问题 / 版本与联系）。版本历史回归 `CHANGELOG.md`，手册不再承担更新日志职责 |
| 4 | **手册内容与实现逐项对齐** | 手册里出现的每个数字都从代码里核出（见下方自检表的 `count-facts.js`）：模板 **12** 条、校务导航 **87** 条 **9** 分区、新手向导 **6** 步、浮窗导览 **11** 步、关注视图 **8** 类（+ 全部学生）等。另外修正了旧手册里两处与实现不符的说法：**「负责班级」是只读的**（由当前批次数据自动带出，不能手改）；数据总览是 **6 张指标卡**（在册学生 / 覆盖班级 / 需重点关注 / 有不及格 / 平均加权成绩 / 平均学分绩点） |

> 需求原文（第 13 轮）：**「引导浮窗 添加常用功能。例如校务导航 登录任意一个 就可以在账号活跃期 同步访问其他需要统一账号验证的网页。方便。 常用模板后续会更新 欢迎提供素材。AI辅导员是校内答疑好帮手，私信后可以添加到自己的群聊。校历方便查阅等等 用户手册更新 仅介绍最新版本的功能。」**

> **本轮自检新增 / 改动的脚本**
>
> | 脚本 | 覆盖内容 | 结果 |
> |---|---|---|
> | `.build/shot-v192.js`（扩 · 真浏览器） | 除原有"每步高亮框套住目标 + 气泡不出视口"外，新增：**11 步全部真的被走到**（锚点找不到会静默跳过，必须断言）· 四个常用功能步骤**逐个点名**（nav/tpl/pol/cal）· **§[1b] 折叠还原**：折叠后锚点确实不存在 → 开始导览后自动临时展开 → 结束后还原成折叠态 → 原本没设过则**不留脏键** | ✅ **全通过** |
> | `.build/count-facts.js`（新建） | **从原型里静态核出手册要写的数字**（模板条数与标签、导航条数与分区、向导步数、关注视图 id 与名称、导览步数、存储键、APP_VER、系统设置分区），避免文档与实现不一致 | ✅ 与手册逐项一致 |
> | `.build/md2html-manual.py`（新建） | 手册 Markdown → doc-typeset 规格 HTML：封面/正文双顶层 section · ≥3 个 h2 自动生成目录 · 表格补 thead/tbody 且表头用 `<th>` · 引用块转段落级 CSS 强调（带两个不折叠空格）· `margin/padding` 一律走 `var(--spacing-*)` 令牌（html-review 的 DT-03 门禁要求） | ✅ html-review **100 分**（六维全过） |
> | `.build/release-notes.md` / `CHANGELOG.md` / `README.md` / 本文件 | 同步 v1.9.4 的功能说明与文件名 | — |

**踩坑记录**

① 手册 Markdown 里那句开篇提示（`> 本手册只介绍当前版本…`）正好落在文件头（封面区）里，
   第一版转换脚本把它当成**封面元信息**渲染，结果文档里出现了字面量 `>`。
   修法：切分封面时**把 `>` 开头的行排除**、并把它挪到正文开头当开篇提示。
   → 规矩：**封面区只收"扁平的单行元信息"，任何带 Markdown 块语义的内容都要挪到正文。**

② html-review 门禁一次没过（47 分）给了三类问题，都不是"好看不好看"而是**硬约束**：
   `thead` 里用了 `<td>` 而非 `<th>`（5 张表各扣 20 分）、`margin/padding` 出现裸值 12 处、
   4 处 `<h2>` 后面直接跟 `<h3>`（缺章节导语）。按门禁改一次后复检 100 分。
   → 前两类是**可机器判定的形式约束**，写转换脚本时就该守住；第三类其实是**内容问题**
   （那 4 节确实上来就进小节），补一句真有用的导语比调样式值更对。

### 6.14 v1.9.5 新增 macOS 版（辅导员第 14 轮反馈）

> 需求原文（第 14 轮）：**「那先做一个MAC端的吧」**（承接上一轮的"其他平台支持 迁移难吗"咨询）。

**结论：这次"移植"几乎没改代码** —— 因为这一层当初的技术选型本来就偏跨平台，事后回看选对了：

| 层 | 现状 | 为 macOS 要做的 |
|---|---|---|
| 前端 | 单文件 HTML、无框架无构建；界面媒体查询已细到 420px | **一行没动** |
| Excel 解析 / 图表 | 纯 JS SheetJS；图表手写 SVG/CSS | 无 |
| Rust 外壳 12 个命令 | 打开外链 / 打开本地文件 / 数据读写 / 托盘 走的是跨平台 API | 只需改 1 处（见下） |
| 开机启动 | `tauri-plugin-autostart`，配置里**早已写好 `MacosLauncher::LaunchAgent`** | 无 |
| 打包 | `bundle.targets` 只有 msi/nsis；图标只有 .ico/png | 补 `tauri.macos.conf.json` + `icon.icns` |

**真正修掉的缺口（会在 Mac 上现形的那种）**：

| 问题 | 后果 | 修法 |
|---|---|---|
| `downloads_dir()` 只读 `USERPROFILE` —— **Windows 专有环境变量** | macOS 上取不到 → 静默退回 `std::env::temp_dir()` → 导出的 CSV / 备份落在 `/tmp`，辅导员永远找不到（表现为"点了导出没反应"） | 优先用 Tauri 跨平台路径解析 `app.path().download_dir()`；再退回 `HOME`/`USERPROFILE`；最后才退临时目录。**静默退到临时目录**这类兜底最危险：不报错、功能"看起来跑了"，只是文件去了别处 |

**macOS 打包配置**（只在 macOS 生效，Windows 配置**一个字没动**）：

- 新增 `app/src-tauri/tauri.macos.conf.json`：`bundle.targets = ["app","dmg"]`、
  `minimumSystemVersion = 10.15`、dmg 窗口布局、图标加 `.icns`。
  Tauri 的平台配置文件会自动与主配置深合并，所以不必在主配置里写平台判断。
- 新增 `icons/icon.icns`：macOS 打包**必需**。用 `.build/make-icns.py` 从 512×512 的 `icon.png`
  生成 6 档（32/64/128/256/512/1024）。**没有用 `tauri icon`** —— 它会把整个 `icons/` 重新生成一遍，
  白白改动 Windows 那套已经验证过的图标。

**怎么编出来的：借 GitHub 的 macOS 跑器**

苹果的工具链只能在 macOS 上跑，**Windows 上编不出 Mac 程序**（苹果的限制，不是配置问题；
Tauri 也不支持从 Windows 交叉编译 macOS）。而公开发布在 GitHub 上就能白用 macOS 跑器，
等于借一台 Mac 来编译：

- 新增 `.github/workflows/build-macos.yml`：装 Node + Rust 双目标
  → 生成前端（`app/src/index.html` 是自动生成的、**不在版本库里**，不先生成就会把空前端打进包）
  → `npx tauri build --target universal-apple-darwin`
  → 核对架构与签名 → 上传 dmg。
- **`universal-apple-darwin` = 同时装 x86_64 与 arm64 再合并**，一个 dmg 两种 Mac 通用。
- ⚠️ **`APPLE_SIGNING_IDENTITY: "-"` 不能省**：那是临时签名（ad-hoc）。
  没有它，Apple 芯片上**程序根本起不来**（arm64 要求可执行文件至少有 ad-hoc 签名）。
- 实测首次运行：**9 分 26 秒全部通过**，产物 `Mach-O universal (x86_64 arm64)`、`Signature=adhoc`、
  dmg 6.6 MB。

> ⚠️ **代价要说清**：没有 Apple 开发者账号（$99/年）就**不能公证（notarize）**，
> 使用者第一次打开会被 Gatekeeper 拦一次，必须「右键 → 打开」放行。
> 这个坑已经写进手册、Release 说明与安装说明三处 —— 否则同事会以为"文件坏了"。

**Windows 端一并重出**：因为改了 Rust 代码，Windows 三个安装包也重新构建，与 macOS 同为 1.9.5。

> **本轮自检 / 新增脚本**
>
> | 脚本 | 用途 | 结果 |
> |---|---|---|
> | `.build/make-icns.py`（新建） | 从 PNG 生成 macOS 的 `.icns`（ic11/12/07/13/09/10 六档），不动 Windows 图标 | ✅ 容器长度自洽、6 块解析完整 |
> | `.build/ci-watch.py`（新建） | 盯 GitHub Actions：等运行结束 → 报结论 → **失败就自动抓日志尾部** | ✅ 用上并拿到全绿结果 |
> | `.build/ci-fetch.py`（新建） | 取 Actions 日志 / 下载 dmg 产物。⚠️ 踩坑：日志与产物接口都会 **302 到预签名地址**，**第二跳再带 Authorization 会 401** —— 必须手动跟随重定向、第二跳去掉鉴权头 | ✅ 成功取回 6.9 MB dmg |
> | `cargo check` | 确认 Windows 端**没被改坏** | ✅ 通过 |
> | `.github/workflows/build-macos.yml`（新建） | macOS 通用二进制 dmg | ✅ 首次即成功 |

### 6.15 v1.9.6 ~ v1.9.8 三版（辅导员第 15~17 轮反馈）

三轮反馈连续到来，按"先修正确性、再谈体验"的顺序处理。挑出有设计价值的几条：

**① v1.9.6 修掉「无批次导入丢数据」（本项目第一个静默丢数据级缺陷）**

根因链条很短但很隐蔽：`setStudents(arr)` 只在 `activeBatch()` 存在时才写 `b.students`，
而 `buildSavePayload()` 持久化的只有 `batches` —— 于是"把批次全删光后再用新增/合并导入"时，
名单只活了在内存里：**结果页显示"导入成功、新增 78 人"，重开程序「全部学生」却是空的**。

修法分两层（都必要）：
1. **源头**：`doImportMerge()` 入口处若发现无批次，先用文件名自动建一个批次再落库，
   并在结果页明说"已自动新建批次「xxx」"——让用户知道发生了什么、可以纠正；
2. **兜底**：`setStudents()` 里加"无批次却要写入非空数据 → 当场建批次 + console.warn"，
   保证**任何**代码路径都不可能再把数据只写进内存。

> 教训：**"当前状态"与"持久化结构"不一致时，写内存就等于丢数据。**
> 这类 bug 的特征是"界面看着对"——所以只能在代码路径上设闸门，不能指望用户发现。

**② v1.9.7 导入硬约束：一个学号只会出现一次**

系统导出偶尔会出现同一学号多行。原先三种模式对它的处理各不相同（new 模式原样保留），
结果是"同一个学生出现两条记录"。现在统一：**所有模式都按学号合并**，并统计"合并了几行"给用户看。

**③ v1.9.8 内置表单模板：内联 vs 附件文件夹的取舍**

八份 PDF/Word/Excel（合计仅 0.30 MB）要"随程序走"，两条路：

| 方案 | 优点 | 缺点 |
|---|---|---|
| 放 Tauri `bundle.resources` | 不撑大前端；大文件也合适 | **绿色版从"一个 exe"变成"exe + 附件文件夹"**，破坏其最大卖点；浏览器版拿不到 |
| **内联 base64 进单文件**（采用） | 三端一致；绿色版仍是单文件；不受用户挪目录影响 | 原型 +416 KB（1.46 → 1.88 MB） |

选内联的决定性理由：**"一个 exe 拷到 U 盘就能跑"是这个项目反复强调的价值**，
为了 0.4 MB 把它换掉不划算。代价用 `.build/embed-templates.py` 的可重复生成来抵消
（改清单 → 重跑 → 替换 START/END 标记之间的内容）。

**④ v1.9.8 关注标签：给"没有数据来源"的事留位置**

`hasFocus()` 原本只看两类**有据可查**的信号（成绩表不及格门数、学生表挂科情况/军训备注）。
但辅导员真正在跟的事往往没有数据来源（心理、单亲、人际）。加一层可自定义标签，两个设计取舍：

- **存 emoji 本身，不存内部 id**：这样导出的 Excel 里直接就是 🎭⛄，用户也能在 Excel 里手写后导回
  （存 id 的话导出是 `psy,social`，用户看不懂、也没法手改）。
- **列表只显示图标、名称放 title**：标签是敏感信息，不该在一眼可见的列表里写"单亲"两个字；
  图标既保留了提示作用，又不至于被旁边的人扫到。
- ⚠️ 实现坑：👩‍👦 是 **ZWJ 组合字符**，`[...str]` 会把它拆成 👩 和 👦 两个 ——
  一律按"已知标签的 emoji 做子串匹配"还原；筛选也必须走「包含」而不是全等（一个字段里塞多个 emoji）。

**⑤ v1.9.8 备份体系：机制不重复，重复的是页面入口**

用户以为"固化和快照重复"，逐条对过之后：三者解决的是**不同场景**——
固化（最新状态镜像，防清缓存/重装）、回退点（导入前快照，防"导错了"）、备份（手动导出，唯一能带走的）。
真正的问题是**页面把三件事平铺成 10 个按钮 + 一张并列卡片，还出现两个"历史"**。
所以做的是**信息架构**而不是功能删减：按"你要干什么"分三组，
把四个备份类入口收进「备份与恢复」弹窗，并把"自动保护"降级为一行状态说明（写明"这不是备份、带不走"）。

> 顺带修掉一个真 bug：`S.importHistory` 只把**列表**截断到 30 条，
> 磁盘上的 `snap-*.json` **一份都不删** → 用久了堆几十上百份完整数据副本。
> 现在列表与磁盘同步只留最近 20 份（删除时校验只删 `snap-*`，绝不碰 `workstation-data.json`）。

**⑥ 本轮的测试坑（都写进了 `html-prototype-regression` 技能）**

| 现象 | 真因 |
|---|---|
| 备份导出测试报"文件名不对"、再往下 JSON.parse(CSV) 崩 | 沙盒缺 `TextEncoder`，报错被函数内 try/catch 吞掉（**先补沙盒全局，再怀疑产品**） |
| 内置模板测试读到 0 份 | 模板数据在**独立的 `<script>` 块**里，沙盒只加载了应用块 |
| 宿舍分段突然 rows=0 | 前面新加的断言**改了状态没还原**，把学生清空了（改状态的测试必须收尾还原） |
| "界面不该出现某文案"误报 | 用了整份 HTML 去断言，而自己的注释里恰好有那个词 → 只看渲染结果 |

### 6.3 原型与正式版的预期差异（沿用原稿，已核对无变化）

| 项 | 原型现状 | 正式版（桌面）目标 |
|---|---|---|
| 存储 | 浏览器 localStorage | SQLite（+ 可选整库加密） |
| 密码 | 简易哈希（djb2 变体） | Argon2id + 随机盐 |
| Excel 解析 | 前端 SheetJS 0.18.5（已内联，离线可用） | Rust calamine 或沿用 SheetJS，**二者都满足离线要求** |
| 数据量 | 内置 80 条演示 | 无上限（SQLite 支持百万级） |
| 安装 | 浏览器打开 | .msi 安装包 + 桌面快捷方式（图标就用 `assets/南大学生工作台.ico`） |
| 主题 | 3 套，localStorage 记忆 | 保持一致，改存 SQLite settings |
| **取数指引里的外链** | 由系统浏览器打开（`<a target="_blank">`） | 桌面壳需**显式交给系统浏览器**打开，不能吞在应用内窗口（同 5.10.5 那类环境差异，见附录 A35） |

---

## 七、后续版本规划（v0.7 修订）

**当前位置**：v0.1 设计稿 ✅ → v0.2 原型 + 数据实证 ✅ → v0.3 批次/三模式/窄屏 ✅ → v0.4 成绩层 ✅ → v0.5 排序/详情编辑/宿舍看板 ✅ → v0.6 页面个性化 ✅ → v0.6.1 内联自定义值 ✅ → v0.7 中南大定制 / 取数指引 / 首次引导 ✅ → **v0.7 已完成 ⬅ 下一步：v1.0 桌面版**

| 版本 | 内容 | 修订说明 |
|---|---|---|
| **v0.3** | 导入三模式（覆盖/新增/新建）· 数据批次与批次管理 · 顶部数字可点击 · 窄屏适配至 360px · 图表按真实像素重绘 | 由辅导员实际使用反馈驱动；本版起给原型配自动化回归测试（6.5） |
| **v0.4** | **成绩层**：成绩独立导入、按 `学号` 与学生画像绑定、列表/详情/看板/导出全覆盖；预设与顶部数字改成绩口径；明确放弃「学期趋势」 | 由系统真实导出模板驱动；数据底座换为「学生信息表 41 列 + 成绩信息表 11 列」，个人维护的旧表降为补充来源 |
| **v0.5** | **操作与信息补全**：列表表头排序 · 详情页全字段编辑（含成绩补录/搬移）· 新增宿舍看板（房间/床位/空位/男女寝）· 看板卡片瘦身（删 2 张废卡、增 3 张"值得关注"卡） | 由辅导员第 7 轮实际使用反馈驱动；本版**删掉了数据不支持的卡片、补上了每天真正会用到的操作**；宿舍解析缺陷靠"拿两套真实模板各跑一遍端到端"抓出 |
| **v0.6** | **页面个性化**：列表列自定义（勾选+拖动）· 详情页字段自定义与「新增自定义字段」· 枚举字段改选择题 · 筛选条件支持多选 | 由辅导员第 8 轮实际使用反馈驱动；本版把"显示什么"从写死改成可配，并解决"导入字段多但用不上"的问题 |
| **v0.6.1** | **桌面端硬约束补丁**：「自定义值」由系统弹窗 `prompt()` 改为**页面内联输入框**（详情页枚举字段 + 批次重命名） | 辅导员第 9 轮反馈"编辑页得能自定义，很多情况不在选项里"；根因是 **WebView 吞掉系统弹窗**，用户点了没反应。确立硬约束：**桌面版一律不用系统弹窗** |
| **v0.7** | **中南大定制与上手路径**：改名「中南大学生工作台」· 校徽图标 · 作者与反馈 · **取数 / 导入指引页** · **首次打开新手引导** | 辅导员第 10 轮反馈；本版把"软件叫什么、从哪拿数据、第一次怎么用"三件事补齐 —— **这些不是功能，却决定功能能不能被用起来** |
| **v1.0** | 原型功能完整落地为 Windows 桌面版；补上导入日志（文件哈希 + 历史日志表）；数据备份/恢复 | "落地范围"以 6.1 #1–12 + 6.4 #1–10 + 6.6 #1–7 + 6.8 #1–7 + **6.9 #1–7** 为准；批次模型、导入三模式、成绩层、宿舍看板、页面个性化**必须在桌面版完整保留** |
| **v1.1** | 成绩深化 —— **前置条件是教务系统能导出「课程级明细」（学号/学期/课程/成绩）**：单科挂科名单、按课程筛选、成绩变化趋势 | 原 v1.1/v1.2 的成绩项合并到此；**数据不支持就不启动**，见 6.7 |
| **v1.2** | 照片批量导入与学号自动匹配（解压 zip → 按学号关联 → 展示） | 原 v1.3 顺延 |
| **v2.0** | 批量导出（自定义模板）、多辅导员数据合并（离线） | ~~数据可视化看板~~ 已在原型阶段完成，从本行移除 |

### 7.1 v1.0 验收清单（由原型能力逐条转写，用作完工标准）

| # | 验收项 | 来源 |
|---|---|---|
| 1 | 首次启动设密码 / 再次启动解锁；密码以 Argon2id + 随机盐存 SQLite | 原型 #1 |
| 2 | 导入向导三步可用，Rust 端或前端解析 xlsx，确认页展示列识别结果 | 原型 #7 |
| 3 | 合并逻辑四条铁律全部生效：①备注永不覆盖 ②空值不覆盖 ③未出现的列保留 ④不静默删除+可回溯（导入日志 + 异常行清单） | 四.2 + 6.2 |
| 3b | **多来源表头归一**：字段别名表生效；先导 32 列再导 12 列，两边字段都不丢；导入确认页显示「已识别 / 新增字段 / 未识别」三态 | 4.3 |
| 3c | **字段缺失降级**：依赖缺失字段的视图/图表置灰并提示原因，不显示 0 | 2.1 第③条 |
| 3d | **新字段落地**：`室友`（详情+筛选）、`电子信箱`（详情） | 2.1 第④条 |
| 3e | **导入三模式**：新增/合并（默认）· 覆盖当前批次（备注按学号搬回 + 搬不回的列清单 + 自动备份）· 新建批次（独立快照） | 4.5 |
| 3f | **数据批次**：批次 Tab 条常驻可见；管理弹窗支持切换/重命名/删除（删前自动备份+二次确认）/单批次导出；上限 20 个批次；v1 数据自动迁移 | 4.4 |
| 3g | **窄屏 360px 起可用**：侧栏抽屉、表格卡片化、看板单列、顶栏精简、图表按真实像素重绘 | 5.5 |
| 3h | **顶部数字可点击**：列表页数字与看板指标卡均有明确落点；无自然落点的（覆盖班级）保持不可点 | 5.4 |
| 3i | **成绩层**：成绩表独立导入（自动定位第 3 行表头、剔除合计行并留痕、按学号 upsert、未出现者保留）；与学生画像按 `学号` 绑定；学生表有而成绩表没有的显示「暂无成绩」而非丢弃 | 4.6 / 5.6 |
| 3j | **成绩口径的预设与看板**：`有不及格` / `绩点偏低` / `暂无成绩` 三个视图；看板成绩分档（<70…95-100）与班级平均成绩；**未导成绩时一律置灰并说明原因，不显示 0** | 5.2 / 5.6 |
| 3k | **页面个性化**：列表列自定义（任意字段成列 + 拖动排序，设置落盘）· 详情页字段设置（隐藏不删数据）· 新增自定义字段（与导入字段等价：可录/可列/可筛/可导出）· 枚举字段选择题（选项来自本批 + 预置，当前值置顶，不吞历史脏值）· 筛选条件多选（同字段「或」· 跨字段「且」） | 5.10 / 5.11 |
| 3l | **禁用系统弹窗（桌面端硬约束）**：`prompt()` 在 WebView 下不生效 → 所有"当场输入一个自定义值"的地方必须用**页面内控件**；重命名类操作走**行内编辑**；`confirm()` 可用但文案需自持 | 5.10.5 |
| 3m | **品牌与作者信息**：名称为「中南大学生工作台」；应用图标用校徽（`.ico` 打包、base64 供界面内使用）；作者与反馈方式可见；**本地存储键不得随改名变更**（否则老盘数据全丢） | 5.14 |
| 3n | **取数指引与首次引导**：内置「取数 · 导入指引」页（智慧学工 / 综合教务两条取数路径 + 导入说明），**无数据时也可访问**；首次启动弹一次新手引导（走完/跳过记已看过，老用户不被拦）；外链交由系统浏览器打开 | 5.12 / 5.13 |
| 4 | 学生列表：统计条（5 个数字，全部可点）、筛选面板、条件 chips、常用筛选、行内关注标签、成绩列、备注摘要 | 原型 #2/#4 |
| 5 | 数据总览：6 指标卡 + 9 个图表模块，全部可点击下钻，图表与列表同口径 | 原型 #10 |
| 6 | 学生详情：7 组（41 列）字段 + 成绩区块（8 项）+ 备注就地编辑保存 | 原型 #5/#6 |
| 7 | 导出 CSV：全字段 + 绑定成绩（`成绩·××` 前缀）、带 BOM、按当前筛选结果 | 原型 #8 |
| 8 | 设置：三主题切换、改密码、清空数据 | 原型 #9/#11 |
| 9 | 顶栏育人金句：60 条，点击换句 | 原型 #12 |
| 10 | 数据备份/恢复：SQLite 单文件拷走即完整备份 | 四.1 |
| 11 | **离线验证**：断网（或防火墙阻断）后所有功能可用，进程无任何外连 | 一、需求确认结论 |
| 12 | 安装体验：.msi 双击安装，生成桌面快捷方式，卸载干净 | 六.3 |

---

## 八、桌面版（Windows）

> 对应工程：`app/`（Tauri 2.x）。出包命令见 8.4。

### 8.1 交付物与安装

Windows 产物以 `app/src-tauri/target/release/` 为根，macOS 产物由 CI 产出（见 8.7）——
**四个产物都能直接拿去用**（v1.9.5 实测体积）：

| 产物 | 路径 | 体积 | 特点 |
|---|---|---|---|
| **安装包（推荐）** | `bundle/nsis/中南大学生工作台_1.9.5_x64-setup.exe` | **3.7 MB** | NSIS，**按当前用户安装，不弹 UAC、不需要管理员**；装到 `%LOCALAPPDATA%\Programs\中南大学生工作台`，自动建桌面与开始菜单快捷方式 |
| 安装包（单位批量部署） | `bundle/msi/中南大学生工作台_1.9.5_x64_zh-CN.msi` | **4.5 MB** | WiX，安装界面为中文，**需管理员权限**（装到 `Program Files`）；适合通过域策略下发 |
| **绿色版** | `中南大学生工作台-v1.9.5-绿色版.exe` | **4.9 MB** | 单个 exe，拷到 U 盘或任意目录都能直接跑，**不写注册表、不留安装痕迹** |
| **macOS 版** | `bundle/dmg/中南大学生工作台_1.9.5_universal.dmg`（CI 产出） | **6.6 MB** | 通用二进制（Intel x86_64 + Apple 芯片 arm64），最低 macOS 10.15，临时签名（adhoc）；使用者首次打开需「右键 → 打开」放行 |

> 与 v1.9.3 **持平**（三个各差几 KB）—— v1.9.4 改的是导览文案与手册，没动前端体量。
> 体积变化史：v1.9.2 **+0.4 MB** 是换**完整版 SheetJS** 的代价（多了 `.xls` / Excel 97-2003 / CFB 解析
> 能力，见 6.11），那部分能力一直保留；v1.9.3 **−0.1 MB** 是把两张二维码裁成纯码 + 16 色量化
> （内联净省约 76 KB 字符，见 6.12）。

**运行前置条件**
- Windows 10 1809+ / Windows 11 x64。
- 需要 **WebView2 运行时**（Windows 11 已自带；Windows 10 多数机器随 Edge 一起装过）。
  若目标电脑没有，安装包会**自动联网下载并装一次**（约 1 分钟，校园网即可）。
  **纯离线的电脑**需先手动装好 "Microsoft Edge WebView2 Runtime" 再运行本软件。
- **不需要**装 .NET / Node.js / Rust —— 那些只在重新出包时才用到。
- 卸载程序**不会删掉用户数据**（有意保留，防误删；详见 8.2）。

### 8.2 数据存在哪

| 项目 | 位置 / 说明 |
|---|---|
| 主存储（程序实际读写） | `%LOCALAPPDATA%\cn.edu.zuel.studentworkstation\EBWebView\Default\Local Storage\` —— WebView2 的 localStorage |
| **数据镜像**（每次保存同时落一份） | `%APPDATA%\cn.edu.zuel.studentworkstation\workstation-data.json`（约 220 KB）—— 清理浏览器缓存、重置 WebView 或重装程序后，**下次打开自动从这里恢复**；这就是"卸载/重装不丢数据"的实现方式 |
| 导入历史快照 | 与镜像**同目录**；删除某条导入历史时会一并清掉它对应的快照文件 |
| 存储键 | 仍是 **`counselor_workstation_v2`**（与网页版同键，便于将来统一） |
| 卸载时 | **保留**（有意为之，防误删）。想彻底清除需手动删掉上述两个目录 |
| 换机 / 备份 | 系统设置 → 数据管理 → **导出完整备份**（JSON），新机器再导入；也可以直接拷走上面那个 `workstation-data.json` |
| 导出与看板产物 | CSV / 备份文件 / 查寝打分表都落在 Windows「**下载**」文件夹；「系统设置 → 数据管理 → 打开备份文件夹」一键打开 |

⚠️ **桌面版与网页版的数据是两份、互不通用** —— 同一台机器上分别用浏览器打开
`中南大学生工作台.html` 和运行桌面程序，看到的数据彼此独立（存在不同容器里）。

### 8.3 与网页原型的差异（桌面适配层）

原型 `中南大学生工作台.html` **没有为桌面改过任何一行**。桌面版由
`.build/build-desktop.js` 把它同步成 `app/src/index.html`，并在 `</body>` 前**多注入一段适配层脚本**，
只处理三件网页做不到的事：

| 症状（如果不处理） | 原因 | 桌面版做法 |
|---|---|---|
| **点"导出"没反应** | WebView 里 `a[download]` + `createObjectURL` **静默失效**——不报错、也不出文件 | 换成 Rust 命令 `save_to_downloads`，**直接写进「下载」文件夹**，重名自动加 `(1)`，弹提示告诉辅导员文件在哪 |
| **点链接窗口白屏** | `<a target="_blank">` 在应用里会把窗口自己导航走 | 拦截后交给 Rust `open_external`，**用系统默认浏览器打开**；只放行 `http/https/mailto`，其它协议一律拒绝 |
| **右键露出"刷新/另存为/检查"** | WebView 自带浏览器右键菜单 | 关掉；**输入框里保留**（否则没法粘贴） |

适配层**只在能拿到 `window.__TAURI__` 时才生效**：同一份文件用浏览器直接打开时，它整段退出、零副作用。
这条以及它与网页行为的边界，都由 `.build/test-desktop.js`（**28 条**）自动守住。

### 8.4 工程结构与重新出包

```
app/
├─ package.json                  只用于跑 tauri CLI
├─ node_modules/                 Tauri CLI（15MB；从 .build/tauri-probe 复制而来，避免联网重装）
├─ src/index.html                ← 由原型同步生成（不要手改，改了会被覆盖）
└─ src-tauri/
   ├─ tauri.conf.json            应用名 / identifier / 窗口尺寸 / CSP / 出包目标
   ├─ Cargo.toml                 productName 之外的 Rust 侧元信息
   ├─ capabilities/default.json  权限：core:default + opener:default + autostart 三个开关
   ├─ icons/                     由校徽生成的全套图标
   ├─ Cargo.lock                 锁死依赖版本 → 可复现、且能走本机缓存不重下
   └─ src/lib.rs                 Rust 侧命令（v2.0.0 共 15 个）：外链、导出落盘、打开目录/文件、
                                 数据快照读写删（save/load/delete_data_file）、托盘、开机启动、
                                 退出；**单实例唤回由 `tauri-plugin-single-instance` 插件负责，不是命令**
```

**重新出包四步**（改完原型，**第 1 步不能省**）：

```bash
node .build/build-desktop.js          # 原型 → app/src/index.html（重新注入适配层）
cd app
npm run tauri build                   # 出 exe + msi + setup.exe（约 2.5 分钟）
python .build/pack-release.py         # 按命名规则放进「发布」目录 + 同步文档里的文件名
```

改校徽：`python .build/make-icons.py <新校徽.png> app/src-tauri/icons`，再重跑上面几步。

**发布命名规则**（v1.9.1 起长期遵守，别再换写法）

> 起因：文件名里不写版本，老师过两周就分不清手里这份是哪一版，也分不清新旧。

| 用途 | 「发布」目录里的文件名 | GitHub Release 附件名 |
| --- | --- | --- |
| 绿色版 | `中南大学生工作台-v1.9.5-绿色版.exe` | `ZUEL-StudentWorkstation-v1.9.5-Portable.exe` |
| 安装程序 | `中南大学生工作台-v1.9.5-安装程序.exe` | `ZUEL-StudentWorkstation-v1.9.5-Setup.exe` |
| 单位部署 | `中南大学生工作台-v1.9.5-单位部署.msi` | `ZUEL-StudentWorkstation-v1.9.5-Deploy.msi` |
| macOS 版 | `中南大学生工作台-v2.1.0-macOS.dmg` | `ZUEL-StudentWorkstation-v2.1.0-macOS.dmg` |

- 格式：`<产品名>-v<版本>-<用途>.<扩展名>`；用途有四个：绿色版 / 安装程序 / 单位部署 / macOS。
  macOS 的 dmg **不在本机出**（Windows 编不出 Mac 程序），由 GitHub Actions 构建后下载过来，
  再随其余产物一起上传 Release。
- GitHub 附件必须用英文名（中文名会被清成 `-.exe`），中文名与英文名的对应关系写进 Release 说明。
- ⚠️ 中文名在 Release 附件里会被清成 `-.exe` / `-.dmg`，所以**上传给 GitHub 的一律用英文名**，
  中文名只用于「发布」目录里给人看的那一份。
- 版本号以 `app/src-tauri/tauri.conf.json` 的 `version` 为**唯一来源**，脚本自动读取，不手写。
- `pack-release.py` 会顺手清理「发布」目录里同用途但写法过时的旧文件，并同步
  `发布/安装说明.txt`、`README.md`、`.build/release-notes.md` 三处的文件名。

> ⚠️ `identifier` 定为 **`cn.edu.zuel.studentworkstation`**，它决定了数据目录的名字。
> **以后绝不能再改** —— 改了所有已安装用户的本地数据会"找不到"（等同于丢失）。

### 8.5 本次实机验证记录（2026-09-19）

| 检查项 | 结果 |
|---|---|
| 启动 | ✅ 窗口标题「中南大学生工作台」；首次运行为「设置访问密码」，界面渲染正常 |
| 图标 | ✅ 任务栏 / 窗口标题栏 / 安装列表均为校徽 |
| 数据落盘 | ✅ `%LOCALAPPDATA%\cn.edu.zuel.studentworkstation\…\Local Storage` 自动生成 |
| 导出链路（JS → IPC → Rust → 磁盘） | ✅ 文件真出现在「下载」文件夹，**BOM 与中文 UTF-8 均正确**（无乱码） |
| 外链白名单 | ✅ `ftp://`、裸字符串被拒；`https://` 正常用系统浏览器打开 |
| 回归测试 | ✅ 原型 8 套（含真实数据 78 项）+ 桌面适配层 28 条，全绿 |

**复验手法**（出包后想自己再验一次）：

```bash
node .build/build-desktop.js --selftest        # 出包时带开机自检
cd app && npm run tauri build                  # 跑起来即自检，报告写进「下载」文件夹
powershell -File .build/verify-desktop.ps1 -Exe <exe路径> -Out <截图.png>   # 启动 + 截图
```

> 截图用 `PrintWindow` 抓目标窗口本身，**不依赖窗口在前台**；
> 并且取"面积最大的可见顶层窗口"而非 `Process.MainWindowHandle`（后者有时会指向一个 24×24 的隐藏辅助窗口）。

### 8.6 已知限制（后续候选）

1. **存储还在 localStorage，不是 SQLite** —— 数据在 WebView 的 profile 里，用户手动清理该目录会丢；
   敏感名册的**落盘加密**也尚未做（见 6.2 表第 4、5 项，仍属待办）。
2. **没有"另存为"对话框** —— 导出固定进「下载」文件夹，重名自动加后缀。换来的是**零新增依赖**。
3. **导出只能出 CSV**（与网页版一致）；将来需要 xlsx 导出再另做。
4. **首次启动约 1 秒白屏** —— 尚未做"先隐藏窗口、就绪再淡入"的打磨。
5. 安装包里 WebView2 用的是**在线引导**方式（缺失时联网装）；纯离线计算机需预先装好运行时。

### 8.7 macOS 版怎么出包（v1.9.5 起）

**本机（Windows）出不了** —— 苹果的工具链只在 macOS 上跑，Tauri 也不支持从 Windows
交叉编译到 macOS。所以走 **GitHub 的 macOS 跑器**（公开仓库免费）：

```
推代码 / 打 tag / 在 Actions 页手动点 Run workflow
        ↓
.github/workflows/build-macos.yml
  ① actions/setup-node@v4（Node 20）
  ② dtolnay/rust-toolchain@stable，targets = aarch64-apple-darwin,x86_64-apple-darwin
  ③ Swatinem/rust-cache@v2（缓存 app/src-tauri，第二次起快很多）
  ④ cd app && npm ci
  ⑤ cd app && npm run sync                    ← 生成 app/src/index.html（不在版本库里，必须先生成）
  ⑥ cd app && npx tauri build --target universal-apple-darwin
       ⚠️ env: APPLE_SIGNING_IDENTITY = "-"    ← 临时签名，不设的话 Apple 芯片上起不来
  ⑦ 核对：lipo -archs（应出 x86_64 arm64 两行）、codesign -dv（应 Signature=adhoc）
  ⑧ 上传 dmg 为 artifact
        ↓
本机下载：python .build/ci-fetch.py artifact <run_id> .build/ci-out
```

关键取舍：

- **`universal-apple-darwin` 一次出两种架构**，一个 dmg 通吃 Intel 与 M 系列，不用发两个包。
- **临时签名不能省**：arm64 要求可执行文件至少有 ad-hoc 签名，否则双击直接失败。
- **不公证（notarize）**：那需要 Apple 开发者账号（$99/年）。代价是使用者第一次打开被
  Gatekeeper 拦一次，必须「右键 → 打开」放行 —— **已在手册 / Release 说明 / 安装说明三处写明**，
  否则同事会当成"文件坏了"。
- **macOS 的 dmg 不参与本机的 `pack-release.py`**：它不在本机产出，所以由人（或脚本）
  下载后放进「发布」目录，再由 `publish-release.py` 一并上传 Release。
- **`app/src/index.html` 不进版本库**（`.gitignore` 里挡着），CI 里**必须**先 `npm run sync`，
  否则会把空的前端打进包 —— 所以工作流里专门加了一步 `test -s src/index.html` 做断言。

---

## 附录 A：文档 ↔ 原型 差异核对表

> 核对日期 2026-09-19（v0.2 首轮；v0.3、v0.4 复核；v0.7 更名）；核对方式：逐段读 `中南大学生工作台.html` 与 `中南大学生工作台_方案说明.md` 对照。

| # | 议题 | 文档（v0.1）写法 | 原型实况 | 判定 | 处置 |
|---|---|---|---|---|---|
| A1 | 字段总数 | 「32 列原样保留」，但字段表实际只列了 **31 个名字** | 演示数据每条含 **32 个字段**（与文档的差集 = **「家长电话」**） | 文档有遗漏 | 已修正（见 二章字段表下方告示 + 原型分组已含该字段） |
| A2 | 数据总览看板 | 5.1 页面结构、六.交付清单**均未提及** | 已实现完整看板：6 指标卡 + 班级柱状/政治面貌环形/成绩分布/宿舍楼条形/毕业学校词云/关注提醒/备注完成度/生源地 Top10 | 实现超纲 | 已补回文档（5.1 ➕、6.1 #10） |
| A3 | 主题切换 | 未提及 | 明亮 / 暗黑 / 护眼 三套，双写 localStorage 且首屏预载防白闪 | 实现超纲 | 已补回文档（6.1 #11） |
| A4 | 育人金句 | 未提及 | 60 条分五类，顶栏 + 看板欢迎条两处展示，点击换句 | 实现超纲 | 已补回文档（6.1 #12） |
| A5 | Excel 解析 | 「联网时用 SheetJS，离线降级演示数据」 | SheetJS 0.18.5 已**完整内联**（约 245KB），离线为真实能力 | 文档已过期 | 已更正（6.1 重要更正） |
| A6 | 导入日志 imports 表 | 4.1 列为核心表；4.2 铁律③「可回溯」 | **v0.3：批次已记录导入时间/来源文件/模式/人数；结果页有本次增改计数**（仍缺文件哈希与历史日志表） | 🟡 部分实现 | 剩余部分列入 v1.0（7.1 #3） |
| A7 | 不静默删除 | 4.2 铁律② | **v0.2 已实现**：合并模式列出「本次未覆盖（仍保留）」名单 | ✅ v0.3 修正本节（原判定"实现缺失"已过期） | 已关闭 |
| A8 | 异常行（学号为空） | 4.2 要求「单独列出」 | **v0.2 已实现**：不写入并给出**对应 Excel 行号** | ✅ v0.3 修正本节（原判定"静默跳过"已过期） | 已关闭 |
| A9 | classes / courses 表 | 4.1 列为核心表 | 无表，班级由 students 派生 | 预期内差异 | v1.1 做成绩表时一并落地 |
| A10 | 挂科情况语义 | 「12 条有值，均为 "1" → 布尔标志」 | 详情页按「有 N 门」渲染，按计数语义处理 | 语义不一致 | v1.0 统一：优先按数值显示，非数值显示「有记录」 |
| A11 | 班级数量 | 5.1 写死「我的班级（11）」 | 侧栏按实际班级**动态生成** | 文档表述不当 | 已在 5.1 注明"按实际数量动态生成" |
| A12 | 六.1 关注视图数量 | 「预设关注视图（6 个）」 | v0.3 后 PRESETS 共 8 个（含"全部新生"），侧栏展示 7 个 | 表述可接受 | 已在 5.1 / 5.2 注明口径 |
| A13 | 存储与密码强度 | SQLCipher / Argon2id | localStorage / 简易哈希 | 预期内差异 | 属原型定位，v1.0 落地 |
| A14 | **数据来源数量** | 只描述了迎新系统一份表（32 列） | 实际存在**第二份来源**：新系统导出（12 列），列名与列集均不同 | **需求遗漏** | 已补（2.1 新增章节） |
| A15 | **列名归一** | 全文未提；原设计假定"字段名按你的表格自动识别"即可 | 两个来源对同一件事用不同列名（宿舍/住宿地址、证件号码/身份证件号、联系电话/手机号） | **设计缺口** | 已补（4.3 字段别名表） |
| A16 | **合并策略** | "UPDATE 基本信息"（等价整行覆盖） | 新表仅 12 列，整行覆盖会抹掉旧表 22 个字段 | **有数据丢失风险** | 已升级为「按列合并 + 空值不覆盖」（4.2，铁律 3→4 条） |
| A17 | **「清空全部数据」的实际行为** | 未提及 | **v0.2 及以前：删 key → 重启后代码发现库为空 → 又灌回 80 条演示数据**（"清空后导入"其实是在演示数据上做合并） | **实现缺陷（v0.3 修复）** | 已改为软清空（写回空批次表），并补空状态引导（4.4） |
| A18 | **学生姓名列** | 2 章字段表同时列了 `姓名1` 与 `姓名`，未说明二者关系 | 迎新表两列同值；**新系统表只有 `姓名`** → 从新系统导入的新学生 `姓名1` 为空，而列表/详情/搜索只读 `姓名1`，**姓名显示为空白** | **实现缺陷（v0.3 修复）** | 新增 `studentName()` 兜底；搜索同时匹配两列（5.4、6.4 #10） |
| A19 | **图表尺寸** | 5.1 未规定 | `barChart` 写死 `viewBox="0 0 560 210"` + 等比缩放 → 窄屏轴字缩到约 5px 不可读、宽屏卡片被拉高到 500px+ | **实现缺陷（v0.3 修复）** | 改为按容器真实像素宽度重绘（5.5） |
| A20 | **快速搜索的计数一致性** | 未提及 | 打字时只刷新统计条第 1 个数字，另 3 个与表格对不上 | **实现缺陷（v0.3 修复）** | 抽出 `statsHtml()` 一次刷新 4 个数字（5.4） |
| A21 | **数据来源（第三次修订）** | 2.1 记述「旧表 32 列 + 新系统 12 列」 | v0.4 拿到系统**真实导出模板**：学生信息表实为 **41 列**（远比原认知的 12 列丰富），另有**成绩信息表 11 列** | **口径变更** | 已补（2.2 权威口径）；个人维护的旧表降为补充来源 |
| A22 | **成绩模块的实现基础** | 4.1 有 courses/成绩表设计；详情页曾有「成绩总览」占位 | v0.3 前"成绩"只是学生表内三个字段（加权平均成绩/专业排名/年级排名），而**新模板里一个都没有** | **需重做** | 已建独立成绩层并绑定显示（4.6 / 5.6） |
| A23 | **学期维度** | v1.2 曾规划「成绩变化趋势」 | 系统导出的成绩表是**一人一行的总评快照**：**无学期列**、无课程级明细 | **数据不支持** | 已明确放弃该功能（6.7），不再列为待办 |
| A24 | **教务系统导出格式陷阱** | 未提及 | `成绩信息.xlsx` 可能实为 **HTML 伪装成 .xlsx**（在"正在准备导出"页另存所致），zip/openpyxl 打开报 `BadZipFile` | **操作陷阱** | 已写入 2.2 ④：真 xlsx 头 `50 4B`、HTML 头 `3C 21 44 4F` |
| A25 | **成绩层首装引用脱钩** | —— | v0.4 初版 `boot()` 建演示批次时只同步了 `S.students`、漏了 `S.grades`，触发 `save()` 引用自检告警 | **实现缺陷（v0.4 修复）** | `boot()` 补同步；`.build/test-grade.js` 已覆盖 |
| A26 | **宿舍房间被拆碎** | 未提及 | v0.5 初版宿舍解析按"第一个 `-`"切分 → `住宿地址=滨湖1栋634-03` 被拆成房号 `滨湖1栋634` + 床位 `03`，**189 人 = 188 间房**，空位算出 500+ 个假值 | **实现缺陷（v0.5 修复）** | 新增 `_normRoomKey()` 归一为 `楼栋-房号`；无床位列时末段两位数字按床位剥离；`test-sort-dorm.js` + `verify-real.js` 各补回归 |
| A27 | **"只验一份真实模板"的盲区** | —— | `verify-real.js` 原只喂 41 列模板（有独立房间号列，走最干净的分支），**12 列模板（住宿地址挤一格）的解析缺陷长期未暴露** | **测试盲区（v0.5 修复）** | `verify-real.js` 扩展到宿舍/排序/预警，并明确"真实数据要覆盖所有导出模板"；见 6.5 教训 |
| A28 | **看板里的空数据卡片** | v0.2 原型做了 8 个图表模块（文档未写） | 其中「**宿舍楼分布**」真数据下一栋楼装百余人、**太碎**；「**毕业学校词云**」依赖的列**新模板根本没有**，永远空白 | **设计缺陷（v0.5 修复）** | 两卡删除；补「学业预警名单」「宿舍分配概览」，并把"单类别自动隐藏"写进规则（6.8 #5/#6） |
| A29 | **详情页只读** | 原稿只承诺「备注编辑」 | v0.4 及以前详情页 40+ 字段全为只读，字段录错只能回 Excel 改再整表重导 | **能力缺口（v0.5 补上）** | 详情页整页可编辑 + 成绩补录/搬移；见 5.8 |
| A30 | **列表列写死** | 5.1 列了 11 列固定列 | `SORT_COLS` 是硬编码常量，`renderRows` 逐 `<td>` 手写 —— 想加「邮箱」列必须改代码 | **能力缺口（v0.6 补上）** | 拆成 `FIXED_COLS` + 通用单元格渲染 + `S.listCols`；列与顺序可配、可拖动、落盘；见 5.10.1 |
| A31 | **枚举字段靠手输** | 未提及校验 | 政治面貌/班级等自由输入 → 出现「英语2401」与「英语 2401」两个班、「共青团员」与「共青团员 」两个值 | **数据质量缺口（v0.6 补上）** | 枚举字段改「选择题」，选项来自本批数据 + 预置；**当前值置顶保留**，不吞历史脏值；见 5.10.4 |
| A32 | **筛选只能单选** | 5.2 一个字段一个值 | 想看两个班只能来回切；`S.filters` 的值是字符串，无法叠加 | **能力缺口（v0.6 补上）** | 值升级为数组（同字段「或」、跨字段「且」），仅改 `applyAll()` 一处；兼容旧字符串写法；见 5.11 |
| A33 | **「新建批次会丢弃整列全空的字段」** | 未提及 | `doImport` 的 new 分支只收 `v != null && v !== ''` 的字段 → 本批全空列（如未启用的「电子信箱」）**不进字段清单** | **既有设计（v0.6 明确并记录，非缺陷）** | 符合"不存空值"；需启用时用**自定义字段**建同名即可；已写进 6.5 自检避免被当 bug 改掉 |

| A34 | **「点了『手输其他』没反应」** | 未提及 | `onSelectField` 用 `prompt()` 收自定义值；原型在浏览器里正常，**放进桌面外壳（WebView）后系统弹窗被吞掉**，用户以为功能坏了 | **环境假设缺口（v0.6.1 补上）** | 所有自定义值输入改**页面内联输入框**，重命名改**行内编辑**；确立"桌面版不用系统弹窗"硬约束；静态断言守住源码内无 `prompt(`；见 5.10.5 |
| A35 | **「第一份 Excel 从哪来」文档没写** | 只写了"导入一份 Excel 名册"，未写去哪导出 | 文档假设辅导员自己会从校内系统取数；但**取数路径（菜单层级、导出方式、筛选口径）恰恰是最容易卡住的地方**，且各校不同 | **上手路径缺口（v0.7 补上）** | 内置「取数 · 导入指引」页 + 首次新手引导，把智慧学工/综合教务两条路径写死进软件；**说明要放在软件里，而不是另一份文档里**；见 5.12 / 5.13 |
| A36 | **改名可能连带改掉存储键** | 未提及 | 若把产品名换成新名字时"顺手"也改了 `localStorage` 的 key，老盘上的全部数据（批次/备注/成绩/个性化）会**静默丢失**，表现为"软件变空了" | **改名风险（v0.7 规避）** | 改名只动展示层，**存储键 `counselor_workstation_v2` 保持不动**；回归里有一条专门断言守这条；见 5.14 |

**一句话总结（v0.7 更新）**：
文档在"**该做的**"上是可靠的（需求结论、数据模型、合并铁律、界面交互要点基本被原型忠实执行）。
偏差集中在"**已经做到了哪一步**"、"**数据实际长什么样**"、"**软件实际跑在什么环境里**"，
以及 v0.7 新增的一类：**"用户之外的人（使用者）拿到手会不会用"**。
v0.4 的收获是**拿到真实模板后口径要重定**（学生表 41 列而非 12 列、成绩是总评快照而非课程明细，故「成绩趋势」在数据上站不住，主动放弃）；
v0.5 的收获是**真实数据不止一种形态**——同一个「住宿地址」在不同模板里是"一格"还是"三列"，不各跑一遍端到端就会漏（A26/A27）；
v0.6 的收获是**"字段多"本身就是需求**——系统导出 41 列，但辅导员日常只看十来列，
于是本版把"显示什么"从写死改为可配（A30/A32），并用选择题把枚举值"改不出脏值"（A31）；
**v0.6.1 的收获是"浏览器里能跑"不等于"桌面版能跑"**——系统弹窗、文件选择、剪贴板这类**浏览器 API 在 WebView 里行为不同**，
必须在原型阶段就避开（A34）。**A34 也提醒：原型越像成品，越容易在"最后套壳"时才暴露环境差异，所以外壳要尽早套一次试跑。**
**v0.7 的收获是"能用"和"会用"是两件事**——功能齐了、测试全绿，但辅导员第一次打开时仍会卡在"第一份 Excel 从哪导"（A35）。
**取数路径这类知识必须写进软件本体**，而不是留在一份只有作者会看的方案文档里。
**A17/A18/A25/A26/A34/A35 属同一类：用户没报、测试先抓到** —— 这正是 v0.3 起给原型配自动化回归测试（6.5）的持续收益。
**A36 则是"改动自身的风险"**：改名看似只动文案，却可能顺手带走数据 —— 所以**凡涉及标识符的改动，都必须有一条断言盯着**。

---

## 附录 B：v1.0 桌面版落地路线 · 三方案差异分析（✅ 已定案）

> **v1.0 更新：本附录的"待决策"已经结束 —— 采用方案甲（原型直套 Tauri 外壳）并已落地出包。**
> 实际结果写在 §八：两条出包路线（NSIS per-user / WiX per-machine）+ 绿色版单 exe，
> 安装包 1.5–2.2 MB，回归与实机验证全绿。下面保留原分析过程，供日后考虑重构（方案乙）时参考。

> 前提：本机已具备 Node.js 22 / npm 10 / WebView2 / VS2022 / Git；**Rust 1.98.1 已装到 `D:\Rust`**（`D:\Rust\bin` 在用户 PATH），
> 且已跑通 `cargo new` → `cargo build` → 运行的端到端验证（VS2022 的 link.exe 能被 rustc 自动找到），Tauri 的编译前置已具备。
>
> **v0.3 补充 · 安装过程的两个坑（值得记录）**
> 1. **`rustup-init.exe` 在本机装不上**：三次均卡在创建硬链接 `rustup.exe → rust-analyzer.exe`，报
>    `当文件已存在时，无法创建该文件 (os error 183)`；换用户目录 / 项目目录 / 独立 `CARGO_HOME` 都一致，
>    而 Git Bash 的 `ln` 建硬链接本身正常。排查指向**安全软件（火绒 HIPS，进程 HipsDaemon）拦截硬链接创建**。
>    → 绕法：改用**官方免 rustup 的完整工具链包**（`rust-1.98.1`，走清华 TUNA 镜像，全程只复制不建硬链接），
>    装到 `D:\Rust` 而非用户目录，彻底避开 `~/.cargo/bin` 里那个 0 字节的 `rust-analyzer.exe` 残留。
> 2. **解压可能中断而不报错**：缺少 `components` / `version` 等安装描述文件时，`install.sh` 会报
>    `unable to find installation components`。核验方式是逐条 `[ -e "$path" ]` 判存在（**不要**用
>    `tar -tf | grep -v "/$"` 与 `find -type f` 对数——bsdtar 列目录不带结尾 `/`，会把目录算成文件，得到假警报）。

### B1 三条路线到底差在哪（大白话版）

| | **方案甲 · 原型直套外壳** | **方案乙 · 按方案重写 Vue3** | **方案丙 · 改用 Electron** |
|---|---|---|---|
| 一句话 | 把已经做好的原型网页，装进一个"Windows 软件壳"，再把浏览器存储换成真正的本地数据库文件 | 同样的功能，用"零件化"的方式重写一遍（每个界面拆成独立组件），再接数据库 | 用纯 Node 生态打包，不用装 Rust |
| 界面会不会变 | **完全不变**，原型什么样，软件就什么样 | 目标一致，但重写过程必然有细节走样，**每页都要重新验收** | 不变 |
| 要不要装 Rust | 要（一次性，约 500MB，需联网下载） | 要 | **不用** |
| 安装包体积 | 约 10MB | 约 10MB | 约 120MB |
| 内存占用 | 低 | 低 | 高 |
| 代码整洁度 | 一般（单文件原生 JS，约 1400 行逻辑） | **最好**（组件化、类型安全） | 一般 |
| 后续改功能难度 | 中等 | **最低** | 中等 |
| 出可用软件的速度 | **最快** | 最慢（≈把 1900 行重新组织一遍） | 快 |
| 主要代价 | 技术债留到以后 | **工期最长，期间手里没有可用的软件** | 与"常驻轻量"诉求相反，v0.1 已否决 |

### B2 核心权衡（一句话）

- 甲乙的差别不是"能不能用"，而是"**先拿到能用的软件、以后再整容**" vs "**先整容、晚点才能用**"。
- 丙的问题只有一个：体积和内存。如果哪天不介意 120MB 安装包，它是环境准备最快的一条路。

### B3 建议

**推荐方案甲**，理由三条：
1. 原型已经 100% 覆盖设计稿的功能，并多做了看板、主题、金句——**没有"功能还没想清楚"的风险**，甲能最快把它变成"能双击安装、桌面上有个图标"的真软件；
2. 甲乙的**业务逻辑完全可复用**（筛选、合并三铁律、看板口径这些代码搬到 Vue 里几乎原样能用），所以甲不是"白干"，是把工期拆成了两段；
3. 真正决定是否值得做乙的，是"**用起来之后觉得哪里别扭**"——先用甲跑一段时间，让问题自己暴露出来，再决定重构方向，比现在拍脑袋更靠得住。

---

### B4 ✅ 决策结果（2026-09-19 · v1.0 已完成）

**采用方案甲，并已交付。** 与原设想的三点出入，都已如实写回 §八：

| 原设想 | 实际结果 | 说明 |
|---|---|---|
| 装到 `Program Files`（需管理员） | **默认给的是 NSIS per-user 版**（不需要管理员），另有 MSI 版供单位批量部署 | 辅导员自己装软件多半没有管理员权限，这点调整是为了让他能自己装上 |
| localStorage 换成 SQLite | **没换**，仍用 WebView 的 localStorage | 见 §8.6 / 6.2：换 SQLite 属下一步，不是 v1.0 的阻塞项 |
| 换 Crypto 加密存储 | 同上，**尚未做** | 敏感名册的落盘加密仍是待办 |

剩余待办一句话：**先让辅导员用起来，把"哪里别扭"跑出来，再决定是先上 SQLite 还是先重构（方案乙）。**

若认可，下一步就是：安装 Rust 工具链 → 用 Tauri 初始化工程 → 把原型前端挂上去 → 把 localStorage 换成 SQLite → 补导入日志/异常行/备份恢复。

> 附：方案乙的收益（组件化、TS 类型安全、Naive UI 现成表格）在**功能已经稳定之后**才真正划算；方案甲的债也主要是"改起来不够清爽"，不影响功能与交付。
